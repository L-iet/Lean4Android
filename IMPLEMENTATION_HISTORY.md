# Implementation history

This is the durable engineering log for Lean4Android. Entries summarize shipped code, validation, and decisions; detailed future work remains in `IMPLEMENTATION_PLAN.md`.


## 2026-08-11 — M0 scaffold and M1 probe boundary

### Implemented

- Created an Android multi-module Gradle scaffold: `app`, `core-model`, `core-process`, and `core-toolchain`.
- Added a minimal Compose screen that reports whether the packaged Lean/Lake executables and writable Lean sysroot are present.
- Added typed toolchain layout/health models. Executable paths resolve only through Android's read-only `nativeLibraryDir`; sysroot data resolves under `noBackupFilesDir`.
- Added a bounded, shell-free one-shot process runner with separate stdout/stderr draining, a real blocking-process timeout, and forced termination. Long-lived LSP supervision remains separate by design.
- Added pinned toolchain inputs, a JSON manifest schema, and valid/invalid Lean conformance fixtures.
- Added initial unit tests for toolchain-ID/path safety and shell-free argument preservation.
- Added repository ignores and explicit build dependency versions.
- Added the official Gradle 8.13 wrapper launcher and JAR, with distribution URL validation enabled.

### Decisions

- Initial platform: `arm64-v8a`, minimum API 29, target/compile API 36.
- Build stack: AGP 8.13.2, Gradle 8.13, Kotlin 2.3.0, JDK 17. This avoids beginning on AGP 9's built-in-Kotlin migration while remaining on a supported stable stack.
- Initial Lean pin: 4.32.1 with toolchain revision ID `lean-4.32.1-android1`; NDK pin: 28.2.13676358.
- Native executables will be APK-installed `.so`-named ELF executables. Writable app directories contain data and projects only, never downloaded executable code.
- Backups are disabled for the initial scaffold until project backup/export and large toolchain-data behavior are deliberately designed.

### Validation

- Source/configuration structure was inspected locally.
- The toolchain JSON schema parses successfully; official wrapper artifacts were downloaded and checksummed.
- Installed JDK 17 and the pinned local Android SDK 36, build-tools 35.0.0, NDK 28.2.13676358, CMake 3.22.1, and platform-tools packages.
- Migrated all modules to Kotlin 2.3's typed `compilerOptions` JVM-target DSL and fixed the toolchain locator's `File` constructor reference found by the first compilation.
- `./gradlew testDebugUnitTest :app:assembleDebug` passes; the model/process unit tests succeed and the debug APK is produced.
- Installed the debug APK on a physical arm64 Android device. The Compose probe launched successfully and correctly reported all three not-yet-packaged toolchain components as absent.
- No Lean Android binaries exist yet, so the probe is expected to report a missing toolchain.

### Next

- Implement the reproducible Lean host-bootstrap/Android-target build and ELF audit.
- Replace the inspection-only probe with `lean --version`, valid-file, invalid-file, and LSP-handshake device probes.

## 2026-08-11 — M1 reproducible cross-build pipeline

### Implemented

- Pinned the exact Lean, LibUV, and OpenSSL commits in addition to their release tags.
- Added scripts that fetch and verify source revisions, build the native host bootstrap, cross-build static Android dependencies, build Lean's generated C/runtime for Android arm64 API 29, assemble a distribution, audit ELF architecture/dependencies, and generate a hashed manifest.
- Added a narrow upstream patch that applies Lean's ELF/PIC linker behavior to Android. Android retains upstream symbol exports required by interpreted/native module loading.
- Kept generated sources, build trees, and distributions outside version control; promotion into APK inputs remains explicit.

### Validation in progress

- The fetch stage resolves Lean `f054605aea4b840552cca2e725580bffd1e1b704`, LibUV `e9f29cb984231524e3931aa0ae2c5dae1a32884e`, and OpenSSL `7b371d80d959ec9ab4139d09d78e83c090de9779` and applies the Android patch cleanly.
- LibUV 1.48.0 and OpenSSL 3.6.0 cross-compile successfully with NDK 28.2 for arm64 API 29; archive members are verified ELF64 AArch64 objects.
- Lean's host stage0 bootstrap completes and runs. The Android target correctly uses it as the previous-stage compiler instead of unnecessarily building host stage1.
- Source validation now rejects revision drift, untracked dependency sources, and any Lean source delta other than the exact checked-in Android patch.
- Resume check: no build process was left running. `bash -n toolchain/scripts/*.sh` passes, the host bootstrap reports Lean `4.32.0-pre`, all three Android dependency archives are present, and the pinned source checkout integrity checks pass.
- Resumed `LEAN4ANDROID_JOBS=16 toolchain/scripts/build-android.sh`: CMake configuration and all native runtime/core targets completed, then the stdlib phase failed because Clang was invoked for generated `Lean/Meta/InferType.c` before that file existed. This is under investigation as a parallel generated-source dependency race; no Android executable was linked by this run.
- A `-j4` retry reproduced the missing `InferType.c`: the interrupted build had retained its primary `.olean` while its generated C side output was incomplete. Moving the still-active `.tmp` was aborted after it exposed a corrupt sparse file; the run was stopped before accepting that output.
- Quarantined the complete generated `InferType` artifact set under `/tmp/lean4android-infertype-recovery` and restarted at `-j1`. The serial rebuild recreated valid `.olean`, `.ilean`, and UTF-8 C output atomically and is continuing cleanly through Lean compiler/meta modules. This recovery build is currently running; the Android executable has not linked yet.
- After a host crash, confirmed no build process survived and resumed `LEAN4ANDROID_JOBS=1 toolchain/scripts/build-android.sh` from the intact build tree. CMake accepted the recovered `Lean/Meta/InferType` outputs, reused completed native targets, and advanced into new `Lean/Compiler/LCNF` modules without error; the serial recovery build is running again.
- The resumed serial build completed the remaining LCNF passes, the `Lean/Compiler` aggregate, parser compiler, module parser, and pretty-printer/delaborator layers. It has entered elaborator modules with no recurrence of missing or malformed generated C, including successful downstream use of the recovered compiler/meta outputs.
- The same run subsequently completed core term/command elaboration, structural and well-founded recursion, pre-definition, deriving, built-in simplifier procedures, and the BVDecide prover/tactic aggregate. It also rebuilt `Lean/Meta/Sym/InferType`, the last `InferType`-named module with a crash-era dependency marker, without error; the serial build continues toward top-level libraries and linking.
- The recovery build then completed the full symbolic-meta, `grind`, Cutsat/linear/commutative-ring arithmetic, `Lean/Meta/Tactic`, user-facing tactic, `do` verification-condition/proof-mode, Omega, `Lean/Elab/Tactic`, and `Lean/Elab` aggregates. No generated-source or compiler failure recurred; remaining work in this run is top-level Lean/server library assembly and native linking.
- The run completed `Lean/Meta`, linter, completion, file-worker, RPC, widget, server, utility, and top-level `Lean.lean` generation, then native C compilation exposed the other artifact left incomplete by the host crash: `Std/Sat/AIG/RefVecOperator.c.tmp` existed while the final `.c` did not, despite completed primary Lean outputs. Clang therefore failed with a missing input; this is the same interrupted atomic-output condition seen for `InferType`, not a new source dependency failure.
- After the next computer restart, no build process remained. The filesystem shows that the interrupted generator completed its atomic rename before termination: `RefVecOperator.c` is present and nonempty, and its `.tmp` no longer exists. The intact serial build tree is ready to resume at `-j1` without quarantining or deleting artifacts.
- Resumed `LEAN4ANDROID_JOBS=1 toolchain/scripts/build-android.sh`. Configuration completed, the runtime/core targets through `leancpp` were reused, and native compilation of the generated Lean library C sources is progressing cleanly through `Lean/Meta`; the recovered `RefVecOperator.c` was accepted with no missing-input recurrence. This serial build is currently running.
- The resumed run completed native compilation of all 2,271 generated main Lean C modules, including the crash-adjacent `Lean/Meta/Sym/InferType.c`, and produced the 313 MB Android `libLean.a`, plus `libLeanc.a` and `libLeanIR.a`. It has advanced into Lake source/C generation and remains active at `-j1` without errors; final executables have not linked yet.
- The run completed all Lean/Std/Lake generation and reached `make_stdlib`, then the first shared-library link failed on `-lc++_static`. ELF inspection revealed the underlying configuration defect: Lean's generated `leanc.sh` had captured CMake's generic host `clang` path without the Android toolchain's separate target arguments, so the generated native objects and archives were x86-64. The build now pins `LEANC_CC` to the NDK's API-29 AArch64 driver, and the upstream patch routes both `leanmake` and `leanc.sh` through that configured compiler. `.olean` outputs remain reusable, but all target C objects/archives must be rebuilt and audited as AArch64.
- Deleted only architecture-dependent `.o`, `.o.export`, `.a`, and `.so` files from the Android build tree and restarted at `-j1`, preserving generated C and Lean artifacts. Regenerated `stdlib.make`/`leanc.sh` both select `aarch64-linux-android29-clang`; newly rebuilt CMake objects and an extracted `libleanshell.a` member are verified ELF64 AArch64. The corrected rebuild is currently running through native runtime/core targets.
- After resuming again, the source-integrity guard stopped before compilation because the checked-in patch omitted a trailing blank context line from its final hunk. The patched Lean sources themselves were unchanged; the patch was canonicalized to exactly match `git diff --no-ext-diff`, restoring the guard's byte-for-byte invariant.
- The corrected AArch64 rebuild completed `make_stdlib`, `leanrt_initial-exec`, and the Lean/Lake shared-library targets through 72%, then failed linking `lake`: `libleanshared.so` retained unresolved C++ ABI symbols. The NDK's static libc++ interface is its API-specific `-lc++` linker script, which groups `libc++_static.a` with `libc++abi.a`; the prior `-lc++_static` override selected only the first archive. The Android configuration now uses `-lc++` so exception, RTTI, and allocation symbols are included. Existing AArch64 objects remain reusable; only shared libraries and executables require relinking.
- With `-lc++`, the targeted relink succeeded: all Lean/Lake shared libraries and executables linked and the full build reached 100%. Distribution audit then correctly rejected their unbundled `libc++_shared.so` dependency. NDK driver inspection showed its static-STL sequence is `-Bstatic -lc++ -Bdynamic`; the configuration now spells those mode switches explicitly so the API linker script supplies both `libc++_static.a` and `libc++abi.a` without introducing a shared-STL runtime dependency.
- The static-STL relink completed successfully. Independent `llvm-readelf` inspection confirms all five shared libraries and the `lean`, `lake`, `leanc`, `leanir`, and `leanchecker` executables are ELF64 AArch64; their dynamic dependencies are limited to packaged Lean libraries and Android system libraries, with no `libc++_shared.so`. `assemble-distribution.sh` then passed its complete ELF/dependency audit and generated the hashed `lean-4.32.1-android1` distribution: 14,899 files occupying 3.1 GB, including its manifest.
- Added explicit APK staging from the audited distribution. All native executables/shared libraries are staged under `jniLibs/arm64-v8a`; the writable sysroot asset excludes build-only archives, exports, IR, private/server caches, native objects, generated C, and duplicate shared libraries. The staged runtime payload is approximately 523 MB. Added a background first-run installer that recursively copies packaged sysroot assets into a temporary sibling directory, validates `Init.olean`, atomically renames the completed tree into `noBackupFilesDir/toolchains/lean-4.32.1-android1`, and writes an installation marker only after activation. The Compose probe now invokes installation off the UI thread before locating the toolchain.
- Kotlin compilation of the installer passed during the first Gradle run. That run was manually stopped after Gradle spent more than ten minutes redundantly processing the 273 MB debug-symbol-bearing Lean shared library; APK packaging had not completed. `jniLibs.keepDebugSymbols` is now enabled to bypass that redundant strip. A second build entered filtered toolchain staging and was manually stopped at the user's logout checkpoint; no code/build error was reported. ADB host access is approved and its daemon starts, but no physical device was connected at the checkpoint.
- On resume: rerun `GRADLE_USER_HOME=/mnt/d/code/Lean4Android/.gradle-user-home ./gradlew testDebugUnitTest :app:assembleDebug --stacktrace`; verify the APK's native entries and asset exclusions/size, then connect the arm64 device, install the APK, run first-start sysroot installation, and execute the version, valid/invalid file, LSP, Lake, cancellation, cold-restart, Unicode-path, and performance probes.
- Pause checkpoint on 2026-08-12: the combined unit-test/APK build compiled successfully and advanced through toolchain staging, resources, Kotlin, dexing, native-library merge, and asset compression. Staging the large payload on `/mnt/d` took approximately 10.5 minutes; Gradle then spent several more minutes packing the compressed-assets output into its local build cache. The run was interrupted after compression at 97% to avoid that redundant cache copy, so no toolchain-bearing APK was produced. Subsequent continuation attempts were blocked before task execution because the restricted runner denied the loopback UDP/TCP sockets Gradle uses for file-lock coordination and its daemon. Temporary Gradle distribution-cache experiments were restored byte-for-byte from backups. ADB was blocked by the same socket restriction, so the connected Samsung tablet was not queried or modified. No Gradle, Java, ADB, compiler, or toolchain build process remains running. The only `app-debug.apk` is the stale 28 MB scaffold artifact from 2026-08-11. Resume with normal local-socket access, use the project SDK's ADB rather than `/usr/bin/adb`, and run Gradle with `--no-build-cache` before device validation.
- With local sockets restored, the host unit tests passed and clean APK assembly completed. The first continuation exposed partial `compressDebugAssets` jars retained from the interrupted build; removing only that derived task output fixed AGP's duplicate-entry failures. `:app:assembleDebug` then completed successfully with `--no-build-cache`. The resulting APK is 250,017,268 bytes (SHA-256 `3e16c09de4d727ad91d283e5c1d5991e12275ac96842a3421243a471d39d40b2`), contains 7,571 toolchain assets, the required `Init.olean`, all Lean/Lake arm64 native libraries, and no excluded build-only asset types.
- Device diagnosis: WSL sees Samsung USB device `04e8:6860` and its correct ADB interface (`ff/42/01`), but `/dev/bus/usb/001/002` is `root:root` mode `0600`; the unprivileged ADB server therefore cannot enumerate it. Windows ADB also sees no device because usbip has attached it exclusively to WSL. Apply a WSL udev rule (or temporarily grant the current node to `plugdev`) and restart ADB before device installation.
- Applied the temporary WSL USB-node fix with `sudo -A`: `/dev/bus/usb/001/002` is now `root:plugdev` mode `0660`. After restarting the project SDK's ADB with its libusb backend and approving the device prompt, serial `R52R40K1PPN` connected successfully as a Samsung SM-T870 running Android API 33.
- Installed the audited 250 MB debug APK successfully for Android owner user 0. The Compose first-run installer copied and activated the writable sysroot, and its health check reports `Ready: lean-4.32.1-android1`. Direct inspection confirms the marker and `Init.olean`; the installed sysroot occupies approximately 472,554 KiB. The packaged Lean/Lake files are executable from Android's read-only native library directory.
- The first real `lean --version` execution reached Lean initialization but aborted with exit 134. Android/Bionic reported `Pointer tag ... was truncated`; the tombstone places the invalid `free()` in `lean_dec_ref_cold`, called during `lean_st_ref_set`/option registration. The same failure occurs both under ADB `run-as` and through the app's `ProcessBuilder`, proving executable placement, dynamic-library discovery, and process launch work while Lean's allocator interaction does not.
- Tested `android:allowNativeHeapPointerTagging="false"` and added an on-device `lean --version` probe to the feasibility screen. The manifest setting does not survive `execve()` into the standalone packaged executable on this Android 13 device, so it is not sufficient. The pinned NDK exposes Bionic's `mallopt(M_BIONIC_SET_HEAP_TAGGING_LEVEL, M_HEAP_TAGGING_LEVEL_NONE)`, and Lean's shared `lean_initialize_runtime_module()` is the narrow candidate for an Android-only compatibility call before runtime subsystem initialization.
- Resolving the pointer-tagging defect requires a small checked-in Lean source patch, recompilation of the affected runtime object/archive, and relinking the Lean/Lake shared libraries and executable wrappers. Generated Lean C, Lean artifacts, and existing module objects should remain reusable. After rebuilding, reassemble/audit the distribution and APK, reinstall it, and repeat version, valid/invalid file, LSP, Lake, cancellation, restart, Unicode-path, and performance probes.
- Added an Android-only Bionic `mallopt` call at the start of Lean runtime initialization to disable heap pointer tagging before Lean can strip allocator tag bits. The guarded incremental build rebuilt only the two runtime objects/archives and relinked the Lean/Lake shared libraries and executables; generated Lean modules were reused. Distribution assembly passed its complete AArch64 ELF, dynamic-dependency, and manifest/hash audit. The rebuilt APK passed unit tests and assembly, installed successfully, and its production-path `lean --version` probe now exits 0 with Lean 4.32.1 for `aarch64-linux-android`.
- Device import probes showed that Lean 4.32's split module artifacts are runtime inputs, not build-only caches: ordinary checking required `.olean.server`, `.olean.private`, and `.ir`. Restored all three classes to APK staging while continuing to exclude static archives, exports, objects, generated C, dependency files, and duplicate shared libraries. Their uncompressed additions are approximately 31.2 MB, 1.33 GB, and 365.5 MB respectively; the resulting installed sysroot is approximately 2,175,501 KiB.
- With the complete module artifacts temporarily installed on the device, `Valid.lean` exits 0 and prints the expected theorem/evaluation output in about 1.0 seconds; `Invalid.lean` exits 1 with the expected `rfl` diagnostic in about 0.9 seconds. APK assembly, installation, sysroot activation, native process launch, version reporting, and valid/invalid proof checking have passed. The corrected larger asset set still needs a final APK rebuild/reinstall; LSP, Lake, cancellation, restart, Unicode-path, and performance probes remain.
- Lake itself reports version 5.0.0 and can load a local project's configuration after the installer layout is adapted with writable sysroot symlinks pointing to the APK-installed Lean/Lake executables. `lake check-build` passes, but spawned Lean initially derived `/data/app/.../lib/lib/lean` from the executable's canonical APK path. Added a canonical Lean patch making internal `getBuildDir` honor the already-documented `LEAN_SYSROOT` environment variable before falling back to `IO.appDir`. The guarded incremental rebuild is regenerating `Lean.Util.Path` and its dependent modules; no build failure has occurred.

### Android/Lean integration findings and architectural consequences

#### Android application and process model

- Android 10/API 29 and later do not permit an app to execute newly written code from its writable private directories. Lean and Lake therefore cannot be unpacked into `filesDir` or the writable toolchain sysroot and run from there. Executable ELF files are packaged as `.so`-named arm64 native libraries and are installed by Android into the app's read-only `nativeLibraryDir`; projects and toolchain data remain under app-private writable storage.
- Android assigns a randomized, version-specific APK installation path. That path changes after reinstall and contains shell-sensitive characters, so it must never be persisted. The app resolves `ApplicationInfo.nativeLibraryDir` at runtime and launches with `ProcessBuilder` argument arrays, never a shell command. ADB shell quoting failures encountered during diagnosis reinforce this production rule but do not affect the Kotlin runner.
- Writable symlinks may point to APK-installed executable inodes and Android permits execution through them. This provides Lake with conventional-looking `<sysroot>/bin/lean` and `<lake-home>/.lake/build/bin/lake` paths without copying executable code into writable storage. These symlinks must be recreated after every APK update because the native-library target path is randomized.
- Native child processes need a deliberately small environment containing at least `HOME`, `LEAN_SYSROOT`, `LEAN_PATH`, `PATH`, and `LD_LIBRARY_PATH`; Lake additionally needs `LAKE_HOME` and `LAKE_OVERRIDE_LEAN=true`. App-private projects provide normal filesystem semantics and stable absolute paths. The Storage Access Framework remains an import/export boundary rather than a live Lake workspace.
- Android's Bionic allocator uses top-byte heap pointer tags on arm64. Lean stores its own metadata in pointer bits and later passed a pointer with Bionic's tag removed to `free`, causing a deliberate Bionic abort. The application manifest's `allowNativeHeapPointerTagging=false` affects the app process but did not carry across `execve()` to standalone Lean. Lean must disable Bionic heap tagging itself before runtime allocation begins.
- APK-installed native libraries are dynamically linked through Android's loader. All required Lean/Lake shared libraries must be packaged together, their dependencies must be limited to those packaged libraries plus the audited Android system-library allowlist, and libc++ must be linked statically. The toolchain audit enforces AArch64 architecture and rejects accidental host objects, glibc dependencies, and unbundled `libc++_shared.so`.

#### Lean and Lake runtime layout/dependency behavior

- `LEAN_SYSROOT` was not sufficient for the original direct Lean startup: internal `Lean.getBuildDir` derived the sysroot from `IO.appDir`. Direct ADB invocation through `/system/bin/linker64` consequently derived `/apex/.../lib/lean`; execution through an APK-path symlink derived `/data/app/.../lib/lib/lean`. `LEAN_PATH` can make direct one-shot checking work, but Lake-spawned Lean intentionally constructs its own project search path and relies on Lean's built-in sysroot path for the standard library.
- Lake already treats `LEAN_SYSROOT` as an installation override, but its executable self-location logic expects a conventional desktop layout. When invoked through Android's linker it cannot infer its installation; `LAKE_HOME` plus sysroot symlinks supplies that layout. Because Lake can otherwise mis-detect Lean as co-located with the linker/native directory, `LAKE_OVERRIDE_LEAN=true` is required to force its documented environment-based Lean installation.
- Lean 4.32 uses split module artifacts. Ordinary checking of the bundled standard library was empirically shown to require `.olean`, `.olean.server`, `.olean.private`, and `.ir`, not only `.olean`/`.ilean`. Missing facets fail successively during import: first server data, then private data, then IR for interpretation. For this distribution there are 2,431 files of each split class; server data is about 31.2 MB, private data about 1.33 GB, and IR about 365.5 MB uncompressed. With all required facets installed, the writable sysroot is about 2,175,501 KiB.
- `.ilean` remains useful for source/reference tooling. Sources are retained for diagnostics, navigation, and server behavior. Static archives, `.export` files, generated C, native objects, dependency files, and duplicate shared libraries remain excluded from the APK runtime payload unless a later supported workflow proves one is required. Native compilation/linking of Lean executables is outside the MVP.
- `lean --version`, direct valid-file checking, and invalid-file diagnostics now pass on Android. The valid fixture exits 0 and evaluates code; the invalid fixture exits 1 with the expected source-positioned tactic error. This proves process launch, shared-library loading, sysroot imports, split-artifact loading, interpretation, stdout/stderr capture, and exit-code handling.
- Lake 5.0.0 starts and parses a local project, and `lake check-build` passes with the layout adapter. Full `lake lean`/`lake build` validation awaits completion and deployment of the `LEAN_SYSROOT` path patch because their spawned Lean process must find the bundled standard library without inheriting an externally forced full `LEAN_PATH`.

#### Upstream/toolchain changes required

- Lean's CMake platform tests were widened from Linux to `Linux|Android` where Android shares ELF/PIC/export/dynamic-loader behavior, while upstream tests are disabled for the cross target.
- Generated `leanc` and `stdlib.make` use the configured `LEANC_CC`, ensuring generated Lean C is compiled by the NDK API-29 AArch64 driver rather than generic host Clang.
- Android linking uses the NDK's static-libc++ linker-script sequence so libc++ and libc++abi are included without a runtime `libc++_shared.so` dependency.
- `lean_initialize_runtime_module()` now makes an Android-only `mallopt(M_BIONIC_SET_HEAP_TAGGING_LEVEL, M_HEAP_TAGGING_LEVEL_NONE)` call before initializing Lean's allocator/runtime subsystems. This resolved the tagged-pointer abort for both Lean and Lake.
- `Lean.getBuildDir` now gives the documented `LEAN_SYSROOT` environment variable precedence over executable-relative discovery. This makes an APK-installed executable compatible with a separately installed writable sysroot and is also consistent with Lean/Lake's public external path-discovery behavior.
- All Lean modifications live in one checked-in canonical patch. The build guard requires the source checkout's exact `git diff` to match it byte-for-byte, preventing accidental local source drift. Cross-build outputs remain generated and untracked.

#### Choices and forward architecture

- Continue with child processes rather than JNI: Android successfully launches the packaged executable, captures its streams, reports exit status, and runs real Lean workloads after the narrow compatibility fixes. There is currently no evidence requiring the more invasive embedded-JNI fallback.
- Keep executable code immutable in the APK and data versioned in `noBackupFilesDir`. The installer stages data into a temporary sibling, validates it, atomically activates it, and marks completion last. Future installer work must also create/refresh the conventional Lean/Lake symlink layout after activation or APK replacement.
- Treat the toolchain environment as a first-class typed component shared by one-shot commands and the future LSP supervisor. Hard-coding paths independently in UI/process code would recreate the failures found during probing.
- Retain the full split module data for correctness for now, despite size. A future size optimization requires changing how Lean produces/loads its standard-library artifacts or producing a deliberately flattened/non-module-system runtime distribution and proving LSP/navigation/interpretation behavior. Silently omitting facets is not viable.
- Keep `arm64-v8a`, API 29+, one pinned Lean revision, and offline core/Std as the initial support boundary. The current physical validation device is API 33; API 29 and a current Android release still need matrix coverage.
- The feasibility UI now executes `lean --version` through the same shell-free Kotlin process runner intended for production. The next architecture layers remain a durable toolchain-layout/environment builder, long-lived LSP supervision, project serialization, and cancellation/process-tree cleanup.

#### Counterfactual optimization opportunities

- If Android allowed execution from app-private writable storage, Lean could use a conventional colocated distribution, avoid `.so` executable naming and symlink adapters, and update toolchains independently of APK native code. Android's W^X policy intentionally rules this out for the supported target SDK.
- If Lean consistently honored `LEAN_SYSROOT` internally, no upstream path patch would be needed and Lake-spawned commands would work with environment configuration alone.
- If Lake accepted explicit executable paths independently of desktop installation-layout inference, the writable compatibility symlinks and `LAKE_OVERRIDE_LEAN` would be unnecessary.
- If Lean's runtime preserved allocator top-byte tags, Android heap tagging could remain enabled for stronger memory diagnostics. Disabling it is a scoped compatibility compromise in Lean child processes.
- If Lean standard-library `.olean` files were self-contained for ordinary checking, or if optional split facets were lazily tolerated, the APK/sysroot could remain near the earlier roughly 523 MB staged payload. Lean 4.32's required private/IR facets make the correct runtime more than 2 GB uncompressed and may materially affect APK size, first-install time, store delivery, and future Mathlib strategy.
- If Lake/library builds did not depend on conventional toolchain paths and external compiler/archive tools, more build workflows could be enabled immediately. The MVP deliberately supports proof checking and Lean library builds first; native targets remain separate.

#### Sandbox, WSL, and host-environment constraints

- Earlier restricted runner sessions denied the loopback UDP/TCP sockets used by Gradle's daemon/file-lock coordination and by ADB. This prevented task startup and device enumeration even though source/build state was valid. With normal socket access restored, both tools worked.
- Gradle's build cache redundantly copied very large compressed-asset outputs and made an interrupted build appear stuck at 97%. `--no-build-cache` avoids that copy. Interrupting `compressDebugAssets` left partial per-asset jars that AGP did not clean on retry, producing thousands of duplicate-entry errors; deleting only that derived task output repaired the build.
- `/mnt/d` filesystem performance dominates large sync/copy/package operations: filtered staging takes about ten minutes, distribution copying/auditing can take longer, and APK packaging is quiet for long periods. These are host-I/O costs rather than compiler failures and should not be interrupted without evidence of failure.
- The Samsung USB device was attached to WSL through usbip and exposed the correct ADB interface, but its `/dev/bus/usb` node was `root:root` mode `0600`. Applying `sudo -A chgrp plugdev` and `chmod 0660` enabled the Linux ADB server; the permission must be reapplied after reconnect unless a persistent udev rule is installed. Windows ADB cannot see a device while usbip has attached it exclusively to WSL.
- Samsung Secure Folder user 150 caused package-manager commands without `--user` to fail even though owner user 0 was active. All installation/package operations now explicitly target Android user 0.
- ADB's remote shell re-parses strings and mishandled randomized APK paths in diagnostic commands. Production code does not use ADB or shell composition; it uses absolute `File` paths and `ProcessBuilder` argument lists. Diagnostic tests use Android's linker or carefully quoted commands only where necessary.

## 2026-08-12 — Post-restart recovery and sysroot-path rebuild

### Recovery status

- The host restarted while the guarded incremental Android cross-build was regenerating Lean modules affected by the `LEAN_SYSROOT` path-discovery change. No compiler, Gradle, or ADB process survived the restart, and the Git worktree recovered cleanly at commit `70e6e10`.
- The Android build tree remains reusable: the patched Lean and Lake launchers and `libleanshared.so` were relinked before shutdown. Temporary outputs under `lib/temp` show that downstream module regeneration was interrupted, so those artifacts are not yet considered a complete distribution.
- The next task is `toolchain/scripts/build-android.sh` with guarded source-patch validation and incremental CMake/Make reuse. After it succeeds, the distribution will be reassembled and audited before rebuilding/deploying the APK and proving `lake lean`/`lake build` on-device.
- Device access is not a prerequisite for this recovery stage. If the restricted WSL session prevents ADB socket creation or USB-node access, that limitation will be recorded and host-side build, packaging, and tests will continue.
- Recovery inspection in the new sandbox confirmed that no Lean, CMake/Make, Gradle, Java, or ADB worker survived. The reusable Android build and prior audited distribution are still present, with ample host disk space. The interrupted generator left a nonempty `Lean/Elab/AutoBound.c.tmp` and an older empty export temp; both are preserved for the build system's atomic-output recovery rather than treated as completed artifacts.
- The guarded serial resume passed the exact source-patch check and CMake configure/generate, then reused the AArch64 runtime/core targets through `leancpp` (CMake's coarse 46% mark). It regenerated the `Lean.Util.Path` dependent chain cleanly through `Lean/Elab/AssertExists` without a missing, truncated, or malformed atomic output.
- The serial build remains active outside the managed sandbox's PID namespace. Process inspection inside the sandbox cannot see its wrapper/compiler workers and initially produced a false stopped-process conclusion, but build artifacts continued advancing every 15–20 seconds during a two-sample check: from `Lean/Elab/PreDefinition/Structural/RecArgInfo` through `Lean/Meta/IndPredBelow` between 23:37 and 23:39. Do not launch a second build or alter the build tree while those timestamps continue advancing.
- At the 23:39 checkpoint, 343 primary `.olean` modules, with corresponding split facets and generated C, had timestamps from the latest post-22:00 run out of 2,433 compiled modules in the tree. Because only modules downstream of `Lean.Util.Path` need rebuilding, this is not a direct whole-tree completion percentage; roughly 1,800–2,100 affected modules plus generated-C compilation and final relinking may remain. At `-j1`, expect several more hours of cross-build work.
- The nonempty `Lean/Elab/AutoBound.c.tmp` predates this latest resume while its completed `AutoBound.c` remains present; an older empty export temp also remains. The successful advance beyond that area confirms neither temp blocked the resumed dependency chain. Preserve them and allow the build rules to decide atomic-output replacement on the next resume.
- A checkpoint commit initially failed because that managed sandbox mounted `.git` read-only, but the user subsequently committed the recovery history as `2f549be`. The current history correction is the only new worktree change at this pause.
- Next-session sequence: first determine activity by sampling newest artifact timestamps twice, because sandbox-local `ps` is not authoritative across PID namespaces. If timestamps still advance, monitor the existing build and do not start another. If they are stable and no host-visible worker exists, resume with `LEAN4ANDROID_JOBS=1 toolchain/scripts/build-android.sh`. After complete success, run `toolchain/scripts/assemble-distribution.sh` and its ELF/hash audit; rebuild the APK with unit tests and `--no-build-cache`; then reinstall for Android user 0 and complete Lake, LSP, cancellation, restart, Unicode-path, offline, and performance probes.
- Resume on 2026-08-13: artifact timestamps had stopped at 00:00:11 after completing nonempty `Lean/Elab/ComputedFields` outputs, and no build worker was visible. A single guarded `LEAN4ANDROID_JOBS=1 toolchain/scripts/build-android.sh` resume passed source-integrity validation and CMake configure/generate, reused the AArch64 runtime/core targets through `leancpp` (46%), and continued module regeneration at `Lean/Elab/Inductive` without an atomic-output or compiler error. The serial build is active; do not start another build while it remains so.
- By 09:59 on 2026-08-13, the same serial run had completed the affected Lean source/artifact regeneration through the top-level `Lean`, `LeanIR`, server, linter, elaborator, and tactic aggregates. Since this resume it regenerated 373 primary `.olean` files with their generated C and split facets, then transitioned into NDK compilation of those changed C modules. The compiler had produced 92 newly timestamped AArch64 objects and was advancing through `Lean/Elab` (`Match.o` completed at 09:59:10). The build session remains alive with no reported error. Remaining work is the rest of changed-C compilation, archive/shared-library and executable relinking, followed by distribution assembly/audit, Gradle tests/APK assembly, and device conformance probes.
- Crash recovery inspection at 10:51 on 2026-08-13 found that the serial run had continued well beyond the prior checkpoint before the computer stopped. It completed the changed top-level Lean/LeanIR objects and advanced through Lake regeneration and AArch64 compilation; the newest completed outputs are `Lake/CLI/BuiltinLint.o` and its export object at 10:42:13. No build worker survived, no Lake `.tmp`/partial output is present, the newest object is a valid ELF64 AArch64 relocatable, and the build volume has ample free space. The existing build tree is therefore reusable without deleting or rebuilding completed work; resume once at `LEAN4ANDROID_JOBS=1` from the normal guarded script.
- The guarded `LEAN4ANDROID_JOBS=1 toolchain/scripts/build-android.sh` resume completed successfully at 100%. It reused the native runtime/core targets through `leancpp`, resumed precisely in the unfinished Lake dependency chain, regenerated and compiled the remaining Lake/LeanChecker outputs, completed `make_stdlib`, and relinked `libleanshared.so`, `libLake_shared.so`, `lean`, `lake`, and `leanchecker` without error. No prior completed artifacts had to be discarded or rebuilt wholesale. The next checkpoint is distribution reassembly plus the full architecture/dependency/hash audit.
- `toolchain/scripts/assemble-distribution.sh` completed successfully after rebuilding the full approximately 3.1 GB output tree. Its independent audit accepted the rebuilt distribution, including AArch64 ELF identity, packaged/system dynamic-dependency constraints, and the regenerated per-file manifest hashes. The recovered toolchain is now complete and auditable; host tests and the toolchain-bearing APK rebuild are next.
- `GRADLE_USER_HOME=/mnt/d/code/Lean4Android/.gradle-user-home ./gradlew --no-build-cache --no-daemon --console=plain testDebugUnitTest :app:assembleDebug` completed successfully in 53m58s. Toolchain staging reached 14,864 assets and final compression/packaging completed without the prior interrupted-output or build-cache problem. The new debug APK is 803,678,420 bytes with SHA-256 `48422d274fbccd88623266397f46fa857466462679e7641774ecb9f42f40e355`; ZIP inspection confirms all eight expected arm64 native entries, `Init.olean`, 2,431 each of `.olean.private`, `.olean.server`, and `.ir`, and no staged `.a`, `.o`, generated `.c`, `.depend`, or `.export` build artifacts. Host recovery, cross-build, distribution audit, unit tests, and APK reconstruction are complete; device deployment and the remaining Lake/LSP/runtime probes are next.
- The project SDK's ADB daemon starts successfully when given local-socket access, but `adb devices -l` currently reports no attached device. No install or device state change was attempted. Continuation is blocked only on reconnecting/reattaching and authorizing the arm64 test device; once visible, install the new APK explicitly for Android user 0, allow the versioned sysroot to reinstall, and run the outstanding Lake build, LSP, cancellation, cold-restart, Unicode-path, offline, and performance probes.
- After the tablet was reattached, WSL enumerated Samsung USB device `04e8:6860` but its node had reverted to `root:root` mode `0600`. Reapplying the temporary `plugdev`/`0660` fix and restarting the project SDK's ADB with libusb restored authorized serial `R52R40K1PPN` (SM-T870, Android API 33, current user 0). The rebuilt 803.7 MB APK then installed successfully with `adb install --user 0 -r -t`. Because the toolchain revision ID is intentionally unchanged, the existing `.installed` marker would make the first-run installer reuse stale pre-rebuild sysroot data; clear only this test app's private data before launch to force a fresh packaged-sysroot activation.
- Cleared only `org.lean4android.app` user-0 private data, launched the rebuilt app, and triggered its first-run installer. The installer copied and atomically activated the complete packaged sysroot at 2,175,513 KiB and wrote `.installed`. The app's production shell-free `ProcessBuilder` probe then reported `Ready: lean-4.32.1-android1`; `lean --version` exited 0 and identified Lean 4.32.1, `aarch64-linux-android`, commit `f054605aea4b840552cca2e725580bffd1e1b704`. Fresh APK installation, sysroot activation, executable discovery, dynamic loading, patched sysroot discovery, and basic process execution therefore pass on the API-33 tablet.
- Fresh direct conformance checks pass against the APK-installed sysroot: `Valid.lean` exits 0 and prints the theorem plus `"Hello from Lean on Android"`; `Invalid.lean` exits 1 with the expected source-positioned `rfl` diagnostic. A copy under `files/conformance/Unicode Ω/证明.lean` also exits 0, validating spaces and non-ASCII app-private paths. Host-observed runs were approximately 2.6 seconds for the first valid check and 0.9 seconds for the invalid check.
- Recreated the conventional writable layout manually for validation: `<sysroot>/bin/lean`, `<sysroot>/.lake/build/bin/lake`, and a Lake build-lib link target the immutable APK executables and installed Lean library. Lake 5.0.0 starts, `check-build` exits 0, and `lake build` successfully invokes the rebuilt Lean, produces the expected `.olean`/`.ilean`/IR outputs, and completes three jobs in about 1.4 seconds. `lake lean` initially exposed Android's unwritable default temporary directory; adding `TMPDIR=<app cache>` makes it exit 0 with the expected output. The production environment builder must include `TMPDIR`, and the installer must create/refresh these symlinks after activation/APK replacement.
- A byte-exact non-PTY LSP initialize/initialized/shutdown/exit exchange succeeded. Lean Server 0.3.0 returned its full capability object, proving stdio framing and server startup; PTY transport is unsuitable because newline translation corrupts `Content-Length`. Server startup emits a non-fatal watchdog warning because Android lacks `/etc/localtime`; initialization still succeeds. A synchronized cold-app restart reused the existing installed sysroot and completed the production version probe in approximately 2.48 seconds without reinstalling data.
- Device-side process termination also passes at the OS boundary: started a real `lean --server` child under the app UID, confirmed its `lean` PID and wrapper relationship, sent SIGTERM, observed both processes disappear, and saw the supervising ADB stream close with `Terminated`. Final filtered logcat inspection shows no app fatal exception, pointer-tagging abort, or native backtrace from these runs. The activated marker remains present and the final sysroot occupies 2,175,523 KiB; the small increase is the manually created compatibility links.
- Synchronized the living documentation after recovery and device conformance. `README.md` now describes the working Lean/Lake feasibility artifact instead of an unfinished cross-build; `MVP_AND_REBUILDING.md` records the final APK/sysroot measurements, passing direct/Lake/LSP/cold-restart/termination checks, and the app-writable `TMPDIR` requirement; `IMPLEMENTATION_PLAN.md` advances the M1 status and immediate actions to production link/environment hardening, diagnostics/offline/migration/resource testing, API coverage, and delivery design. `initial_plan.md` remains the historical concept document and `toolchain/README.md` remains accurate without a status edit.

## 2026-08-13 — Minimal visual Lean editor probe

### Implemented

- Replaced the probe-only Compose screen with a deliberately minimal single-file editor for `Main.lean`. It starts with a valid theorem and `#check`/`#eval` example, allows multiline editing, prevents overlapping checks, shows progress, and renders selectable exit status, elapsed time, stdout, stderr, timeout, and launch failures.
- Each Check atomically saves UTF-8 source to `filesDir/projects/visual-probe/Main.lean`, installs/locates the packaged toolchain on demand, and invokes the APK-installed Lean executable through the existing shell-free bounded runner. The direct command uses the device-validated `HOME`, app-writable `TMPDIR`, `LEAN_SYSROOT`, `LD_LIBRARY_PATH`, and minimal system `PATH` environment.
- Kept this M1 visual probe explicitly separate from the M3 editor decision and future project/LSP layers. It intentionally has one fixed file, plain Compose text editing, explicit checking, and no syntax highlighting, file tree, live diagnostics, or Lake dependency model.

### Validation

- `:app:compileDebugKotlin` passes after resolving a Compose `weight` import mismatch found by the first compile.
- Added app unit coverage for atomic source replacement, stdout/stderr/status formatting, and timeout/no-output presentation. `:app:testDebugUnitTest` passes all three tests.
- The full `testDebugUnitTest :app:assembleDebug` run passes. Because the audited toolchain payload was unchanged, Gradle reused staging/compression and rebuilt the application code, dex, and package in 3m43s. The resulting editor APK is 803,729,953 bytes with SHA-256 `e9caea1b87204e109ae2c43ffde2c02eae1eb7210b3512ad31f3b78c9bc4839b`.
- Installed the editor APK for Android user 0 while preserving the validated sysroot. The tablet UI hierarchy confirms the multiline Lean source field, `Check Lean` action, and output panel render. Running the default source atomically created `files/projects/visual-probe/Main.lean`; the visible result reports exit 0 in 3,112 ms and displays `one_plus_one : 1 + 1 = 2` plus `"Hello from Lean on Android"`.
- The user completed the hands-on visual test on the physical Android device and confirmed the editor can still edit source, communicate with Lean, and run Lean programs. This working visual loop is now a standing regression gate: subsequent runtime, project, LSP, and delivery changes may evolve the editor according to the roadmap, but must retain an operable visual source editor and an end-to-end Lean execution path.

## 2026-08-13 — Runtime layout and command hardening

### Implemented

- Moved direct Lean and Lake command construction into a typed `ToolchainCommandFactory`. It owns absolute executable selection, the minimal cleared environment, app-writable `TMPDIR`, timeouts, and the deliberate difference between direct Lean's `LEAN_PATH` and Lake's project-aware child environment.
- Changed the visual editor to use the shared Lean command factory without changing its source editing, atomic save, Check action, progress, or result UI.
- Added an installer-owned `ToolchainLayoutAdapter` that atomically creates and refreshes `<sysroot>/bin/lean`, `<sysroot>/.lake/build/bin/lake`, and `<sysroot>/.lake/build/lib/lean`. Refresh runs even when sysroot data is already installed, repairing links after Android assigns a new native-library path during APK replacement.
- Extended toolchain health reporting to reject absent or stale compatibility links.

### Validation

- Added host unit tests proving all compatibility links are created, a stale Lean executable target is replaced, direct Lean receives the complete Android-safe environment, and Lake receives its writable compatibility layout without a globally forced `LEAN_PATH`.
- `testDebugUnitTest :app:compileDebugKotlin` passes after the refactor. The initial sandboxed attempt was blocked before compilation by Gradle's local-socket discovery; the identical approved run completed successfully in 4m22s.
- The full `testDebugUnitTest :app:assembleDebug` regression run passes in 4m24s and produces an 803,729,541-byte APK with SHA-256 `1e8a51c14f878bd9dda02c36edc54cc315260cbb2b5c8724b83577e9d70f64d1`.
- Restored ADB access to the connected SM-T870/API-33 tablet after its USB node reverted to `root:root` mode `0600`: changed only `/dev/bus/usb/001/002` to `root:plugdev` mode `0660`, restarted the project SDK's ADB with libusb, and confirmed the authorized device.
- Update-installed the hardening APK with `adb install -r`, preserving the existing approximately 2.18 GB sysroot and project data. The visual editor still renders an enabled multiline source field, Check action, and result panel. Its production Check path atomically saved `Main.lean`, exited 0 in 3,115 ms, and visibly returned the expected theorem type plus `"Hello from Lean on Android"` evaluation.
- The same Check refreshed all three compatibility links without reinstalling sysroot data. `bin/lean` and `.lake/build/bin/lake` point to the current randomized `/data/app/.../lib/arm64/` executables, while `.lake/build/lib/lean` points to the preserved writable sysroot. Recent filtered logcat contains no app fatal exception, native fatal signal, or Bionic pointer-tagging abort.

## 2026-08-13 — LSP framing and lifecycle foundation

### Implemented

- Added the `core-lsp` Android library as an isolated protocol layer. The visual editor and its proven direct Check path remain unchanged; the app will depend on LSP only after long-lived process ownership is implemented.
- Added byte-accurate stdio JSON-RPC framing with UTF-8 `Content-Length`, case-insensitive header parsing, bounded headers, exact payload reads, clean EOF handling, synchronized writes, and explicit errors for missing, duplicate, invalid, or truncated frames.
- Added minimal typed generation for the pinned Lean server lifecycle sequence: `initialize`, `initialized`, `shutdown`, and `exit`, including safe JSON string escaping for workspace URIs.

### Validation

- `:core-lsp:testDebugUnitTest` passes. Tests exercise multibyte Unicode byte counts, two-byte fragmented reads, coalesced messages, EOF, malformed/duplicate/invalid lengths, truncated payloads, lifecycle ordering through the real framing boundary, and URI escaping.
- This checkpoint proves protocol encoding/framing in isolation. The earlier physical-device raw handshake remains the evidence that the packaged Lean server starts; supervised real-process lifecycle, `didOpen`, diagnostics, version filtering, and orphan-process checks remain the next slice.

## 2026-08-13 — Real Lean diagnostics and process lifecycle

### Implemented

- Added a shell-free interactive `JvmProcessLauncher`/`RunningProcess` boundary with independently owned stdin/stdout/stderr, bounded exit waiting, graceful termination, and forced direct-child fallback. Added a dedicated `ToolchainCommandFactory.lakeServer()` command using workspace-aware `lake serve`.
- Added Lean `didOpen`, full-text `didChange`, and `didClose` message generation plus a synchronized document-version gate that rejects stale, closed, and unknown-document diagnostics.
- Added `toolchain/conformance/run-device-lsp.py`, a repeatable non-PTY ADB conformance client. It creates a private local Lake project, performs initialize and dynamic-registration exchange, opens invalid Lean source, waits for versioned nonempty diagnostics, and completes shutdown/exit. It also has a forced-stop mode and checks for remaining Lean/Lake process names.
- Added an installer-owned 114-byte UTC TZif fallback and explicit `TZ=:/.../UTC` in the centralized toolchain environment.

### Findings and validation

- The previously reported `/etc/localtime` watchdog message was not harmless for a usable LSP session. Initialization returns capabilities, but after `initialized` Lean starts its client task, fails to resolve Android's absent `/etc/localtime`, and exits 1 before document diagnostics. Android's tzdata APEX exposes a packed database rather than individual TZif files, so it is not a compatible direct replacement.
- With the app-owned UTC TZif, the intended `lake serve` path returns Lean Server 0.3.0 capabilities, requests file-watcher registration, accepts a version-1 `didOpen`, publishes the expected `rfl` error for `1 + 1 = 3`, acknowledges shutdown, and exits 0. A separate forced-transport-stop run after initialization reports zero remaining `lean`, `lake`, `liblean_exe.so`, or `liblake_exe.so` processes.
- Added unit coverage for interactive stream separation, environment clearing, bounded/forced direct-child termination, `lake serve` command construction, pinned Lean's `initialized: null`, document message escaping, monotonic versions, and stale/closed diagnostic rejection. All affected model/process/toolchain/LSP unit suites pass.
- The full `testDebugUnitTest :app:assembleDebug` regression passes in 3m42s. After deleting only the manually staged UTC diagnostic fixture, update-installing the new APK, and running Check, production `ToolchainLayoutAdapter` recreated a 114-byte `TZif` fallback. The visual editor still exited 0 with the expected `#check`/`#eval` output in 2,911 ms.
- Repeated both automated device modes using the production-created fallback: workspace-aware `lake serve` produced the expected version-1 diagnostic and exited 0 through shutdown/exit; forced transport termination reported no remaining Lean/Lake processes.

## 2026-08-13 — Installer schema and representative integrity hardening

### Implemented

- Replaced the presence-only `.installed` marker contract with schema version 1 plus the immutable toolchain ID. Existing ID-only markers migrate atomically in place only when representative runtime facets are complete, avoiding an unnecessary 2.19 GB reinstall on upgrade.
- Installation health now requires nonempty `Init.olean`, `.olean.private`, `.olean.server`, `.ilean`, and `.ir` files as well as a matching marker, native executables, compatibility links, Lean library, and UTC fallback.
- The installer always removes stale `.installing` state, can restore a healthy `.previous` tree left by interrupted activation, validates staged facets before replacing the active tree, and preserves the former active tree until the staged rename succeeds.
- Added a free-space preflight using the measured 2,194,903,155-byte filtered sysroot plus a 64 MiB reserve. The size constant is documented beside its Gradle build field and must be updated whenever APK filtering changes.

### Validation

- Added unit tests for healthy schema markers, safe legacy migration, wrong schema/ID reporting, empty/missing split facets, and the exact free-space threshold. `:core-toolchain:testDebugUnitTest :app:compileDebugKotlin` passes.
- Complete per-file manifest/hash validation, injected rename interruption, actual low-storage behavior, and corrupted-device repair remain explicitly open; representative facet validation is not being treated as complete corruption coverage.
- The full `testDebugUnitTest :app:assembleDebug` regression passes in 4m38s. Update-installed it over the reference tablet's legacy ID-only marker and invoked migration through the visual Check path. The marker changed in place to `schema=1` plus the matching toolchain ID, the existing 2,175,531 KiB sysroot was retained, no `.installing` or `.previous` tree remained, and Lean exited 0 with expected output in 3,212 ms.

## 2026-08-13 — Complete filtered-runtime manifest foundation

### Implemented

- Added a build-time filtered-manifest generator that maps every staged sysroot file back to the audited distribution manifest. It fails on unaudited paths or size drift and emits a compact tab-separated manifest containing relative path, byte size, and the audited SHA-256.
- Added a strict Kotlin runtime-manifest parser and streaming verifier. Paths cannot be absolute or traverse upward; duplicate paths, malformed records/sizes/hashes, missing files, wrong sizes, and same-size content corruption are reported.

### Validation and boundary

- Generated a 14,864-file manifest with SHA-256 `f7e389dcee7bd8f146fcd9e7f05ca6ddc9243bd3e99e3261a5dee79e7d1aa797`. `:core-toolchain:testDebugUnitTest :app:writeFilteredToolchainManifest` passes, including missing, size-corrupt, hash-corrupt, traversal, duplicate, and malformed-hash cases.
- The first generation took approximately eight minutes because enumerating thousands of files on `/mnt/d` is slow; no 2.19 GB rehash was performed. This checkpoint deliberately does not claim installed-tree enforcement yet: binding the manifest digest and one-time full verification into schema 2 is the next step, and routine visual Check must not rehash the sysroot.
- The full unit/APK regression passes in 18m10s; changing the asset set forced recompression of the large payload. The APK contains the 1,785,411-byte manifest with 14,866 lines (two headers plus 14,864 files), is 804,540,704 bytes, and has SHA-256 `3af006cf096ec50431bda6af1db07efb1068226c245cd143541ab4e67c8a182c`. Update installation succeeded with schema-1 sysroot data preserved. The tablet entered its secure lock screen before the final visual Check, so hands-on/editor automation for this exact APK remains pending rather than being inferred; the immediately preceding schema-aware APK passed the editor on the same device.

## 2026-08-13 — Schema-2 full runtime verification

### Implemented

- Advanced the installation marker to schema 2 and bound it to the exact filtered runtime-manifest SHA-256. The installer verifies the manifest asset's own digest/schema/toolchain ID before trusting its entries.
- Fresh staging, legacy markers, and schema-1 installations now stream-verify all 14,864 runtime paths, sizes, and hashes before schema 2 is written. A corrupt legacy/schema-1 tree falls through to the existing free-space-checked staged replacement path; it is never marked verified.
- Routine schema-2 health remains intentionally fast: marker digest, representative facets, native/layout links, Lean library, and UTC fallback are checked without rehashing 2.19 GB on every visual Check.

### Device validation

- After the tablet was unlocked, closed the pending manifest-APK gate: visual Check exited 0 in 3,013 ms, workspace-aware `lake serve` returned the expected version-1 `rfl` diagnostic and exited 0, and forced transport stop left zero Lean/Lake processes.
- Update-installed schema 2 over the existing schema-1 2,175,531 KiB sysroot and invoked its one-time full verification through the editor. The marker migrated in place to schema 2 with manifest digest `f7e389dcee7bd8f146fcd9e7f05ca6ddc9243bd3e99e3261a5dee79e7d1aa797`; Lean then exited 0 with expected output. The subsequent Check skipped full hashing and completed Lean execution in 1,505 ms.
- Repeated graceful diagnostics and forced-cleanup conformance after migration; both pass. The UI currently reports only Lean execution elapsed time, so the full migration duration was not measured separately and is not inferred from the displayed result.
- Corrected the migration recovery branch so a legacy/schema-1 hash failure falls through to staged replacement instead of surfacing immediately from the verifier. The final `testDebugUnitTest :app:assembleDebug` run passes in 3m48s; its update installation preserves the schema-2 marker, and the visual editor again exits 0 with expected output in 3,013 ms. Filtered logcat contains no app crash; a previously noticed `FATAL EXCEPTION` was traced to overlapping `uiautomator dump` helper processes (`UiAutomationService ... already registered`), not `org.lean4android.app`.

## 2026-08-13 — Deterministic activation rollback coverage

### Implemented

- Isolated sysroot directory activation and rollback recovery from the Android locator into a host-testable `ToolchainActivation` boundary while preserving the production staging/destination/previous layout.
- Hardened the failed-activation branch: failure to rename the preserved tree back into place is now surfaced explicitly instead of being ignored before reporting the original activation failure.
- Added deterministic tests for a successful atomic directory swap, a staging-rename failure that restores the old sysroot, cold-start recovery that accepts only a healthy `.previous` tree, and retention of an unhealthy active tree when rollback is also unhealthy.

### Validation and remaining boundary

- `:core-toolchain:testDebugUnitTest` passes all focused toolchain tests; the final run including the explicit rollback-rename failure case completed in 1m15s. The first compile also caught and prompted correction of a missing test-only `java.io.File` import.
- The complete `testDebugUnitTest` regression passes across the app, model, process, toolchain, and LSP modules in 3m36s (107 tasks; 3 executed and 104 up-to-date).
- These tests inject rename failures without copying the 2.19 GB payload and close the host-testable state-machine gap. They do not replace the planned Android instrumented interruption, real low-storage, complete installed-tree corruption, or cold-update cases.

## 2026-08-13 — Explicit runtime audit and process-boundary ADR

### Implemented

- Added `AndroidToolchainLocator.verifyInstalledRuntime()` as an explicit slow path. It first applies schema-2 representative health checks, then reads the digest-bound packaged manifest and streams size/hash verification across every installed runtime entry.
- Added a **Verify runtime** action beside **Check Lean** in the visual probe. The audit runs off the UI thread, disables concurrent actions, and reports duration, success, or the complete mismatch list. Ordinary startup and Lean checks do not call it.
- Added ADR 0001 accepting packaged child processes, data-only writable sysroots, typed shell-free command construction, compatibility-link refresh, the Bionic heap-tagging and `LEAN_SYSROOT` patches, and the current no-shell/no-native-build capability boundary. JNI and a native launcher remain evidence-triggered fallbacks.

### Validation and boundary

- Added app unit coverage for successful and failed integrity-result presentation. `:app:testDebugUnitTest :app:compileDebugKotlin` passes in 3m48s (76 tasks; 8 executed and 68 up-to-date).
- The implementation reuses the already-tested manifest parser/verifier. A physical full audit is intentionally still pending because it will read and hash approximately 2.19 GB; its measured duration belongs in the reference-device performance record.

## 2026-08-13 — M1.6 monolithic-package baseline

### Measurements and findings

- Audited the current 804,600,480-byte debug APK (`a121310dd949b5918bdaaed3b04a7b35a7e20e77e33476cb512920bce4009acc`) with ZIP entry accounting. Its 14,864 `assets/toolchain/` files occupy 2,192,429,171 bytes uncompressed and 692,307,895 bytes compressed.
- The eight arm64 native entries occupy 286,998,976 bytes uncompressed and 79,491,830 bytes compressed. These must remain APK-installed executable code; the runtime assets are data-delivery candidates.
- `.olean.private` dominates both installed and download size: 1,330,725,152 uncompressed bytes and 443,221,498 compressed bytes. It cannot be removed by filtering because the Android conformance sequence already proved ordinary imports require private facets.
- Added `docs/delivery/M1_6_BASELINE.md` with the complete facet table, reproduction method, and the resulting independent budgets for base native code, runtime download, expanded installation, and rollback/staging headroom.

### Boundary

- This measurement covers the current monolithic debug APK, not an AAB or release build and not a delivery prototype. The next M1.6 work remains Play asset-pack layout plus an independently signed, stream-installed data pack behind one installer interface.

## 2026-08-13 — App-owned long-lived Lean LSP session

### Implemented

- Connected `core-lsp` to the existing shell-free `core-process` boundary and added `LeanLspSession`, which owns one launched server, its byte-framed stdin/stdout transport, and document-version gate.
- Enforced lifecycle ordering from process start through initialize request, initialized notification, document open/change/close, shutdown request, exit, and closed state. Invalid ordering fails before writing protocol bytes.
- Added graceful-exit behavior that closes server stdin, waits for the requested grace period, and falls back to forced termination. Closing a session before shutdown also owns forced cleanup.
- Kept raw inbound message reading single-owner and blocking by design. JSON-RPC response/notification dispatch, dynamic registration replies, diagnostic decoding, and reconnectable Android service ownership are the next layer rather than being approximated with substring parsing in production.

### Validation

- Added fake-process integration tests through the real framing implementation. They assert exact lifecycle/document message order, stale diagnostic rejection after a version change, graceful exit without forced termination, forced cleanup on early close, and invalid lifecycle rejection.
- `:core-lsp:testDebugUnitTest` passes in 2m37s (38 tasks; 6 executed and 32 up-to-date).
- The complete `testDebugUnitTest` regression passes after the integrity UI, activation hardening, and LSP session work in 2m54s (107 tasks; 2 executed and 105 up-to-date).
- The first `:app:assembleDebug` pass completed in 3m43s; after correcting the shared busy label from check-specific wording, the final incremental pass completed in 3m23s while reusing unchanged toolchain staging/compression. The resulting 804,600,480-byte APK has SHA-256 `a121310dd949b5918bdaaed3b04a7b35a7e20e77e33476cb512920bce4009acc`; ZIP integrity passes and inspection confirms 2,431 each of private/server/IR facets plus all eight arm64 native entries. ADB started successfully but reported no attached device, so install, visual Check, the new full integrity action, and device LSP regression remain an explicit handoff rather than inferred validation.

## 2026-08-13 — Current-APK device audit, offline proof, and LSP metrics

### Device recovery and validation

- `lsusb` confirmed the attached Samsung tablet at bus/device `001/002`; its WSL USB node had reverted to `root:root` mode `0600`. Changed only that node to `root:plugdev` mode `0660`, restarted the project ADB with `ADB_LIBUSB=1`, and restored the authorized SM-T870 transport.
- Update-installed the 804,600,480-byte APK for Android user 0 while preserving schema-2 sysroot/project data. UI hierarchy confirms the multiline editor plus **Check Lean** and **Verify runtime** actions. Production Check exited 0 in 2,911 ms with the expected theorem type and string evaluation.
- Ran the user-triggered full integrity audit. All 14,864 installed entries matched the digest-bound packaged manifest in 16,778 ms, closing the earlier physical-validation handoff without adding hashing to routine checks.
- Repeated workspace-aware graceful LSP diagnostics and forced transport cleanup. The invalid theorem published the expected `rfl` diagnostic, graceful shutdown exited 0, and forced termination left zero Lean/Lake processes.

### Measurement instrumentation and offline result

- Extended `run-device-lsp.py` to emit initialize latency, `didOpen`-to-first-diagnostic latency, launch-to-diagnostic latency, and peak aggregate Lean/Lake RSS while retaining its lifecycle assertions.
- A connected warm run measured 399 ms to initialize, 1,047 ms from `didOpen` to the first diagnostic, 1,450 ms from launch to diagnostics, and 714,888 KiB peak aggregate RSS.
- Disabled Wi-Fi after recording its original enabled/connected state. The complete local LSP workflow still passed: 413 ms to initialize, 1,035 ms from `didOpen` to diagnostics, 1,453 ms launch-to-diagnostics, and 591,564 KiB peak aggregate RSS. Restored Wi-Fi and confirmed reconnection to the original network.
- Aggregate RSS sums Lean and Lake process RSS and can double-count shared mappings. Treat it as an upper bound; collect process-tree PSS before using the number as a memory budget.

## 2026-08-13 — Physical corruption repair exposed symlink-following deletion

### Probe and root cause

- Confirmed approximately 37 GB free on `/data`, then preserved `Init.ilean`, changed one byte without changing its size, and changed only the installation marker to schema 1. Production Check correctly rejected full verification and staged all 2,175,501 KiB from packaged assets.
- The first activation unexpectedly produced schema 2 with an empty `lib/lean`, and Check reported every representative facet missing. Inspection showed the old `.previous/.lake/build/lib/lean` absolute compatibility link targeted the stable destination path.
- Root cause: Kotlin `File.deleteRecursively()` follows directory symlinks. After staging was renamed to the stable destination, deleting `.previous` traversed its old absolute compatibility link and erased the new destination's Lean library. This destructive interaction was not represented by the earlier payload-only activation tests.

### Fix and regression coverage

- Added `deleteTreeWithoutFollowingLinks()` using `Files.walkFileTree` without `FOLLOW_LINKS`, and replaced recursive deletion for staging, rollback cleanup, and recovery replacement.
- Added an exact host regression: the old tree contains an absolute Lake-style link into its own destination, a new tree takes that destination name, and rollback cleanup must leave the new target bytes intact.
- `:core-toolchain:testDebugUnitTest` passes with the new reproducer in 2m15s. Built the corrected APK in 3m21s and update-installed it over the intentionally incomplete runtime. The final APK is 804,600,480 bytes with SHA-256 `d67647ce2a65f3826bdb8073527532081d54b95577719a6b5566c87bcf188dcb`; ZIP integrity passes with 2,431 each of private/server/IR facets and all eight arm64 native entries.

### Corrected physical recovery

- Production Check restaged the full 2,175,501 KiB, stream-verified it, activated schema 2, preserved `Init.olean`, and removed both `.installing` and `.previous`. Lean then exited 0 in 3,115 ms with expected output. Wall time from Check through repaired Lean output was approximately 54.2 seconds.
- A post-repair user audit matched every manifest entry in 16,626 ms. Graceful LSP diagnostics passed and forced cleanup again left zero processes.
- Added Android `dumpsys meminfo` PSS sampling to the LSP probe. The first cold post-repair run measured 1,072 ms initialize, 2,094 ms `didOpen`-to-diagnostic, 3,171 ms launch-to-diagnostic, 674,488 KiB aggregate RSS, and 544,681 KiB aggregate PSS. The immediate warm repeat measured 419 ms, 967 ms, 1,393 ms, 695,596 KiB RSS, and 89,430 KiB PSS. Report cold and warm results separately.
- Timed same-version installation of the 804.6 MB APK at 36.22 seconds. Before Check its compatibility links still referenced the prior randomized APK path; production Check refreshed them to the current path and remained functional.

## 2026-08-13 — Physical interrupted-staging recovery

### Validation

- Reintroduced the same one-byte/schema-1 corruption, started production repair, and force-stopped the app after staging reached 2,175,505 KiB but before `.installed` was written or activation began. The active tree remained schema 1 and the complete-but-unmarked `.installing` tree remained on disk, accurately representing process death before commit.
- Restarted production Check. The no-follow cleanup removed the stale staging tree before creating a new one, restaged and stream-verified the complete payload, briefly preserved the old active tree as `.previous`, activated schema 2, and safely removed rollback without traversing its compatibility link.
- Recovery left neither `.installing` nor `.previous`, restored the expected manifest-bound marker, and Lean exited 0 in 2,913 ms with expected output. Restart-to-result wall time was approximately 85.4 seconds; this includes deleting the complete stale tree, copying/verifying a second complete tree, activation cleanup, and Lean execution.
- A real low-storage case remains open. The reference device currently has about 37 GB free; manufacturing pressure by consuming unrelated user storage would be inappropriate. The exact preflight threshold remains host-tested.
- The final complete `testDebugUnitTest` regression passes in 2m00s (107 tasks; 4 executed and 103 up-to-date). Final device health shows a 2,175,530 KiB sysroot, a Lean link targeting the current APK native directory, and no residual Lean/Lake process.

## 2026-08-13 — Low-storage pressure moved beyond MVP

- Removed manufactured low-storage pressure from M1/MVP exit requirements. The reference tablet has ample free capacity, and filling unrelated user storage solely to create pressure is disproportionate to the MVP.
- Retained the exact byte-based preflight and readable required/available-space failure. Installed footprint, update/install time, full-audit duration, and cold/warm PSS/latency remain published facts that can inform users when their device is not a practical fit.
- Kept low-storage pressure as an optional M6 hardening/stretch case. This changes test prioritization only; it does not weaken atomic staging, corruption rejection, rollback, or insufficient-capacity refusal.

## 2026-08-13 — Typed JSON-RPC dispatch layer

### Implemented

- Added a dependency-free recursive JSON value parser/renderer with strict trailing-content, duplicate-key, escape, number, object, and array validation so host tests execute the same parsing code used on Android.
- Added typed JSON-RPC request, notification, and response envelopes with numeric/string IDs and exact `result` versus `error` validation.
- Added `LeanLspDispatcher`: Lean `client/registerCapability` requests receive a protocol response automatically; other server requests and ordinary notifications reach a typed sink; `publishDiagnostics` is decoded and delivered only when the session's open-document version gate accepts it.
- Connected dispatcher construction and single-message read/dispatch to `LeanLspSession`. This established the single-reader boundary subsequently implemented by `LeanLspSupervisor`; no competing reader or substring-based production parsing was introduced.

### Validation

- Added tests for nested Unicode JSON round trips, automatic dynamic-registration replies, stale versus current diagnostic routing, notification delivery, malformed JSON, duplicate fields, ambiguous responses, invalid versions/IDs, and trailing-comma rejection.
- `:core-lsp:testDebugUnitTest` passes in 1m28s (38 tasks; 5 executed and 33 up-to-date). Reconnectable Android service ownership is now the next layer.

## 2026-08-13 — Single-reader LSP supervisor

### Implemented

- Added `LeanLspSupervisor` as the sole owner of a session's blocking inbound reader. It creates one named daemon reader thread, dispatches through the typed session dispatcher, and prevents duplicate starts.
- Made supervisor termination explicit and observable: clean server EOF, caller-requested close, and protocol/transport failure are distinct stop reasons. Closing remains idempotent, closes the session/process, and briefly joins the reader without deadlocking a callback running on that reader.
- Kept Android component ownership out of `core-lsp`: the next bound-service layer can retain this supervisor across activity recreation without coupling the protocol library to an Activity or Service lifecycle.

### Validation

- Added tests through real byte framing for multi-message dispatch, automatic `client/registerCapability` reply bytes, clean EOF, malformed-server failure propagation, duplicate-start rejection, and forced process cleanup.
- `:core-lsp:testDebugUnitTest` passes in 1m24s (38 tasks; 5 executed and 33 up-to-date).
- The stricter dispatcher/session changes also pass the complete `testDebugUnitTest` regression in 2m27s (107 tasks; 5 executed and 102 up-to-date).

## 2026-08-13 — Reconnectable bound-service ownership foundation

### Implemented

- Added the app's dependency on `core-lsp` and declared a non-exported `LeanLspService`, keeping protocol/process access inside the application.
- Added a local Binder contract with generation-tagged per-project snapshots and replay-on-attach listeners. A recreated Activity can query existing ownership and distinguish a current session from an older callback.
- The service launches and owns one `LeanLspSession`/`LeanLspSupervisor` per project, rejects duplicate ownership before launching another child, provides explicit project stop, and closes all retained supervisors during service destruction.
- Kept this first component bound-only. Foreground promotion is intentionally deferred until a user-visible background operation and notification/cancellation contract exist; binding the editor and validating recreation are the next integration step.

### Validation

- `:app:testDebugUnitTest` compiles the service, manifest, and new module dependency and passes in 4m07s (91 tasks; 12 executed, 2 from cache, and 77 up-to-date).
- The complete post-service `testDebugUnitTest` regression passes in 2m33s (111 tasks, all up-to-date). The task count increased from 107 because the app now consumes and validates the `core-lsp` library path.

## 2026-08-13 — M1.6 delivery prototypes and decision

### Implemented and decided

- Added a delivery-neutral `RuntimePayloadSource` and one manifest-driven streaming installer. APK assets and an independently distributed ZIP now share entry-size/SHA-256 validation and the existing atomic activation boundary.
- Added complete detached RSA/SHA-256 verification before an independent ZIP exposes entries. No release key was committed; measurements used a temporary `/tmp` test key.
- Added a conditional `core_toolchain_pack` install-time Play Asset Delivery module. `-PplayAssetDelivery=true` removes sysroot data from base and puts data plus manifest in the pack while executable/native entries remain APK-installed.
- Selected install-time PAD for Play and a signed ZIP independently. A smaller artifact model is deferred because both fit without deleting conformance-required private/server/IR facets.

### Measurements and validation

- The PAD debug AAB is 693,129,838 bytes (SHA-256 `46edcbed0eff9a36ed390701d799ed9035a70920c9b79b11cd7fbf2d29d3f9f7`). It contains the runtime under `core_toolchain_pack/assets/` and no base `Init.olean` duplicate; the `/mnt/d` build took 21m33s.
- The independent ZIP is 595,064,864 bytes (SHA-256 `43cf8ffce36db523cbf121b56cdb02c8850063f9c19b3a395c65e164aade6410`) with a 256-byte signature (SHA-256 `2473601a28b0cb77ffdd6b68ea8d41770ecf645f9b50480c768eb8509826b9af`). OpenSSL verification and ZIP integrity pass.
- A complete external-pack test authenticated, streamed, size/hash-checked, and re-verified all 14,864 entries. The successful Gradle invocation took 1m26s including compilation. Its first run safely exposed the builder/source `toolchain/` prefix mismatch before copying; the source contract now accepts that documented prefix.
- Current Play limits checked on 2026-08-13 are 1.5 GB per asset pack and 4 GB cumulative install-time content, both above this payload. Final bundletool/Play Console measurement and limit revalidation remain release tasks; `docs/delivery/M1_6_DECISION.md` records the channel boundary and budgets.

## 2026-08-13 — M2 offline two-module project lifecycle

### Implemented

- Added `core-project` with strict IDs, schema/toolchain compatibility metadata, create/list/open/delete, monotonic atomic saves, typed `lake lean`/supported `lake build`, and a two-module template.
- Added deterministic atomic ZIP export and traversal-safe import with entry-count and 256 MiB expansion bounds. Unsupported Git/network dependencies, executable/native targets, and shell-download workflows fail before Lake launch.
- Added bounded structured one-shot job state and cancellation plus a reconnectable, non-exported `ProjectJobService`; retained LSP ownership remains in `LeanLspService`. Foreground promotion remains deferred until background work has a notification/cancellation UI contract.
- Replaced the single editor buffer with simple `Main.lean` and `Basic.lean` tabs. **Build project** atomically saves both and runs the supported Lake build.

### Host and device validation

- Focused project/process/toolchain/app suites pass, including hostile ZIP traversal, wrong-toolchain and unsupported-workflow rejection, rapid-save timestamp monotonicity, signed-pack tampering, bounded output, and cancellation. The Android instrumentation source compiles.
- Recovered the attached SM-T870 by changing only `/dev/bus/usb/001/002` from `root:root 0600` to `root:plugdev 0660`, restarting project ADB with `ADB_LIBUSB=1`, and using authorized serial `R52R40K1PPN`.
- The first two offline instrumentation runs exposed template defects: sub-second error/fix writes were invisible to Lake's timestamp cache, then `roots = ["Main"]` excluded the imported module. Saves now advance modification time by at least one second, and both `Main` and generated `*.Basic` are explicit roots.
- The final API-33 test passed offline in 6.78s: create two modules, observe an intentional error, fix it, build both, export, delete, import, and rebuild. Wi-Fi was restored to enabled; the imported project exists and `ps -A` shows no residual Lean/Lake process.
- The passing app APK is 804,635,316 bytes (SHA-256 `f115d148ac1f485d199d0b8c7d1009f478020c9ca14f109387ef82dfe812410e`); its 823,291-byte instrumentation APK has SHA-256 `61c7900b8533066d62988cb7a3e6c530821adbd7fc41921b4073d50902d5a132`. Clean-cache assembly took 39m32s on `/mnt/d`; the final incremental rebuild took 3m25s.

## 2026-08-13 — Post-crash device rerun and visible editor migration fix

- Recovered the restarted WSL USB attachment by restoring only `/dev/bus/usb/001/002` to `root:plugdev 0660`; project ADB/libusb reconnected to authorized SM-T870 serial `R52R40K1PPN`.
- The complete host `testDebugUnitTest` regression plus app/test assembly passed in 15m07s. Update-installed both artifacts and reran the M2 instrumentation scenario with Wi-Fi disabled; all error/fix/build/export/delete/import/rebuild assertions passed in 8.82s, then Wi-Fi was restored.
- Launched the physical UI and confirmed the visible `Main.lean` and `Basic.lean` tabs. The first manual **Build project** exposed an upgrade-only collision with the legacy M1 `projects/visual-probe` directory, which predates M2 metadata. Preserved that directory and moved the M2 visual sample to `projects/visual_probe`; the underscore retains the generated Lean namespace `VisualProbe`.
- `:app:testDebugUnitTest :app:assembleDebug` passed after the migration fix in 5m06s. Update-installed APK SHA-256 `f0a63caec34f03c59e556f59edb75c10c70dceb327cad8d0b3d7e4b13e1d91b8` and left the editor visibly open on a successful result: exit 0 in 3,918 ms, `VisualProbe.Basic` and `Main` built, theorem output displayed, and `#eval` returned `42`.
- Final device state has Wi-Fi enabled and no residual Lean/Lake child. The `ps` match is only `org.lean4android.app` itself.
- Final exact-build confirmation: reinstalled migration-fix app APK `f0a63caec34f03c59e556f59edb75c10c70dceb327cad8d0b3d7e4b13e1d91b8` together with instrumentation APK `61c7900b8533066d62988cb7a3e6c530821adbd7fc41921b4073d50902d5a132`, disabled Wi-Fi, and reran `M2OfflineProjectLifecycleTest`. The complete error/fix/build/export/delete/reimport/rebuild scenario passed in 6.791s. Wi-Fi was restored to enabled, the explicit process-name check found no Lean/Lake child, and the latest editor was reopened.

## 2026-08-13 — M3 durable editor state and forced-recreation exit proof

- Began the first M3 slice from the closed M2 visual baseline: retain editor tabs and dirty buffers across Activity recreation, persist recovery state for process recreation, bind the Activity to retained app services, and add contained Lean source file operations around `LeanProjectRepository`.
- No long-running process is active at this checkpoint. The existing migration-fix APK and API-33 M2 offline lifecycle evidence remain the trustworthy baseline.
- Implemented contained source create/read/rename/delete operations with traversal, type, collision, and last-source protection; added atomic recovery snapshots with saved baselines, dirty buffers, active-tab restoration, and corrupt-snapshot fallback; bound `MainActivity` to the retained local LSP service; and exposed New/Rename/Delete actions with dirty indicators in the visible editor.
- Focused `:core-project:testDebugUnitTest :app:testDebugUnitTest :app:compileDebugKotlin` passes (111 tasks) after both the state layer and visible file-operation wiring. No Gradle/Java process remains from those completed runs.
- The full `testDebugUnitTest :app:assembleDebug` regression passes in 4m51s (175 tasks; 9 executed and 166 up-to-date). The resulting 804,770,286-byte APK has SHA-256 `d5282712abfa8baf3fd565b44aa9e7feea341f05e0c010fa6b3e52b366d8fd9f`.
- Update-installed that exact APK for owner user 0 on the authorized SM-T870/API 33. Entered distinct unsaved `M3MAIN` and `M3BASIC` markers through the visible editor, selected `Basic.lean`, and confirmed both dirty indicators before forcing a full app process stop/restart.
- After restart, `Basic.lean` was still active, both tabs still showed dirty indicators, and UI hierarchy inspection found `M3BASIC` in Basic and `M3MAIN` after switching to Main. This is stronger than Activity-only recreation and satisfies the M3 no-loss exit scenario for the two-module sample.
- The exact process-name check found no `lean`, `lake`, `liblean_exe.so`, or `liblake_exe.so` child. Removed only the test-created `files/editor-recovery/visual_probe.bin`, relaunched the app, and confirmed the clean on-disk sample was visible without either marker or dirty indicator. The app remains open; Wi-Fi and project data were otherwise unchanged.
- Remaining M3 product work is the editor bake-off record, search, syntax highlighting, undo/redo, responsive phone/rotation polish, hardware-keyboard coverage, and accessibility basics. File operations and durable state are implemented, but this checkpoint does not claim those remaining features.

## 2026-08-14 — M3 usable editor complete

- Continuing from the device-proven durable-state slice. This checkpoint targets the remaining M3 boundary: bounded per-tab undo/redo, Unicode-safe search, Lean syntax decoration, adaptive phone/tablet portrait/landscape layout, hardware-keyboard shortcuts, accessibility semantics, an explicit native-versus-CodeMirror decision record, and final host/device validation.
- No build or native child is currently running. The trustworthy starting APK remains the 804,770,286-byte `d5282712...` build whose two dirty buffers and active tab survived full process recreation on the API-33 reference tablet.
- Exact next validation: focused editor-engine tests, app compilation, UI/instrumentation coverage, full unit/APK regression, then rotation/forced-recreation and keyboard/accessibility checks on the reference device. M3 will not be marked complete until those observed results and any remaining boundary are recorded.
- Before the host/session crash, implemented the bounded editor engine, adaptive Compose shell, accessibility semantics, hardware-keyboard routing, Compose UI test dependencies, `M3EditorRecreationTest`, and the native-editor decision record. `:app:testDebugUnitTest :app:compileDebugKotlin` passed in 2m54s (106 tasks; 6 executed). The first compile had failed only on missing Compose `key`/`type` extension imports; the corrected rerun passed.
- Crash recovery on 2026-08-14 found no Gradle, Kotlin, ADB, Lean, or Lake process alive. The interrupted `:app:compileDebugAndroidTestKotlin :app:testDebugUnitTest` emitted task progress but no completion. `app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk` is the stale 823,291-byte M2 artifact from 2026-08-13 20:45 and is not M3 evidence; the 804,770,286-byte app APK is likewise the prior durable-state build.
- Resume exactly with `GRADLE_USER_HOME="$PWD/.gradle-user-home" ./gradlew --no-build-cache --no-daemon --console=plain :app:compileDebugAndroidTestKotlin :app:testDebugUnitTest`, correct any instrumentation compile failure, then assemble/install both APKs and run the targeted M3 test before the full regression.
- The exact recovery command completed successfully in 7m08s (117 tasks; 45 executed and 72 up-to-date). It compiled `M3EditorRecreationTest` and passed app unit tests. The only warning was the deprecated v1 Compose Android test-rule namespace; switched the test to the v2 rule and aligned the adaptive side-pane breakpoint to the standard 600dp tablet boundary. No Gradle process remains.
- Next exact command: assemble `:app:assembleDebug :app:assembleDebugAndroidTest`, record both hashes/sizes, then install them for owner user 0 and run only `M3EditorRecreationTest` before broader device checks.
- `:app:assembleDebug :app:assembleDebugAndroidTest` completed successfully in 41m23s (175 tasks; 66 executed and 109 up-to-date). The long quiet intervals were active `/mnt/d` asset compression and APK packaging, not a hang; host inspection measured sustained Gradle CPU use and the run was preserved to completion.
- New exact artifacts: app APK 804,771,115 bytes, SHA-256 `0123b03d67f9ebfe371337d3eec91383510e23ddfeceb699da71616af539ef03`; instrumentation APK 2,475,861 bytes, SHA-256 `6b6effeb22b5019ca8bbd9d1af684e555618b489073bf621e4352ba3c85b134b`. The authorized SM-T870/API 33 is attached as `R52R40K1PPN`; no USB permission repair is currently needed.
- Next exact step: update-install both artifacts for owner user 0, run `M3EditorRecreationTest`, inspect any failure before broader UI/device mutation, and restore the clean editor snapshot afterward.
- Both APKs update-installed successfully for owner user 0. The first targeted `M3EditorRecreationTest` ran but failed in 1.083s before mutation because it assumed `Main.lean` was initially active; the durable snapshot correctly restored `VisualProbe/Basic.lean`, so only the Basic editor semantic node existed. Normal launch confirmed the M3 tablet side-pane, full project paths, Basic editor, Undo/Redo/Find/Build/Verify controls, and no app crash.
- Filtered logcat `FATAL EXCEPTION` entries belong to overlapping `uiautomator dump` helpers (`UiAutomationService ... already registered`), not `org.lean4android.app`, matching the earlier documented Samsung behavior. Made the test select Main only when the Main editor node is absent, preserving the product's active-tab recovery contract.
- Next exact validation: rebuild the small instrumentation APK (the app APK is unchanged), reinstall it, rerun the targeted M3 test, and only then proceed to device-shape/keyboard checks.
- The corrected test APK rebuilt in 3m28s and installed, but the rerun exposed a Compose v2 test-rule compatibility issue: `StandardTestDispatcher` left the Activity launch queued and the test reported no Compose hierarchy after 7.95s. This is distinct from the first active-tab assumption and from product startup; normal app launch remains visible.
- Reverted only the warning-driven v2 test-rule migration to the v1 rule, which previously launched the Activity and reached semantics. The deprecation warning is accepted for the pinned Compose 1.11.4 test lane; M3 does not require migrating coroutine test dispatch semantics.
- Next exact validation: rebuild/reinstall only the instrumentation APK again and rerun the targeted test with both the proven v1 launch rule and active-tab-independent setup.
- The v1-rule test APK rebuilt in 2m31s (SHA-256 `9350cecf0122212d6405d7c37f890b60830f518e2668c011e3da3594f09353f0`) and installed. Both with the app already open and after an explicit owner-user force-stop, the Activity-owning Compose rule remained launch-order-dependent: the former could attach too late and the latter found no hierarchy. This is test-harness behavior, not an app crash; normal launch remains healthy.
- Replaced `createAndroidComposeRule` with `createEmptyComposeRule` plus explicit `ActivityScenario.launch/recreate/close`, making Activity ordering deterministic and allowing exact recovery-snapshot cleanup through the target context.
- Next exact validation: rebuild/reinstall only the instrumentation APK and rerun from a force-stopped app; if it passes, keep this invocation as the repeatable M3 recipe.
- Crash recovery confirmed that deterministic-test rebuild completed successfully in 2m36s (157 tasks; 4 executed and 153 up-to-date). No Gradle process remains. The newly packaged instrumentation APK is the next trustworthy test artifact; install it for owner user 0 and run the targeted class from an explicitly force-stopped app.
- Installed the 2,475,861-byte instrumentation APK (SHA-256 `471d174697a5e40ae2318c2fff7b6f8208fc1c61ee5ea6c9631e482b4240fd2c`) and reran the targeted class. The in-method `ActivityScenario.launch` still raced before Compose registered its hierarchy and failed before product mutation after 9.633s. Replaced it with an outer empty Compose rule and inner `ActivityScenarioRule`, which guarantees rule registration before Activity launch, and added failure-safe recovery-snapshot cleanup.
- Next exact validation: rebuild/reinstall the instrumentation APK and rerun the same targeted class. Product APK and normal tablet launch remain unchanged and healthy.
- The rule-ordered APK rebuilt successfully in 35m35s after the case-normalized Gradle path invalidated all 157 prerequisites; the filtered toolchain manifest remained 14,864 entries with SHA-256 `f7e389dcee7bd8f146fcd9e7f05ca6ddc9243bd3e99e3261a5dee79e7d1aa797`. The resulting test APK SHA-256 was `bdc253f919b1abf3036e24e943157f666d8af2f915ffe5965bec4ef189439366`.
- Even with Android visibly reporting a successful `MainActivity` launch, the pinned Compose v1 test registry never attached a hierarchy under instrumentation; repeated failures occurred before editor mutation. Removed that unreliable registry coupling and its dependencies. The device test now covers actual `ActivityScenario` resumed/recreate/resumed lifecycle, while deterministic editor engine/session behavior remains covered by host unit tests and visible UI behavior by direct device acceptance.
- Next exact validation: rebuild the now-small lifecycle instrumentation APK from the warm canonical cache, install it, run `M3EditorRecreationTest`, then perform visible adaptive-layout, hardware-keyboard, accessibility-semantics, build, and orphan-process checks.
- The lifecycle-only test APK rebuilt successfully in 5m52s (157 tasks; 154 executed) with SHA-256 `4b646c3750cd762d5cb0946bae1000ec70b0c11cf30760efbe7a2ae134d5a420`. Its first run revealed the actual harness blocker: the tablet was `Dozing`, NotificationShade had focus, and ActivityScenario remained at `CREATED`. After `KEYCODE_WAKEUP` and `wm dismiss-keyguard`, the unchanged test passed in 0.908s, proving resumed/recreate/resumed lifecycle. No product defect was involved.
- Device is now awake. Next exact validation: launch the app visibly; capture tablet and compact layout semantics; exercise Ctrl-F/undo/save or visible controls; run a project check; restore all display mutations; and verify no exact Lean/Lake child remains.
- Visible API-33 acceptance passed. Portrait tablet semantics show the `Project files` side panel, full `Main.lean` and `VisualProbe/Basic.lean` paths, editor description, and Undo/Redo/Find/Build/Verify controls. Hardware Ctrl-F opened `Find text in current Lean file`. A temporary `wm size 720x1280` produced the compact tab/action layout without the side panel. Forced landscape retained the tablet side panel and every action. Restored physical `1600x2560`, `accelerometer_rotation=1`, and `user_rotation=0` exactly.
- Tapped the visible Build project action after a cold launch. The UI reported exit 0 in 1,214 ms, replayed Main, printed the theorem type and value 42, and ended `Build completed successfully (4 jobs).` The exact process-name check found no `lean`, `lake`, `liblean_exe.so`, or `liblake_exe.so` child afterward.
- Next exact validation: run the required full host `testDebugUnitTest :app:assembleDebug` regression, record final APK hash, inspect diff/status, finish the M3 plan/rebuild documentation, and commit only the M3 change set while preserving the user's separate `AGENTS.md` modification.
- Final `testDebugUnitTest :app:assembleDebug` passed in 40m46s (175 tasks; 48 executed and 127 up-to-date). The final debug APK is 804,770,604 bytes with SHA-256 `fa034846972bf9285f47c9dce8e6cc5a522d54b454e68060bd5010ba6717bcb5`; `git diff --check` passes.
- Update-installed that exact APK for owner user 0 and left `MainActivity` top-resumed and visibly showing the completed tablet editor: Project files, Main editor, Undo, Redo, Find, Build project, and Verify runtime. No native child remained. M3 exit criteria are complete; the next product boundary is M4 editor-to-LSP document synchronization and live diagnostics.

## 2026-08-14 — M3.1 pre-M4 application-shell requirements accepted

- Added a required M3.1 milestone ahead of M4 for a Pydroid-inspired but native Compose IDE shell. The accepted specification is `docs/editor/M3_1_APP_SHELL_REQUIREMENTS.md`; this is a requirements/documentation checkpoint only, with no product code or device state changed.
- The top bar will contain a hamburger/drawer control, active filename and project-relative path, Run/Play, an anchored Files menu (New, Open, Save, Save As, Close), and an anchored More menu (Undo, Redo, Find). The Find surface's text close action becomes an accessible X icon.
- The drawer will contain a collapsible hierarchical **Project** section plus Build project and Verify runtime, and will remain structurally extensible for future Settings and destinations.
- Semantics were made explicit to prevent misleading UI: Save persists without building; Save As retains the original file and switches to the validated new path rather than relabeling Rename; Close closes a tab without deleting and protects dirty buffers; Run stays within the supported offline Lean/Lake boundary and must not enable native targets, arbitrary scripts, downloads, or a terminal.
- M4 editor-to-LSP synchronization is now gated on host and physical-device proof of the M3.1 menus, drawer, file transitions, save-build-run behavior, adaptive layout, keyboard/focus, accessibility, recreation, cancellation, and exact no-orphan cleanup.
- Clarified popup dismissal as an explicit requirement: each attached menu closes on outside click, Back/Escape, selection, or opening the other menu; only one may be open. The drawer likewise closes on outside click, Back/Escape, or hamburger toggle, and opening it closes any popup menu.
- Accepted and documented the M3.1 Open workspace design in `docs/editor/M3_1_OPEN_WORKSPACE_REQUIREMENTS.md`. Open will provide Recent, My Projects, Import Project Archive, Import Project Folder, Open Lean File, and New Project rather than exposing ambiguous raw Android storage roots.
- Reaffirmed SAF as an import/export boundary: external archives/folders are validated and atomically copied into internal projects; a selected `.lean` file becomes either a generated standalone scratch project or an atomic copy inside a selected existing project. Lean/Lake never runs directly against provider URIs, cloud/removable pseudo paths, or external mutable sources.
- Documented Save/Save As versus Export semantics, uninstall-sensitive app storage guidance, dirty-buffer/project-switch behavior, bounded hostile-import tests, picker/recreation/cancellation handling, and exact orphan-process acceptance. M3.1 and its Open workflow remain required before M4.

## 2026-08-14 — M3.1 application shell and file workflow complete

- Began implementation from the clean, device-proven M3 baseline and the accepted M3.1 shell/Open-workspace specifications. No Gradle, ADB, Lean, or Lake process is assumed to have survived from the prior session; no long-running process has been started at this checkpoint.
- Trustworthy baseline remains the final M3 debug APK (804,770,604 bytes, SHA-256 `fa034846972bf9285f47c9dce8e6cc5a522d54b454e68060bd5010ba6717bcb5`) and its recorded API-33 editor/build/recreation evidence. M3.1 has requirements only and no product implementation yet.
- Work in progress: inspect the existing project repository, editor session/recovery model, Activity shell, services, and tests; then implement repository/import primitives, editor command state, the Compose shell/Open workspace/SAF boundary, and proportional host/device validation.
- Exact next resume step: inspect `LeanProjectRepository.kt`, `EditorSessionStore.kt`, `LeanEditorEngine.kt`, `MainActivity.kt`, `ProjectJobService.kt`, their tests, Gradle dependencies, and manifest before editing product code. No relevant process is currently known to be alive.
- Implemented the first M3.1 shell/state slice. `EditorSessionState` now supports a useful zero-tab state, per-file saved baselines, Save As retaining the original tab, and schema-2 recovery snapshots (older snapshots safely fall back to project files). `LeanProjectRepository.copySource` provides atomic Save As with contained paths and case-folded collision rejection.
- Replaced the generic header with filename/path, hamburger, Run, anchored Files and More menus; moved project file selection plus Build/Verify into a collapsible drawer surface; changed Ctrl-S to atomic Save; added dirty Save/Discard/Cancel Close handling and an empty-editor message. The Files menu exposes New/Open/Save/Save As/Close and More exposes truthful Undo/Redo/Find enablement.
- Focused validation passes: `:core-project:testDebugUnitTest :app:testDebugUnitTest :app:compileDebugKotlin` completed in 2m52s (111 tasks; 5 executed, 106 up-to-date). An earlier compile correctly exposed nullable active-path call sites after zero-tab support; all are now guarded. No Gradle process remains from the successful run.
- Still pending and not claimed: the current Open-workspace surface is only the destination shell; Recent/My Projects switching, SAF archive/folder/file import, scratch/add-to-project flows, cancellation/recreation, and uninstall/export guidance require implementation. Run currently retains the proven save-and-build behavior but still needs the specified supported entry execution/cancellation integration. Popup/drawer Back/Escape/outside-click behavior, adaptive/accessibility polish, instrumentation/APK assembly, and physical-device acceptance also remain.
- Exact next resume step: implement host-testable bounded folder and standalone-file import/activation plus recent-project identity, connect Activity Result SAF launchers and internal project switching to the Open-workspace surface, then rerun the same focused suites before adding Run/cancellation and device tests.
- Added host-testable external-copy primitives: bounded folder import walks without following links, caps entry/per-file/aggregate sizes, rejects case-folded duplicates, validates project metadata and the offline workflow before atomic activation, and removes failed staging. Standalone and add-to-project Lean-file imports enforce an 8 MiB cap and copy into app-managed storage.
- `:core-project:testDebugUnitTest` passes in 2m14s (51 tasks; 5 executed, 46 up-to-date), including valid folder activation, standalone scratch generation, invalid-folder cleanup, Save As retention, traversal, unsupported workflow, and archive coverage. No Gradle process remains.
- Exact next resume step is now Activity integration: register SAF document/tree launchers, stream provider content into bounded temporary staging, expose project naming/collision decisions, switch only after successful activation, and persist stable recent identities without provider URIs.
- Connected the Open-workspace surface to Android SAF contracts without broad storage permission. Archive, directory-tree, and Lean-file provider content is bounded while streaming into cache staging; repository validation/atomic activation completes before workspace switching, provider URIs are not persisted, and cleanup uses a no-follow file-tree deletion. Lean-file selection explicitly offers standalone scratch or add-to-current-project copies.
- Added stable recent project IDs in private preferences, validated before display; full-screen Recent/My Projects navigation; New Project; uninstall/export guidance; and Back/Escape/outside dismissal for the Open surface, menus, and drawer. The search close control is now an accessible X icon.
- Run now saves every open buffer, performs the supported offline Lake build, then runs `lake lean` on `Main.lean` (or the first Lean source) and combines bounded results. Unsupported workflow validation remains before command construction. Cancellation/service ownership and exact orphan checks remain to be closed on device.
- `:app:testDebugUnitTest :app:compileDebugKotlin :app:compileDebugAndroidTestKotlin` passes in 3m37s (117 tasks; 7 executed, 110 up-to-date). No Gradle process remains.
- Exact next step: run the full host unit regression and assemble both app/instrumentation APKs; then update-install on the reference device and perform M3.1 menu/drawer/save/Save As/Close/Open/Run/recreation/adaptive/accessibility/orphan acceptance, correcting any observed product defect before closing the milestone.
- Full `testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest` passes in 12m55s (205 tasks; 7 executed, 198 up-to-date). App APK: 805,120,894 bytes, SHA-256 `880086b446cd489355b891d619146629e24fc2b771e3ba71adae8a9be44578fc`; instrumentation APK: 824,695 bytes, SHA-256 `4b646c3750cd762d5cb0946bae1000ec70b0c11cf30760efbe7a2ae134d5a420`. `git diff --check` passes.
- Sandbox-local ADB failed only because sockets/libusb are restricted. Outside that sandbox, `lsusb` resolved the current Samsung node as `001/002`, project ADB/libusb started without a permission change, and authorized SM-T870/API-33 serial `R52R40K1PPN` is online. Exact next step: update-install both recorded APKs for owner user 0 and begin targeted device acceptance.
- Physical testing of the first M3.1 APK found and corrected three UI defects: the legacy bottom action strip duplicated commands moved into menus/drawer; the drawer surface did not consume pointer events, allowing its scrim to close it immediately; and its tree was derived from open tabs, making the zero-tab recovery path empty. The drawer now consumes inside taps, lists repository files, and reopens closed files.
- On the corrected build, Files showed truthful New/Open/Save/Save As/Close states and outside dismissal. The drawer showed collapsible Project, `Main.lean`, `VisualProbe/Basic.lean`, Build, and Verify. Save As created `M3Copy.lean`, retained byte-identical `Main.lean`, Ctrl-S saved without building, and dirty Close visibly enforced Save/Discard/Cancel. Cancel retained the buffer; Discard preserved saved bytes.
- Full-screen Open workspace physically showed all six destinations plus uninstall/export guidance; picker cancellation returned to the unchanged editor. A SAF-selected valid ZIP activated as internal `archive_1786730738483` and ran offline with exit 0 in 2,518 ms and `#eval 73`. The visual project Run exited 0 in 3,625 ms after build plus `lake lean Main.lean`; exact process-name inspection found no Lean/Lake child.
- Moved both Run phases to bounded `ProcessJobSupervisor` instances, exposed Cancel while active, close the current supervisor on Activity destruction, and map cancellation to a truthful terminal state. Focused `:app:testDebugUnitTest :app:compileDebugKotlin` passes in 2m42s (106 tasks; 5 executed, 101 up-to-date).
- Exact next step: assemble/install the cancellable APK, trigger Run then Cancel and verify zero children; complete adaptive/recreation and remaining practical SAF checks; rerun the final full regression and record the final artifact before closing M3.1.
- Device acceptance subsequently proved cancellation during a cold build: the UI reached `Run cancelled.`, Activity-owned supervision terminated the command, and exact process-name inspection found no Lean/Lake child. Compact `720x1280`, tablet portrait, forced landscape, and recreation checks passed; the display was restored to physical `1600x2560`, accelerometer rotation enabled, and user rotation 0. The dedicated provider-boundary instrumentation scenario passed valid archive/folder/scratch/add-to-project imports plus hostile-workflow rejection and cleanup.
- Final audit in progress: provider folder staging previously trusted the reported aggregate size after enforcing only the declared per-file size. The copy now caps bytes actually read for every file and computes the aggregate from actual bytes; stale deleted-project preferences also self-heal to the baseline project. Exact next step: rerun the full host/APK regression, reinstall the resulting app and instrumentation APKs, rerun the physical import and recreation scenarios, then record final hashes/state and close M3.1. No Gradle or native child is assumed alive at this checkpoint.
- Final validation completed. `testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest` passed in 4m03s (205 tasks; 9 executed, 196 up-to-date). The installed final app APK is 805,124,846 bytes with SHA-256 `b1fee0f2ea44eeb3878ffd2f59fc103b7efee1ae5b9eaced7842483ce5e2af26`; the installed instrumentation APK is 846,964 bytes with SHA-256 `7b7acbe9d2146eeb24b167a6b7a182f04052b3bc416ca05abc17df5d9d0cfca7`.
- On authorized Samsung SM-T870/API 33 serial `R52R40K1PPN`, the final APK passed `M31WorkspaceImportTest` in 0.106s and `M3EditorRecreationTest` in 0.910s. A final owner-user-0 cold launch completed in 643 ms and visibly exposed the zero-tab recovery shell (`No file open`, navigation drawer, Run, Files, and More), proving the deleted imported-project preference repaired to `visual_probe` without retaining an external URI.
- The device remains at physical `1600x2560`, accelerometer rotation 1, user rotation 0. Test-created archive/folder/scratch/source projects and Downloads fixtures were removed; the final exact `pidof` checks for `lean`, `lake`, `liblean_exe.so`, and `liblake_exe.so` all returned absent. No Gradle or native child remains. M3.1 exit criteria are complete; the next exact product step is M4 editor document synchronization with `LeanLspService` and live version-filtered diagnostics.

## 2026-08-14 — M3.2 tabs, line numbers, and appearance complete

- Added M3.2 as the required final pre-M4 milestone. The accepted behavior is recorded in `docs/editor/M3_2_TABS_LINES_APPEARANCE_REQUIREMENTS.md`: browser-style retained/focused open-file tabs, aligned one-based line numbers, and an extensible Drawer → Settings → Appearance hierarchy with a persistent dark-theme toggle.
- The completed M3.1 APK and physical evidence remain the trustworthy baseline. This is initially a Kotlin/Compose and app-private-preference change; it does not authorize changes to the Lean toolchain, process boundary, project format, SAF boundary, or supported offline workflow.
- Implementation in progress: add the visual/semantic tab strip over the existing recovered multi-buffer state, derive a presentation-only gutter without modifying source, and lift theme preference ownership to the Activity so all full-screen pages and dialogs update immediately. No Gradle or native Lean/Lake process is assumed alive at this checkpoint.
- Exact next step: implement the Compose tab/gutter/settings surfaces and small host-testable line-number helper, then run focused app tests/compilation before assembling and physically validating the exact APK on the API-33 reference tablet.
- Implemented the first product slice. Every recovered/open buffer now appears in a horizontally scrollable tab strip with selected/inactive and dirty semantics; drawer file selection continues to add/focus unopened files without removing existing tabs. The source surface now pairs a one-based presentation-only gutter with the same monospaced line metrics and shared vertical scrolling. Drawer Settings opens a full-screen extensible page, Appearance opens a distinct child page, and its accessible switch immediately updates the complete Material theme and persists app-privately.
- Added a host unit check for empty, multi-line, and trailing-newline gutter derivation. Focused `:app:testDebugUnitTest :app:compileDebugKotlin` passed in 2m54s (106 tasks; 5 executed, 101 up-to-date); no Gradle process remains. Exact next step: assemble the exact APK, update-install it on the reference tablet, visually inspect tab/gutter layout, exercise dirty tab switching plus Settings/Appearance theme persistence and recreation, correct device-visible defects, then run the final full regression and offline Run/no-orphan acceptance.
- Final `testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest` passed in 3m22s (205 tasks; 4 executed, 201 up-to-date). The installed M3.2 app APK is 805,164,966 bytes with SHA-256 `18fc800c0f745535d86053a24b0c0079f091754de45ff98d396ad0bb5240fd99`; the matching installed instrumentation APK remains 846,964 bytes with SHA-256 `7b7acbe9d2146eeb24b167a6b7a182f04052b3bc416ca05abc17df5d9d0cfca7`.
- Physical API-33 acceptance on authorized Samsung SM-T870 serial `R52R40K1PPN` visibly showed `Main.lean` and `Basic.lean` together, selected/inactive styling, and an unsaved `Basic.lean •` marker retained while focus moved through the other tab and back. The temporary one-character edit was deleted before stopping. The gutter showed lines 1–8 exactly aligned with the eight logical lines of `VisualProbe/Basic.lean`; the host test separately proves empty and trailing-newline derivation.
- Drawer → Settings opened a full-screen Settings page, Appearance opened a distinct full-screen child page, and the labeled accessible dark-theme switch changed the whole surface immediately. Captured light/dark Appearance screenshots differed correctly; `M3EditorRecreationTest` passed in 0.849s while dark was selected, and a subsequent 667 ms cold launch remained dark. The original light preference was then restored and a final screenshot confirmed the light editor.
- Temporary `720x1280` compact and forced landscape checks both kept tabs, gutter, editor, and output usable; the device was restored to physical `1600x2560`, accelerometer rotation 1, user rotation 0. The final visible offline Run exited 0 in 3,426 ms with the expected theorem, values 42 and 5, and successful four-job build. Exact `pidof` checks found no `lean`, `lake`, `liblean_exe.so`, or `liblake_exe.so` process afterward. M3.2 is complete; the next exact product step is M4 editor document synchronization and live version-filtered diagnostics.

## 2026-08-14 — User-facing project configuration design recorded

- Added `docs/project/PROJECT_CONFIGURATION_DESIGN.md` and linked it from the implementation plan. The accepted direction is a project-specific, typed Settings model that generates and atomically validates the supported `lakefile.toml` subset rather than presenting unrestricted executable configuration as if all desktop Lake capabilities worked on Android.
- The initial model covers package identity, Lean-library roots/source layout, supported default build/Run targets, read-only pinned-toolchain compatibility, and a small allowlist of Lean/server options applied consistently to builds and M4 LSP sessions. Signed offline dependency-pack selection aligns with M5.
- Preserved boundaries: no `lakefile.lean`, arbitrary/raw Lake configuration, Elan/toolchain download, Git/registry/network resolution, provider-backed live workspaces, or executable/native/external/custom/plugin targets without a separate security and capability decision. This documentation-only change did not alter product code, APKs, project/device data, or processes.

## 2026-08-14 — M3.3 user-facing project export requirements accepted

- Added M3.3 as the next required pre-M4 milestone and recorded the accepted contract in `docs/project/M3_3_PROJECT_EXPORT_REQUIREMENTS.md`. This is requirements/documentation only; the existing repository ZIP primitive and M2 instrumentation round trip are not claimed as a user-facing export implementation.
- The planned drawer **Export project** action uses SAF `CreateDocument`, offers explicit Save and Export / Export saved version / Cancel handling for dirty buffers, and creates a bounded portable ZIP containing sources and required configuration/metadata while excluding `.lake`, caches, recovery, links, device paths, logs, secrets, and toolchain data.
- Updated the implementation plan, current rebuild/status guide, M3.1 shell/Open requirements, project-configuration design, and README so M4 is gated on physical export/reimport/offline-build, provider cancellation/failure cleanup, adaptive drawer access, recreation, and exact no-orphan evidence. No product code, APK, device/project/provider data, or process state changed.

## 2026-08-14 — M3.3 implementation and M4 completion work in progress

- Began from the device-proven M3.2 baseline and the accepted M3.3 export requirements. No Gradle, Kotlin, ADB, Lean, or Lake process is assumed to be alive; no long-running command has started at this checkpoint. The existing M3.2 APK and its recorded physical evidence remain the trustworthy product baseline.
- Expanded M4 in `IMPLEMENTATION_PLAN.md` before product work. The required Goals pane has persisted Auto/Right side/Bottom placement under Settings → Editor; Auto means Bottom in portrait and Right side in landscape. Goals/editor/output layouts require accessible bounded splitters, independent persisted normalized sizes, recreation/orientation re-clamping, compact-window/IME behavior, and physical tablet plus phone-sized validation.
- Work in progress starts with the gating M3.3 boundary: inspect the existing ZIP repository primitives, Activity/SAF launchers, editor dirty-state commands, drawer, and tests; implement bounded portable export and the user-facing CreateDocument flow; then run focused host validation before APK/device acceptance. M4 LSP document synchronization will begin only after M3.3 exit evidence is complete.
- Exact next resume step: inspect `LeanProjectRepository`, import/export helpers and tests, `MainActivity`, editor session state, and existing Activity result contracts. No relevant process is currently known to be alive.
- Implemented the first M3.3 product slice. `LeanProjectRepository` now streams deterministic portable ZIPs to either a file or provider output, includes only metadata/toolchain/Lake configuration and Lean sources, rejects links/non-regular entries, bounds entries/per-file/aggregate bytes, and removes a failed atomic file export. The drawer now exposes accessible **Export project** through SAF `CreateDocument`; clean projects launch it directly, while dirty projects offer Save and Export, Export saved version, and Cancel. Provider failure attempts deletion of the exact partial document and never changes project identity or runs Lean/Lake.
- Added repository tests for deterministic ordered membership, generated/private exclusion, streaming equivalence, and link rejection. The initial focused run reached the tests and found only an expected-message mismatch for a correctly rejected symlink; after making link errors explicit, `:core-project:testDebugUnitTest :app:testDebugUnitTest :app:compileDebugKotlin` passed in 3m11s (111 tasks; 9 executed, 102 up-to-date). The sandbox-only Gradle wildcard-socket failure before that run was infrastructure and did not reach compilation.
- No Gradle or native process remains from the successful focused run. Next exact step: add Android lifecycle/result coverage where practical, assemble the exact app/instrumentation APKs, then perform the required API-33 clean/dirty/cancel/export/reimport/offline-run/archive-inspection/adaptive-layout/no-orphan acceptance before marking M3.3 complete and starting M4.
- Exact APK assembly passed in 5m14s (175 tasks; 6 executed, 169 up-to-date). The M3.3 app APK is 805,188,838 bytes with SHA-256 `0eed423c2e4b3482e3467f541ad806d268942d44da817620115065b5102782a2`; the unchanged instrumentation APK is 846,964 bytes with SHA-256 `7b7acbe9d2146eeb24b167a6b7a182f04052b3bc416ca05abc17df5d9d0cfca7`.
- The first sandboxed device probe could not initialize libusb or bind the ADB smart socket (`Operation not permitted`); this is WSL/sandbox infrastructure, not an app result. No device mutation occurred. Exact next step is an unrestricted `lsusb` plus project ADB device check, narrow USB-node repair only if currently required, then owner-user-0 update install and M3.3 physical acceptance.
- The authorized SM-T870/API 33 was attached without USB permission repair. Both exact APKs update-installed for owner user 0, and the app cold-launched in 873 ms. The navigation drawer visibly exposed the accessible **Export project** action between Verify runtime and Settings.
- Physical M3.3 acceptance passed. A clean `visual_probe` export used the real Downloads `CreateDocument` provider. The 1,064-byte ZIP had exactly five sorted entries (`.lean4android-project`, `Main.lean`, `VisualProbe/Basic.lean`, `lakefile.toml`, `lean-toolchain`), correct bytes/toolchain identity, and none of the forbidden generated/private patterns. Repeating Export saved version while a visible `M33DIRTY` buffer remained dirty produced the identical SHA-256 `ac1a5ea57c2b5bf5f57dc7b4c3b9457552f138e7947f9b6f1036566f338c6970`; Save and Export produced a distinct archive containing the marker and cleared the dirty indicator. Dialog Cancel and actual provider-picker Back both preserved the project/editor state.
- Used the proven Ctrl-Z/Ctrl-S shortcuts to remove and durably save away the temporary marker. Reimported the clean exported ZIP through Files → Open → Import Project Archive as fresh internal project `archive_1786739762075`. With Wi-Fi disabled, Run built four jobs and ran Main with the expected theorem plus values `42` and `5`. Drawer export access remained visible in forced landscape and at temporary `720x1280` phone size.
- Restored physical `1600x2560`, accelerometer rotation 1, user rotation 0, and enabled Wi-Fi. Switched away from and removed only the test-created imported project/recovery state and the two Downloads fixtures. A final 793 ms cold launch repaired to the original project; exact `pidof` checks found no `lean`, `lake`, `liblean_exe.so`, or `liblake_exe.so` child. M3.3 exit criteria are complete and M4 is now unblocked.
- M4 implementation is now in progress. Exact next step: inspect `LeanLspService`, core LSP supervisor/client message APIs and tests, command factory startup, and editor state/cursor ownership; then connect generation-aware document open/change/save/close with current-version diagnostics before adding the Goals pane and its placement/resizing settings.
- Implemented the first M4 host-side synchronization slice. Core LSP now emits `didSave`, validates generic JSON-RPC requests, constructs standard UTF-16 text-document positions, and has typed pinned-Lean `$/lean/rpc/connect` plus `$/lean/rpc/call`/`Lean.Widget.getInteractiveGoals` payloads based on the exact checked-out Lean 4.32.1 protocol sources. Added transcript tests for those messages.
- `LeanLspService` now retains the actual session together with its one-reader supervisor, exposes generation-checked initialize/document/request operations, and reconciles the complete editor buffer set through service-owned version state so Activity recreation does not duplicate `didOpen`. `MainActivity` now starts the pinned workspace-aware `lake serve`, completes initialization from response ID 1, synchronizes all open/changed/saved/closed buffers, filters callbacks by current service generation, and renders bounded current-file diagnostic messages plus visible server state.
- Validation in progress: run `:core-lsp:testDebugUnitTest :app:testDebugUnitTest :app:compileDebugKotlin`, correct compile/protocol/state failures, and only then assemble or exercise the real server. No long-running process was alive before this command.
- The first compile exposed only two contained Kotlin issues: a `buildList` type inferred too narrowly from the generation-open branch and cross-module smart-cast restrictions on nullable JSON errors. Explicit operation typing and local nullable rendering corrected them. The following focused run passed all 111 tasks in 3m26s.
- Extended the slice to cover the full initial M4 surface. Cursor changes are debounced while preserving ordered service-owned document versions; diagnostics retain ranges, reject stale generations/versions in the session, render in a bounded Messages surface, and underline the exact UTF-16 source range. Standard hover, completion, definition, and reference requests are generation/version tagged; same-project definitions focus/open the target tab and cursor position.
- Runtime goals use both `$/lean/plainGoal` for a resilient text result and the exact pinned `$/lean/rpc/connect` plus `$/lean/rpc/call` `Lean.Widget.getInteractiveGoals` path, with RPC sessions scoped to generation/document and reconnectable after restart. The visible Goals pane displays server state, goals, hover, completions, definition status, and references.
- Added Settings → Editor → Goals pane position with persisted Auto/Right side/Bottom choices. Auto resolves Bottom in portrait and Right side in landscape. Independent normalized right/bottom sizes and the editor/output size persist privately and clamp to usable bounds. Visible splitters support drag, arrow keys, and accessibility larger/smaller actions. Added explicit restart, three-attempt 500/1000/2000 ms automatic crash backoff, current-document reopening on the new generation, and a separate bounded stderr drain so server logs cannot deadlock or contaminate stdout framing.
- Host coverage now includes generation reopening/no-duplicate sync, monotonic edits, save/close ownership, UTF-16 offset conversion in both directions, diagnostics decoration, placement resolution/clamping, didSave, standard requests, pinned Goals RPC payloads, and bounded stderr draining. The full current focused command `:core-lsp:testDebugUnitTest :app:testDebugUnitTest :app:compileDebugKotlin` passed in 3m27s (111 tasks; 10 executed, 101 up-to-date). No Gradle/native process remains.
- Exact next validation: assemble the app and instrumentation APKs, install on the authorized API-33 tablet, observe real `lake serve` initialization/diagnostics/goals/hover/completion/definition/reference behavior, correct any protocol/UI defect, then exercise rapid edit/save/switch/recreation/restart and all pane placements/splitters before the final regression and measurements.
- The first complete M4 app/instrumentation assembly passed in 4m53s (175 tasks; 8 executed, 167 up-to-date). The candidate app APK is 805,335,178 bytes with SHA-256 `ecc73df8bf98570e274e2eb842fcacf008d19a29a18aa092a3b0dea4f68d0e75`; the instrumentation APK remains 846,964 bytes with SHA-256 `7b7acbe9d2146eeb24b167a6b7a182f04052b3bc416ca05abc17df5d9d0cfca7`. No device result is inferred from assembly.
- Physical validation is now in progress. Exact next step: resolve the current Samsung USB node and authorized project-ADB transport, update-install both recorded APKs for owner user 0, then cold-launch and inspect real server initialization and UI before mutating editor or device state. No Gradle process remains from the completed assembly.
- The first physical launch reached `Lean server: Ready` in portrait Auto/Bottom mode and rendered the real current-file parser diagnostic for the pre-existing `M33DIRT` fixture. Moving the cursor then exposed a packaging defect: the app crashed with `NoClassDefFoundError: org.lean4android.lsp.LeanLspRequests`. Audit proved the class exists in the regenerated core-LSP compile/runtime JARs but was absent from the candidate APK dex, so this is stale incremental dex packaging rather than a Lean protocol failure.
- Validation in progress: force-rerun the toolchain-bearing `:app:assembleDebug` task graph with `--no-build-cache --no-daemon --console=plain --rerun-tasks` (one Gradle build, project-local state). It is currently alive in the expected quiet multi-gigabyte sysroot staging phase on `/mnt/d`; do not interrupt it. After completion, inspect APK dex for `LeanLspRequests` before reinstalling and repeating the cursor/goals test. The crashed Activity and its owned server process were killed by Android; no old app process remains.
- Added the user-requested post-M4 M4.1 plan boundary: named project creation plus direct **New Project** access in the Files menu, sharing one validated atomic flow with Open workspace. This remains queued until all M4 exit evidence passes.
- The forced clean task rerun completed successfully in 1h16m49s (144/144 tasks executed). Direct SDK `dexdump` inspection proves `Lorg/lean4android/lsp/LeanLspRequests;` and its methods are present in the replacement APK. The audited replacement is 805,135,964 bytes with SHA-256 `104cfc93b9208012016979d1e0930bbd2b22c1836c6c173b74078673b46ef752`.
- Exact next physical step: update-install the audited replacement for owner user 0, cold-launch, repeat the theorem cursor action that crashed, and inspect goals/RPC output plus process/log state before proceeding to the remaining M4 matrix. No Gradle process remains.
- The dex-audited replacement fixed the cursor crash. Real API-33 acceptance then proved `Lean server: Ready`, exact-range live diagnostics during a temporary invalid edit, diagnostic clearing after restore/save, and a cursor-synchronized Markdown goal of `⊢ 0 < answer` on the theorem's `by`. Hover returned `VisualProbe.answer : Nat`; completion returned `answer` and other valid candidates; references returned both the declaration and theorem use.
- Device testing exposed and corrected two final integration defects. Standard Lean definition responses may use `LocationLink` (`targetUri`/`targetSelectionRange`) rather than `Location`; the parser now accepts both and a host test pins selection-range precedence. The editor/output splitter had inherited Goals-specific accessibility text; every splitter now receives its actual pane name. On the final APK, definition navigated to `VisualProbe/Basic.lean:3:5` and semantics read `Resize Output panel height` separately from `Resize Goals pane height/width`.
- Physical layout acceptance passed explicit Right side and Bottom, Auto Bottom at 720×1280, Auto Right at 1280×720, right-side drag from x=1031 to x=1168, persistence of that normalized size across a cold process recreation, and restoration to physical 1600×2560. Settings exposed exactly Auto/Right side/Bottom with the documented portrait/landscape explanation. Explicit Restart Lean server replaced process IDs `8606/8616/8621/8624` with `8883/8891/8896/8899`, returned Ready, and republished the current diagnostic.
- A temporary M3 export token found in the saved sample was removed through the visible editor and saved. The temporary proof-hole validation was fully undone one input event at a time, the original `by decide` bytes were saved, and the resulting diagnostics cleared. No temporary project/provider fixture was created.
- Measurement evidence: the final physical cold Activity launch completed in 1,236 ms; the polling-based acceptance observed server Ready within the subsequent 7-second bound and warm diagnostics/goals within the 4-second observation bound. With the two-module workspace fully elaborated, app TOTAL PSS/RSS was 129,252/191,832 KiB. The four LSP-side processes measured 14,297, 15,287, 431,293, and 440,228 KiB PSS respectively (aggregate 901,105 KiB); treat aggregate RSS/PSS cautiously because shared pages and concurrent file workers can inflate sums.
- Final validation passed. Focused app tests/compilation passed in 2m55s (106 tasks; 5 executed). Corrected app/instrumentation assembly passed in 3m22s (175 tasks; 4 executed); direct `dexdump` again proved `LeanLspRequests` in the APK. The required full `testDebugUnitTest :app:assembleDebug` regression passed in 3m35s (175 tasks, all up-to-date), and `git diff --check` passes.
- The final installed app APK is 805,700,918 bytes with SHA-256 `a767fab0a666034786897e20c66014be35ffcefb86a1a222f7b37a069305c1a6`; the installed instrumentation APK is 846,964 bytes with SHA-256 `7b7acbe9d2146eeb24b167a6b7a182f04052b3bc416ca05abc17df5d9d0cfca7`. After final force-stop, exact `pidof` checks found no `lean`, `lake`, `liblean_exe.so`, or `liblake_exe.so` child. Wi-Fi is enabled; physical 1600×2560, accelerometer rotation 1, and user rotation 0 are restored. M4 exit criteria are complete; M4.1 named-project creation is the active next milestone.

## 2026-08-14 — M4.1 named-project creation in progress

- Began immediately after the device-proven M4 boundary. Added a shared New Project dialog entry from both the Files menu and Open workspace; the Files menu keeps file-level **New** distinct and places **New Project** beside it. Dialog name/error state is saveable across Activity recreation.
- Project names trim outer whitespace and normalize internal whitespace to hyphens before the existing safe-ID validation. Repository creation now rejects case-folded project-directory collisions in addition to exact collisions and retains its atomic staging/move behavior. Failure returns inline without switching/saving away the current workspace; success registers and activates through the existing `switchProject` flow.
- Added repository tests for normalization, invalid paths, and case-folded collisions. Validation in progress: run focused repository/app tests and compilation, correct any state or API failure, then assemble/install and physically exercise both entry points, invalid/collision/cancel/recreation, first save/Run, cleanup, and exact no-orphan behavior.
- Focused `:core-project:testDebugUnitTest :app:testDebugUnitTest :app:compileDebugKotlin` passed in 2m56s (111 tasks; 10 executed). The first physical candidate exposed one recreation-order defect: creation succeeded, but Activity recreation captured the saveable dialog as still open. Posting `switchProject` until after the click handler closes successful dialog state corrected it; collision errors remain inline.
- Physical acceptance passed on the API-33 tablet. Files visibly listed distinct **New**, **New Project**, Open, Save, Save As, and Close rows. The dialog exposed Project name, Create project, Cancel, and naming guidance. `M41 Named Project` survived a compact-window configuration recreation, normalized to `M41-Named-Project`, generated the `M41NamedProject` namespace and both sources, reached Lean server Ready, and completed its first offline Run with exit 0 in 4,419 ms.
- Open workspace displayed the same New Project entry and shared dialog. Cancellation returned to the unchanged workspace. A repeated name produced `Project already exists: M41-Named-Project` inline. The final ordering-fix APK created `M41 Final` as `M41-Final`, closed the dialog before recreation, and opened the generated `M41Final` project with the Lean server Ready.
- Final M4.1 app test/compile/assembly passed in 5m12s (150 tasks; 7 executed); the required full `testDebugUnitTest :app:assembleDebug` regression then passed in 2m35s (175 tasks, all up-to-date). The final installed APK is 805,721,226 bytes with SHA-256 `41eeeeaa95eb0ffb5b042819d90721cec99dd8ddfd11226686ae28cca8ab9d80`.
- Returned to `visual_probe` and removed only the resolved `M41-Named-Project`/`M41-Final` project directories and recovery snapshots. Final app force-stop left no exact Lean/Lake child and no M41 fixture path. Physical 1600×2560, accelerometer rotation 1, user rotation 0, and enabled Wi-Fi are restored. M4.1 exit criteria are complete.

## 2026-08-14 — M4.2 expected types and richer editor inspection complete

- Added M4.2 after the device-proven M4.1 boundary. It covers the omitted `$/lean/plainTermGoal` expected-type request, independent Goals/Expected type fallback rendering, a native selection-context **Hover** action routed through the existing versioned hover path, and bounded Markdown rendering with syntax-highlighted Lean fences.
- The implementation must preserve standard Cut/Copy/Paste/Select all behavior, selection/source bytes, current LSP generation/version rejection, Auto/Right/Bottom panes, light/dark and adaptive layouts, and exact child cleanup. Markdown remains local data rendered by Compose: no WebView, remote fetch, raw HTML execution, or executable code path is authorized.
- Initial implementation checkpoint: add the typed term-goal constructor and independent response state/tests, then extend the Compose text-context menu and introduce a host-testable Markdown block/inline model that reuses `leanHighlightedText`. No Gradle, Lean, Lake, or device process was alive at that checkpoint.
- Implemented the first slice. `plainTermGoal` is a typed request and has independent per-file UI state; the Goals pane applies the required tactic-only/term-only/both/neither model. Inspection of the exact pinned Lean source (`Lean/Data/Lsp/Extra.lean`) confirmed the response uses `goal` plus `range`, unlike `plainGoal`'s `rendered` field, and the client parser follows that contract. Goal responses now reject stale cursor positions in addition to stale generations and document versions.
- Compose's native text-context-menu extension appends **Hover** without replacing platform editing components. Hover Markdown is parsed into a bounded local block/inline model, selectable in Compose, and `lean`/`lean4` fences reuse the existing source highlighter; URLs remain readable labels and no content is loaded. The initial focused run reached compilation and found only a missing annotated-string extension import plus receiver-lambda signature mismatch; both were corrected. The next focused run passed 111 tasks in 3m43s (6 executed, 105 up-to-date).
- After the pinned-response/cursor-gate correction, focused tests were rerun, response/render edge coverage was added, and the full host/APK plus physical API-33 tactic/term/context-menu/Markdown acceptance followed. No Gradle or native process remained from the completed focused build.
- The corrected focused regression passed 111 tasks in 4m24s (5 executed, 106 up-to-date). The first complete `testDebugUnitTest :app:assembleDebug` regression passed 175 tasks in 5m24s. Device inspection then exposed only presentation punctuation in fallback `plainGoal.rendered` Markdown; the client now uses Lean's structured `goals` array, and a regression test proves fence-free output.
- Physical API-33 acceptance passed on Samsung SM-T870 serial `R52R40K1PPN`. A normal term cursor produced only **Expected type** with local context and `⊢ Sort ?u.7`; selecting `answer` produced only **Expected type** `⊢ Nat`; the theorem's `by` produced only **Goals** `⊢ 0 < answer`; and an unrelated position produced **No goals**. The automated render-model test covers the otherwise source-dependent case where both responses exist and displays both distinct sections.
- Long-pressing `answer` visibly preserved Cut, Copy, and Select all and appended **Hover**. Activating it left the selection/source unchanged and displayed `VisualProbe.answer : Nat` in the Goals pane. Its Lean Markdown fence rendered as a separate monospaced highlighted code surface without backticks; the visible `Nat` token used Lean syntax coloring. The bounded parser also has host coverage for headings, paragraphs, lists, inline code/emphasis, readable link labels, Lean fences, and null responses.
- The final full regression passed `testDebugUnitTest :app:assembleDebug` in 5m04s (175 tasks; 8 executed, 167 up-to-date). The final installed APK is 805,747,666 bytes with SHA-256 `a16cab1b9427695d10375857d003b972d81914d79a09a07c327e85fb924d00a7`. The installed final fallback tactic goal was directly rechecked as `⊢ 0 < answer` with zero fence markers.
- Compact `720x1280` recreation retained a Ready server, reachable Goals pane, and truthful No goals fallback. The device was restored to physical 1600x2560, accelerometer rotation 1, user rotation 0, and connected Wi-Fi. Final owner-user-0 force-stop left exact `pidof` checks empty for `lean`, `lake`, `liblean_exe.so`, and `liblake_exe.so`; no Gradle process remains. M4.2 exit criteria are complete.

## 2026-08-14 — Removed duplicate editor/output server status

- Removed the redundant `Lean server: Ready` row between the editor and output panel. The Goals pane remains the single visible server-status surface, and its status, goals, expected-type, hover, and restart behavior is unchanged.
- The required full `testDebugUnitTest :app:assembleDebug` regression passed in 4m58s (175 tasks; 7 executed, 168 up-to-date). The resulting APK is 805,747,514 bytes with SHA-256 `c88e875afbc2191f3de5003e1f203e19299c93d566a66c2f41788416b04275c6`.
- The exact APK was update-installed for owner user 0 and cold-launched on Samsung SM-T870/API 33 serial `R52R40K1PPN`. The UI hierarchy contained exactly one `Lean server: Ready` node, directly beneath the Goals heading, and no duplicate at the editor/output boundary. Final owner-user-0 force-stop left exact `pidof` checks empty for `lean`, `lake`, `liblean_exe.so`, and `liblake_exe.so`; the tablet remained at physical 1600x2560, accelerometer rotation 1, user rotation 0, and connected Wi-Fi. No Gradle process remains.

## 2026-08-14 — M4.3 mobile editor navigation and symbol input in progress

- Added M4.3 as the required pre-M5 milestone for the reported fresh named-project diagnostic, vertically reachable drawer actions, a VS Code-style hierarchical project tree with extensible typed icons and independent two-axis scrolling, and an editor-bottom mobile symbol row that remains above the IME.
- Started from the device-proven M4.2 APK. The authorized Samsung SM-T870/API 33 serial `R52R40K1PPN` is currently online through the project-local ADB/libusb backend; no USB-node permission repair was required. No Gradle or native Lean/Lake process was alive when this work began.
- Validation in progress: reproduce the exact Files/Open workspace → New Project → Create Project flow on the installed APK, capture the generated project files and live LSP diagnostic, then correct the scaffolding with regression coverage before implementing the drawer/tree and symbol-row slices.
- Exact next step: exercise named creation on the tablet, inspect `lakefile.toml`, `Main.lean`, and the generated module path in app-private owner-user-0 storage where accessible, then record observed versus inferred root cause. No long build is currently running.
- Reproduction completed on the installed M4.2 APK. The existing user-created `Some-Project` opens `Main.lean` with the exact live diagnostic `unknown module prefix 'SomeProject'`; the server search path omits the project library output. App-private inspection proved the generated files agree on `SomeProject.Basic`, but `lakefile.toml` also uses the hyphenated storage/display ID as package `name`; Lake writes that manifest identity as `«Some-Project»`. The known-good underscore project does not cross this identifier boundary. This is observed device evidence; the corrective implementation will keep the safe app-managed ID while deriving a valid Lean/Lake package and module identifier.
- The reference tablet remains online and the app is currently open on the failing `Main.lean`; no project bytes, device settings, Wi-Fi state, or permissions were changed during reproduction. Exact next step: add a repository regression for hyphenated names, correct generated package identity, run focused project/app tests, then install and retest a newly created project before proceeding to UI slices.
- First implementation checkpoint complete. New scaffolds retain their normalized app-managed project ID but emit the derived safe Lean identifier for Lake package, library, module directory, import, and roots. Added a hyphenated-name regression. Added a host-testable hierarchical tree model with single-child chain compaction, typed folder/Lean-file/generic-file icon selection, and symbol insertion at forward or reverse selections.
- The drawer now reserves an independently horizontal/vertical scrolling tree region and an independently vertically scrolling action region, uses left-aligned indented folder/file rows, and keeps Settings below large trees. The editor now has a persistent horizontally scrolling symbol row immediately below the source surface, routed through the existing edit/undo/LSP cursor path. Focused Gradle validation is about to start with project and app unit tests plus Kotlin compilation; exact command uses the project-local Gradle state, no cache, no daemon, and console plain. No native Lean/Lake process is running.
- Focused validation passed: `:core-project:testDebugUnitTest :app:testDebugUnitTest :app:compileDebugKotlin` completed in 3m27s (111 tasks; 11 executed, 100 up-to-date). The first sandboxed attempt failed before Gradle startup only because its restricted namespace could not determine a wildcard socket address; the unrestricted retry compiled and passed all tests. No Gradle process remains from the successful run.
- APK/device validation is now in progress. Exact next step: assemble the app APK with no build cache, update-install it for owner user 0, create a fresh uniquely named hyphenated project through the real dialog, wait for the initialized server, and prove absence of the prior diagnostic plus successful offline Run. Then exercise tree/action horizontal and vertical scrolling, folder expansion, symbol insertion/selection/undo, Samsung keyboard layouts, recreation, and exact process cleanup. The previously installed M4.2 APK remains the trustworthy baseline until this replacement is assembled and accepted.
- The first exact APK assembled successfully in 3m44s (144 tasks; 6 executed, 138 up-to-date): 805,782,258 bytes, SHA-256 `353744a8997497db74b1ee32782380ade3ed9a04bb8a487fa3adf34e1c26d624`. It update-installed successfully for owner user 0.
- Physical defect regression passed for fresh `M43-Hyphen`: the real dialog generated Lake package/library/module identity `M43Hyphen`, reached `Lean server: Ready` with no `unknown module prefix`/Messages surface, and offline Run exited 0 in 2,615 ms after building `M43Hyphen.Basic` and `Main`. The installed drawer visibly showed compact `M43Hyphen/Basic.lean` plus `Main.lean`, left-aligned file icons, and every action through Settings; the horizontally scrolling symbol row was visible below the editor.
- User-requested name-style expansion is now in progress. Added a table-driven host regression for capitalized, snake_case, camelCase, TitleCase, space-normalized, and hyphenated IDs, asserting a single consistent Lake/library/import/module-directory identity. Exact next step: rerun the focused project tests, then create each style through the installed real dialog and inspect live diagnostics and generated identities before completing IME/tree stress acceptance.
- The rapid physical name matrix revealed the initial correction was incomplete: projects whose generated module path sorted before `Main.lean` appeared healthy only because `Basic.lean` was the active tab. Explicit `Main.lean` cases still showed the prefix error. Fresh Lean Server resolves imports from Lake's compiled output, so importing an as-yet-unbuilt sibling in the initial source is invalid until the first build. The scaffold now keeps the two-root library but makes initial `Main.lean` self-contained and immediately checkable; the first Run still builds both roots.
- Corrected the name-style test after review: its cases now distinguish raw user input, normalized app-managed ID, and generated Lean identity. In particular, raw `With spaces` must normalize to ID `With-spaces` and Lean identity `WithSpaces`; the test no longer starts from an already-normalized hyphenated surrogate.
- The corrected focused regression passed in 2m24s (111 tasks; 6 executed, 105 up-to-date), covering the self-contained fresh `Main.lean`, raw-name normalization matrix, tree/icon model, symbol insertion, all project tests, all app tests, and app Kotlin compilation. No Gradle process remains. Exact next step: assemble/update-install this corrected APK and repeat the real name matrix while explicitly activating `Main.lean` for every case; the earlier first APK is not accepted because its initial Main scaffold still required an unbuilt sibling.
- The corrected APK assembled in 3m02s (144 tasks; 3 executed, 141 up-to-date): 805,782,258 bytes, SHA-256 `7947d55c22000423d7f989b1aa306a5790e3efb1cca4ec4c74351e5d48dee9f0`, and update-installed successfully for owner user 0. The explicit active-`Main.lean` matrix passed for fresh `CapitalizedCheck`, `snake_case_check`, `camelCaseCheck`, `TitleCaseCheck`, raw `With spaces check` normalized to `With-spaces-check`, and `with-hyphens-check`: every case reached Ready with no prefix/error diagnostic. The visible Messages item was only deliberate `#check Nat` information (`Nat : Type`).
- Large-tree physical stress then exposed a separate scalability defect: without a valid recovery snapshot, `EditorSessionStore` opened every repository source as a tab and synchronized it to Lean Server. A temporary 30-sibling/deep fixture, and then a reduced 15-sibling fixture under current memory pressure, caused Samsung's low-memory service to kill the top app after roughly 6–7 seconds; there was no Java/native crash buffer entry and exact app/Lean/Lake PIDs were absent afterward. This is device evidence, not a UI success.
- Initial session creation now opens `Main.lean` plus at most one companion source while the independent drawer tree continues to discover every repository file on demand. This preserves the established visible two-tab generated-project baseline and bounds initial LSP memory independently of tree size. Added a 32-source host regression for the two-tab selection and full repository discovery. Exact next step: rerun focused app tests/compile, assemble/install, and retry the retained 15-sibling/deep tree fixture before symbol/IME acceptance.
- Bounded-tab focused validation passed in 2m31s (106 tasks; 5 executed, 101 up-to-date). The APK assembled in 3m12s (144 tasks; 3 executed, 141 up-to-date): 805,783,426 bytes, SHA-256 `ab056d44898f8c19c6e7d1affca823eb4fe5c6ad03b45d19ce11351676ec9d9f`, and update-installed successfully.
- The retained 15-sibling plus deep-chain project stayed alive beyond 10 seconds and reached the drawer. The compact row visibly represented `Deep/Nested/Folder/Structure/That/Is/Intentionally/Very/Long/DeepFile.lean`; a vertical tree swipe changed visible siblings from 01–05 to 05–11 while Build and Settings retained identical bounds, and a horizontal swipe shifted the deep label left while those actions again stayed fixed. This physically proves independent tree scrolling and action reachability. The initial 30-file kill is corrected by bounded initial tabs rather than hidden by deleting the finding.
- Samsung HoneyBoard is the active IME. Its original `show_ime_with_hard_keyboard` value was 0; it was temporarily set to 1 for validation and must be restored. Floating keyboard mode left the symbol row visibly available below the source surface, though the floating window naturally overlapped part of the workspace. Switching HoneyBoard to its docked split mode exposed a defect: the split keyboard obscured the symbol row because the Compose workspace did not consume IME insets. Added `imePadding()` at the workspace boundary; exact next step is focused compile, assemble/install, and repeat floating/split/standard keyboard screenshots plus actual symbol insert/undo before restoring the IME setting and removing temporary project fixtures.
- App tests plus APK assembly after `imePadding()` passed in 4m13s (150 tasks; 7 executed, 143 up-to-date). The candidate APK is 805,783,578 bytes with SHA-256 `92afd538324f9af1a10250d82353c08ee4e1fdd98d864c7368ccf8840b7408cb` and update-installed successfully. Repeating Samsung split mode showed the activity still retained its full height and HoneyBoard overlaid the row; the IME inset remained ineffective because the Activity had no explicit resize policy. Added `android:windowSoftInputMode="adjustResize"` to the sole editor Activity while retaining workspace `imePadding()`. Exact next step: rebuild/install and repeat the identical split screenshot before accepting or revising the approach.
- The explicit resize APK assembled in 3m03s (144 tasks; 5 executed, 139 up-to-date): 805,783,578 bytes, SHA-256 `ccc3ab7f6615461b229c070fba55a758e88a304d48dbcb255c6fa3e7f3e92851`, and update-installed successfully. Samsung split mode now physically resizes the source from bottom 642 to 441 and places the symbol row at 458–560 immediately above HoneyBoard. Floating mode was also rechecked, then restored as the tablet's original keyboard style.
- Real symbol editing passed in split mode: tapping `{` inserted it at the active caret through the editor, marked the buffer dirty, and hardware Ctrl-Z removed it byte-for-byte; the test change was never saved. The row remained horizontally scrollable and exposed later Unicode symbols. The original `show_ime_with_hard_keyboard=0` value was restored, and HoneyBoard was no longer shown with the connected hardware keyboard.
- Removed only the 13 projects and matching recovery snapshots created for the name/style/tree tests, including the temporary 15 siblings and deep chain. A subsequent 790 ms cold launch self-healed the stale active-project preference to `visual_probe`, reached Lean server Ready, and showed the symbol row with the IME hidden. The clean baseline Run exited 0 in 3,126 ms with expected theorem, values 42 and 5, and successful four-job build. Final force-stop left exact `pidof` results empty for `lean`, `lake`, `liblean_exe.so`, and `liblake_exe.so`.
- Final repository regression is now in progress: run `testDebugUnitTest :app:assembleDebug` with project-local Gradle state, no build cache, no daemon, and plain console; then audit diff/status, finalize M4.3 plan/rebuild documentation, and record the final artifact hash. No Gradle or native process is currently alive.
- Final `testDebugUnitTest :app:assembleDebug` passed in 2m40s (175 tasks, all up-to-date after the preceding validated assemblies). The final APK is 805,783,578 bytes with SHA-256 `ccc3ab7f6615461b229c070fba55a758e88a304d48dbcb255c6fa3e7f3e92851`; this exact artifact is installed for owner user 0 and supplied the final device evidence.
- Final audit passed `git diff --check`; the diff contains only the M4.3 implementation, tests, manifest IME policy, bounded initial-session behavior, and corresponding plan/history/rebuild/README updates. The tablet remains at physical 1600x2560, accelerometer rotation 1, user rotation 0, connected Wi-Fi, original floating HoneyBoard mode, and original `show_ime_with_hard_keyboard=0`. The app is force-stopped and no Gradle, Lean, or Lake process remains.
- M4.3 exit criteria are complete. The exact next product step is M5 Mathlib beta: produce and verify a version-matched offline pack, then measure capacity, install/remove recovery, import/goal latency, memory, and thermal behavior without regressing the completed editor lifecycle and UI baselines.

## 2026-08-14 — M4.4 collapsible panes and compact-landscape reachability complete

- Added M4.4 as the final user-requested pre-M5 usability boundary. It covers direct collapse/restore affordances for Goals, Messages, and Output; temporary Output suppression only for docked/non-floating IMEs; and a practical expanded-tree minimum height when landscape height is less than half the width.
- Started from the exact device-proven M4.3 APK and preserved the existing dirty worktree as the active uncommitted M4.3 implementation. No Gradle, app, Lean, or Lake process is assumed alive. The tablet's recorded trustworthy state remains physical 1600×2560, rotation settings restored, connected Wi-Fi, floating HoneyBoard mode, and `show_ime_with_hard_keyboard=0`.
- Implementation checkpoint: keep pane size fractions unchanged while adding saveable collapsed state; attach Goals/Output controls to orientation-aware splitters and Messages control to its header; detect a docked IME by a nonzero IME bottom inset and suppress only visible Output. In extreme landscape, allocate an expanded tree minimum without removing independent action scrolling.
- Exact next step: implement small host-testable visibility/layout helpers and Compose state/controls, then run focused app tests and Kotlin compilation before assembling or mutating the device. No long command is currently running.
- First implementation slice complete. Goals and Output splitters now retain resizing while carrying orientation-aware collapse/restore controls; collapsed Goals moves the same control between Right and Bottom placement without changing either stored fraction. Messages has a counted header control and retains diagnostics while collapsed. All three states are saveable across Activity recreation.
- A nonzero IME bottom inset now temporarily removes both Output and its splitter from the resized workspace without changing the user's Output collapsed state or saved fraction; dismissing the docked IME restores that prior state. Floating IMEs continue to report no bottom inset and therefore preserve Android overlay behavior.
- The expanded tree now receives an explicit 120 dp height in windows whose height is strictly less than half their width; the action region takes the remaining height and remains independently vertically scrollable. Normal landscape/portrait retains the existing weighted tree and bounded action region. Added host tests for pane/IME visibility and the exact extreme-landscape threshold.
- Focused validation is about to run: `:app:testDebugUnitTest :app:compileDebugKotlin` with project-local Gradle state, no cache, no daemon, and plain console. No Gradle or native process is running at this checkpoint.
### 2026-08-14 — M4.4 focused validation checkpoint

- The first focused `:app:testDebugUnitTest :app:compileDebugKotlin` pass reached Kotlin compilation and exposed only Compose API/scope errors: an invalid explicit `getBottom` import and nested layout lambdas shadowing the outer `BoxWithConstraints` dimensions.
- Removed the import (the inset method is available directly) and captured the workspace width/height before entering the drawer column. No behavior or persisted pane state changed.
- No Gradle process remains from the failed pass. Exact next step: rerun the same focused Gradle command, then assemble and perform physical-device keyboard/pane/compact-landscape validation.
- The corrected focused pass succeeded in 3m07s (106 tasks; 5 executed, 101 up-to-date), including all app unit tests and debug Kotlin compilation. No Gradle process remains. Exact next step: assemble the candidate APK, record its identity, update-install it for owner user 0, and physically verify pane persistence, docked/floating IME behavior, and the extreme-landscape tree floor.
- The candidate APK assembled in 3m19s (144 tasks; 3 executed, 141 up-to-date): 805,807,402 bytes, SHA-256 `2a27aaecfda89704a1af0942b6c9c9799e1eca6a3ef3d99628ecae97d32301d4`, and update-installed successfully for owner user 0.
- Physical pane acceptance passed. Goals, Messages, and Output each collapsed independently and retained a direct restore control. Rotation recreation preserved all three collapsed states; Goals moved from a right-edge `Restore Goals pane` control in landscape to a bottom `▲` control in portrait, then restored normally. Stored size fractions were never mutated by collapse/restore.
- Floating HoneyBoard left the full Activity height and kept symbol row, expanded Messages, Output, and Goals present, preserving overlay behavior. The already-installed docked MathKeyboard provided a deterministic non-floating inset check: Android reported the IME visible and resized content to y=598; the symbol row and expanded `Messages (3)` remained present while both Output and its splitter were absent. HoneyBoard and `show_ime_with_hard_keyboard=0` were restored afterward.
- A temporary rotated 1280×600 window (strictly under the 2:1 threshold after system bars) visibly retained the expanded Project tree's 120 dp allocation with multiple file rows, while the separate action region remained scrollable. The display was reset to physical 1600×2560 with accelerometer rotation 1 and user rotation 0.
- A coordinate probe inserted one unsaved symbol and then deleted an adjacent character. Two Ctrl-Z operations visually restored the active buffer and the on-disk `visual_probe/Main.lean` remained byte-for-byte unchanged, but the later baseline Run proved the recovery snapshot still retained the symbol and correctly failed at line 8. The symbol was then deleted at its exact line-8 location, the full UI text was verified against disk, and the repeat Run exited 0 in 2,718 ms with values 42 and 5 and a successful four-job build. This recovery-only test artifact is no longer present.
- Final `testDebugUnitTest :app:assembleDebug` passed in 2m40s (175 tasks, all up-to-date). Final diff/status and artifact/device-state audits are in progress; no Gradle process remains.
- Final `git diff --check` passed. The final APK remains 805,807,402 bytes with SHA-256 `2a27aaecfda89704a1af0942b6c9c9799e1eca6a3ef3d99628ecae97d32301d4`; that exact artifact is installed for owner user 0 and supplied the physical evidence. Existing uncommitted M4.3 changes and its new tree files remain intentionally preserved alongside M4.4.
- Final tablet state is physical 1600×2560, accelerometer rotation 1, user rotation 0, HoneyBoard selected, and `show_ime_with_hard_keyboard=0`. The app is force-stopped and exact app/Lean/Lake `pidof` results are empty. M4.4 exit criteria are complete; the next product step is M5 Mathlib beta.

## 2026-08-14 — Pre-M5 UI polish in progress

- Began from the device-proven M4.4 baseline to address four reported presentation/state defects: oversized pane boundaries and symbol-row padding, lost drawer/tree expansion state across orientation recreation, excess drawer spacing below a collapsed tree, and unconditional error coloring/height in Messages.
- Existing uncommitted M4.3/M4.4 implementation and documentation are the preserved baseline. No Gradle, app, Lean, or Lake process was started by this task yet.
- Exact next step: make drawer and expanded-folder state saveable, make the collapsed-tree layout consume only its content height, introduce severity-aware Messages styling, and reduce splitter/collapsed-header/symbol control dimensions with host coverage before focused app tests and APK assembly.
- First implementation slice complete. Drawer visibility, Project expansion, and the expanded-folder list now use saveable state; closing Project no longer inserts a weighted spacer ahead of Build project. Diagnostic severity 1 alone selects the error container/content colors, while warnings, information, hints, and absent severity remain neutral. Splitters are reduced from 28 dp to 16 dp with the control visually encased at the boundary, the collapsed Messages toggle no longer imposes its former large header padding, and symbol buttons use a 34 dp high/36 dp minimum-width compact treatment.
- Focused validation is starting with `:app:testDebugUnitTest :app:compileDebugKotlin` using project-local Gradle state, no cache, no daemon, and plain console. No native Lean/Lake process is running.
- The first sandboxed focused attempt failed before Gradle startup because the restricted namespace could not determine a wildcard IP for Gradle's local lock socket. The unrestricted retry passed in 3m38s (106 tasks; 5 executed, 101 up-to-date), covering all app unit tests and debug Kotlin compilation. No Gradle process remains.
- Exact next step: run the required full `testDebugUnitTest :app:assembleDebug` regression, audit the diff/artifact, then update-install and physically verify thinner boundaries/symbol row, drawer/tree orientation persistence and collapsed spacing, and neutral-versus-error Messages treatment if the API-33 reference tablet remains available.
- The required full `testDebugUnitTest :app:assembleDebug` regression passed in 3m23s (175 tasks; 3 executed, 172 up-to-date). `git diff --check` passed. The resulting APK is 805,812,666 bytes with SHA-256 `f21735ba64fc1b81a3e2211e795105cf8d9b55db5301b3ece4b661522b474034` and update-installed successfully for owner user 0.
- Physical API-33 acceptance passed on Samsung SM-T870 serial `R52R40K1PPN`. The installed symbol row is visibly tighter; Output and Goals boundaries measure 16 dp with their circular controls attached. Closing Project placed Build project immediately below it (Project row y=214–316, Build row y=333–435), and both the open drawer and closed Project state survived a configuration recreation. Collapsed Messages measured 32 dp high while retaining its restore control.
- The active file's real `Unknown identifier` diagnostic kept Messages in the error container as required. Host coverage proves empty, absent, warning, information, and hint severities select the neutral branch and severity 1 selects the error branch; no source or recovery bytes were changed to manufacture a device-only severity case.
- Final device state is restored to physical 1600×2560, accelerometer rotation 1, and user rotation 0. Messages was restored before the app was force-stopped. Exact `pidof` checks were empty for `lean`, `lake`, `liblean_exe.so`, and `liblake_exe.so`; no Gradle process remains. Pre-M5 UI polish is complete and M5 remains next.

## 2026-08-14 — M4.6 project and tree lifecycle actions in progress

- Began the final requested pre-M5 boundary from the device-proven M4.5 APK. Scope is guarded rename/delete for projects, files, and folders, exposed through drawer project actions, long-press workspace projects, long-press file tabs, and long-press project-tree entries.
- Existing uncommitted M4.3–M4.5 implementation and documentation remain the preserved baseline and will be included in the user-requested final commit only after proportional host/APK/device validation. No Gradle, app, Lean, or Lake process is currently assumed alive.
- Exact next step: audit the existing repository source rename/delete semantics and session/LSP switching boundaries, add atomic folder/project lifecycle operations and regressions, then build shared Compose action/confirmation flows without permitting traversal, silent dirty-buffer loss, last-source deletion, or orphan processes.
- First implementation slice complete. `LeanProjectRepository` now atomically renames project roots and file/folder entries, recursively deletes only a resolved contained entry without following links, rejects case-folded collisions/self-nesting/traversal/extension loss, and preserves at least one Lean source. Folder operations remap or close every affected open tab while preserving dirty contents until an explicit delete confirmation.
- UI wiring now exposes exact **Delete Project** and **Rename Project** drawer actions below Export project. Long-press project rows in Open workspace, file tabs, and project-tree rows expose anchored Rename/Delete menus. Shared dialogs keep validation failures inline; destructive dialogs name the target and warn when affected tabs are dirty. Active project lifecycle operations stop its LSP session, migrate or clear recovery state, update recent/active preferences, and recreate onto a valid project.
- Focused validation is starting with core-project/app unit tests and app Kotlin compilation using project-local Gradle state, no cache, no daemon, and plain console. No device mutation or native Lean/Lake process has begun in this milestone.
- Focused validation passed in 3m08s (111 tasks; 10 executed, 101 up-to-date), covering repository project/folder/file lifecycle cases, session prefix remapping/removal with dirty buffers, all app tests, and debug Kotlin compilation. Review then corrected recent-project migration to capture the list before renaming invalidates the old ID, and structural entry actions now post a recreation after their updated session snapshot is published so the complete tree and LSP document set refresh deterministically.
- Exact next step: assemble the candidate APK, update-install it for owner user 0, create an isolated lifecycle fixture through the real dialog, and physically exercise all four entry surfaces, validation/cancellation/dirty confirmation, folder/tab remapping, active project rename/delete recovery, baseline build/run, and exact cleanup/no-orphan behavior.
- The first candidate assembled in 4m06s (144 tasks; 6 executed, 138 up-to-date): 805,926,114 bytes, SHA-256 `bfa1579679f0bef55ac44212fcc365bee686731bb9cb82a3c52acb95df9df5d7`, and update-installed successfully. Physical UI inspection proved the exact drawer ordering and Rename/Delete long-press popups on tabs, tree entries, and Open workspace projects. Renaming fixture `Main.lean` to `Renamed.lean` preserved its tab/content across recreation.
- Active fixture rename from `M46-Lifecycle7` to `M46-Renamed` exposed a durability defect under current device memory pressure: the project directory and renamed recovery snapshot were correct, but asynchronous preference `apply()` did not reach disk before the process was reclaimed during recreation. Cold launch therefore fell back safely to `visual_probe`; baseline project bytes were untouched. Project rename/delete transitions now use checked synchronous preference commits before recreation. Exact next step is focused revalidation, replacement APK assembly, and repetition of active rename/delete with the retained isolated `M46-Renamed` fixture.
- The synchronous-commit replacement passed focused app tests/compile in 2m50s, assembled in 3m14s (144 tasks; 3 executed), and update-installed. Active rename then durably migrated the fixture to `M46-FinalM46-Renamed`: project directory, recovery, active preference, recent preference, cold editor tabs, and Ready LSP all agreed. Active delete durably removed the fixture and its recovery and selected `Some-Project`, but exposed a distinct Compose transition race before recreation.
- Exact crash evidence: the old composition recomposed after the destructive callback returned but before posted `Activity.recreate()`, and the setContent argument expression attempted `repository.open(activeProjectId)` while the in-memory field still named the removed project. The same race explains the transient launcher after rename. The transition now replaces `activeProjectId`, `editorStore`, and `editorState` with the already persisted renamed/fallback state before returning to Compose; `onStop` can therefore no longer recreate an obsolete recovery snapshot. Exact next step: compile/assemble/install this race correction and repeat active rename/delete on a fresh isolated fixture.
- The race-corrected app test/assembly pass succeeded in 4m19s (150 tasks; 7 executed, 143 up-to-date) and update-installed. A fresh `M46Race` fixture renamed in place to `M46RacRenamede`; the immediate post-callback hierarchy remained in the app with Main/Basic tabs, the renamed title, and Lean server Ready, and recent logs contained no new fatal exception. Active deletion then immediately opened valid fallback `Some-Project` with Main/Basic and Ready, removed the fixture directory and recovery snapshot, and again produced no recurrence of `Project does not exist`.
- Returned through Open workspace to `visual_probe`. Its full offline Run exited 0 in 1,916 ms, built four jobs, and produced the expected theorem plus values 42 and 5. All M46 lifecycle fixtures and recovery snapshots are absent. Exact next step: run the required full `testDebugUnitTest :app:assembleDebug` regression, audit diff/status/artifact/device state, finalize documentation, and create the user-requested git commit.
- The required final `testDebugUnitTest :app:assembleDebug` regression passed in 2m45s (175 tasks, all up-to-date after the immediately preceding app test/assembly). `git diff --check` passed. The final APK is 805,926,958 bytes with SHA-256 `f7ea520897c0ebe2a216d5da26d88ddb18b58e7923c921f25dc7f7052636a4df`; that exact race-corrected artifact is installed for owner user 0 and supplied the final physical evidence.
- Final tablet state is physical 1600×2560, accelerometer rotation 1, user rotation 0, and the app is force-stopped on the restored `visual_probe` baseline. Exact `pidof` checks are empty for `lean`, `lake`, `liblean_exe.so`, and `liblake_exe.so`; no M46 project or recovery path and no Gradle process remains. M4.6 exit criteria are complete; documentation is current and the full preserved M4.3–M4.6 worktree is ready for the requested commit.

## 2026-08-14 — Post-M4.6 roadmap placement

- Kept M5 Mathlib beta as the immediate next feasibility gate so its distribution, memory, latency, and thermal evidence is not diluted by another editor expansion.
- Inserted three pre-beta increments after M5: M5.1 general project text files plus explicit stdin/stdout/stderr workflows, M5.2 direct definition/reference navigation, and M5.3 symbol-row/font preferences. Existing M6 remains the feature-frozen hardening and beta milestone.
- Recorded that a child blocking on stdin exposes no reliable structured prompt/request event: interactive runs therefore require an explicit open input channel with send/EOF/cancel controls, alongside immediate-EOF and project-file modes. Programs receive the contained project root as deterministic working directory; plain text is the fallback for a future extension/content-type highlighter registry.
- Placed complex Lean-style Unicode abbreviation completion, word wrap, and additional syntax grammars in post-beta M7. Updated the plan's tests, risks, immediate actions, and official Lean `cat` example reference, plus the README and rebuilding boundary summary.
- Documentation-only change: no Kotlin, build configuration, runtime artifact, app/device state, or process was changed; no build or device validation was required. Exact next step remains beginning M5 from the committed M4.6 baseline.

## 2026-08-14 — M5 Mathlib integration planning

- Added `docs/delivery/M5_MATHLIB_INTEGRATION_PLAN.md` as the high-level execution plan for the next milestone. It keeps Mathlib as an optional, immutable, signed data/library pack matched to the pinned Lean toolchain, with no on-device Git/network resolution or downloaded executable content.
- Defined the sequence from pins/inventory through complete-facet Android conformance, signed dual-channel packaging, streaming recoverable activation, typed project selection, and physical size/performance validation. Passing `import Mathlib` alone is explicitly insufficient; the gate includes Lake, LSP, navigation, tactics, supported interpretation, lifecycle recovery, and no-orphan evidence.
- Documentation-only work: no source code, build/runtime artifact, app/device state, or process changed, and no build was run. Exact next step is M5 Phase A: select and record the version-compatible Mathlib revision, then produce the unfiltered candidate inventory and provenance report.
- Expanded the plan's feasibility model to make all storage and memory concerns explicit: Core/Std plus active/staging/rollback/cache accounting, Lean import-reachability and broad `import Mathlib` hypotheses, separate app/LSP/job measurements, long-session growth, lazy editor source access, bounded state, optional LSP/job serialization, and measured minimum/recommended storage/RAM support policy. These remain experiment gates rather than unverified Android claims.

## 2026-08-14 — M5 Mathlib Phase A in progress

- Began M5 from committed M4.6 baseline commit `179d415`; the worktree was clean and no Gradle, app, Lean, Lake, or producer process was assumed alive.
- Active scope is Phase A: identify the exact Mathlib revision compatible with pinned Lean 4.32.1, record immutable upstream identities and licenses, obtain the complete dependency closure in an isolated producer tree, and generate the unfiltered inventory/provenance report before attempting any facet filtering.
- Nothing downloaded by this producer experiment is authorized as an Android runtime pack yet. Exact next step: audit the repository's toolchain pins and producer conventions, verify Mathlib's official Lean-version metadata from primary upstream sources, then checkpoint the selected revision and reproducible fetch/build command here before starting a long build.
- Selected official Mathlib tag `v4.32.1` at commit `520045ab14e26149ee970e2e617ca04b09bde5d6`; its `lean-toolchain` exactly selects `leanprover/lean4:v4.32.1`. Materialized all eight `lake-manifest.json` dependency repositories at their locked commits without running `lake update` or changing upstream metadata.
- Added checked-in Phase A pins plus exact-source fetch and no-follow inventory tooling. The first source inventory recorded 9,778 files, 113,350,974 expanded bytes, 9,451 Lean source files, nine license files, and three upstream symlinks; links are recorded evidence and remain forbidden as pack entries. Inventory JSON is at ignored path `toolchain/output/mathlib-4.32.1-source-inventory.json`.
- Validation in progress: download and SHA-256 verify the official 559,515,529-byte Lean 4.32.1 Linux producer archive (`57d5c062a6b4bae6fba511a1704aa124dff461c37d0fc94585637fbb7d951b50`), use it to materialize Mathlib's pinned cache/build facets, and rerun the unfiltered inventory. No Android/Gradle/native child process is active; only the explicit producer download/build command may remain alive.
- Installed the missing host `zstd` package, verified and extracted the official producer toolchain, and relocated the active producer copy to `/tmp/lean4android-m5-mathlib` after the cache's whole-tree hashing proved disproportionately slow on `/mnt/d`. The original ignored workspace remains at `toolchain/work/mathlib-producer`; the complete ignored inventories remain under `toolchain/output/`.
- Mathlib's official cache supplied 8,639 `.ltar` objects totaling 439,158,169 bytes. The unfiltered extracted source/dependency/build tree (Git metadata excluded, host cache utility deliberately still counted) is 122,207 records and 6,955,114,198 bytes: 1,882,088,688 `.olean`, 3,685,716,976 `.olean.private`, 286,315,370 `.ilean`, 103,350,296 `.olean.server`, 274,036,040 `.ir`, 486,266,715 generated C, and 101,874,110 Lean source bytes. The report and license audit are in `docs/delivery/M5_PHASE_A_BASELINE.md`.
- Host completeness validation passed: pinned `lake build Mathlib` reported 8,654 jobs satisfied, the targeted algebra/analysis/tactic corpus checked and evaluated `[2, 4]`, and broad `import Mathlib` checked with `omega`. Added those conformance sources. The first focused dependency-manifest/parser/verifier tests passed in 1m46s; the contract rejects traversal, case collisions, links, undeclared bytes, corruption, executable magic, mismatched totals, and unbound licenses.
- Physical Phase B probe is now in progress. The authorized API-33 SM-T870 has 37,508,020 KiB free under `/data`, enough for one 6,835,691,747-byte completeness-first data tree after excluding the known x86-64 cache executable and three symlinks, but not evidence that staging plus rollback is viable. Exact next step: while the app is stopped, stream the explicit inventory allowlist into `no_backup/mathlib-packs/mathlib-4.32.1-complete-probe`, verify device byte/file counts, then run direct Android Lake/Lean conformance without changing the active Core/Std installation. The stream command and its process state must be recorded if interrupted.

## 2026-08-15 — M5 Android artifact compatibility investigation in progress

- The completeness-first stream completed without an intermediate device archive: exactly 122,203 regular files were installed under owner-user-0 `no_backup/mathlib-packs/mathlib-4.32.1-complete-probe`; `du` reports 7,056,580 KiB allocated, the x86-64 cache executable is absent, no symlink is present, and `/data` retains 29,957,780 KiB free. Existing Core/Std and projects were not overwritten.
- Added an isolated `M5MathlibProbe` project plus an explicit all-path Lake manifest for Mathlib and its eight dependencies. The first run proved why typed offline manifests are required: without it Lake truthfully attempted Git and failed because no `git` executable exists. With it, Android Lake resolved 2,997/2,998 targets entirely locally and invoked Lean on `Main.lean`.
- The first imported official-cache artifact failed with `incompatible header`. Direct byte audit shows official Mathlib/Lean artifacts identify olean version `4.32.1` and commit `f054605…`, while the accepted Android Core/Std artifacts identify `4.32.0-pre` with no release commit because `build-android.sh` intentionally uses `work/build/host/stage0/bin/lean` as its previous-stage generator. The Android executable reports 4.32.1, but compiled-facet compatibility follows the generator header. Do not patch or relabel the official cache.
- A separate lifecycle observation preceded the probe: legacy `files/lake` still pointed at a prior randomized APK native directory while PackageManager and the current toolchain link named the installed directory. The probe resolved the current native directory read-only; it did not persist it. Production startup refresh behavior must be rechecked after the M5 artifact experiment.
- Validation in progress: create a clean `/tmp/lean4android-mathlib-android` source/dependency tree at the recorded pins, build Mathlib facets with the same `toolchain/work/build/host/stage0` generator used by the Android Core/Std build, and test one resulting targeted artifact on the tablet before committing to the full 8,654-module build. No Mathlib/Lean/Lake process remains alive on the device after the failed check; the app remains force-stopped and the isolated probe data remains installed.
- The clean target tree was created and a small host generator probe produced an olean header byte-compatible with current Android Core/Std. A targeted 191-job dependency build then proved the bootstrap compiler is insufficient: after dozens of successful outputs it deterministically aborted in `Batteries.Tactic.Lint.Simp` and `Qq.Macro` at `stage0/src/library/ir_interpreter.cpp:928` (`fn_body_tag(...) != Unreachable`). The invalid build was stopped; no producer or device child remains.
- This moves the correction boundary below Mathlib: build a true host stage1 release compiler, regenerate Core/Std with that compiler into a new Android target/output tree, and build Mathlib with the same artifact ABI. Preserve the current `lean-4.32.1-android1` distribution and installed baseline until the replacement passes all M0–M4 conformance. Do not change the active toolchain ID or manifest constants prematurely.
- Validation in progress: remove only the temporary `host/stage1/lib/lean` compatibility symlink created for the failed experiment, then run `cmake --build toolchain/work/build/host --target stage1 --parallel 4`. Command uses four jobs, writes only the previously unbuilt host stage1 tree, and leaves the audited Android output untouched. After completion, require `stage1/bin/lean --version` to report exact 4.32.1 and compile a targeted Mathlib slice before designing the isolated Android-v2 cross build.
- The system CMake 3.22.1 could build/reuse host stage0 but could not configure Lean stage1 because upstream now uses `block()`. Pinned and SHA-256 verified standalone Kitware CMake 3.31.10 (`3cb3dd247b6a1de2d0f4b20c6fd4326c9024e894cebc9dc8699758887e566ca7`). The first four-job stage1 build generated 2,247 olean files and reached the final Grind tactic wrappers without an assertion or other compiler failure, but the host crashed before it linked `stage1/bin/lean`; no producer process survived and that binary is absent, so the build is not yet complete or trustworthy. Restored the verified CMake under durable ignored path `toolchain/work/mathlib-producer/producer-tools/cmake-3.31.10-linux-x86_64` and refreshed only the existing stage1 metadata successfully, preserving completed outputs and exact commit `f054605aea4b840552cca2e725580bffd1e1b704`. Exact resume command: `toolchain/work/mathlib-producer/producer-tools/cmake-3.31.10-linux-x86_64/bin/cmake --build toolchain/work/build/host/stage1 --parallel 4`; after exit, require the version/header checks above. No relevant producer or device child is currently alive; the accepted Android distribution remains untouched.
- Crash recovery resumed that exact command with four jobs in unified session 60034. C/C++ libraries were reused and relinked successfully; Lean generation resumed at the final `Lean.Meta.Tactic` and elaborator Grind/VCGen wrapper modules rather than rebuilding completed artifacts. The session is currently alive, the accepted Android distribution remains untouched, and no device child is active. Exact recovery step: poll session 60034 without interrupting it; if the process did not survive, rerun the same incremental command and then perform the required version/header checks after a clean exit.
- The recovered stage1 build exited 0 after linking Lean, Lake, leanc, leanir, and leanchecker. `stage1/bin/lean --version` reports exact Lean 4.32.1 release commit `f054605aea4b840552cca2e725580bffd1e1b704`; Lake reports `5.0.0-src+f054605` with Lean 4.32.1. SHA-256 is `2f95bc69f39c0d11e2a232d3ecdf567df6e9d7a860b72cafff01e9639e097c3a` for Lean and `eb7672ec51de88aa72db79c7aa4d0de1fd5d197d71a54f98f0c65cdcce2a94a1` for Lake.
- Bootstrap distinction is now directly proven: the stage1 tree's boot `Init.olean` remains `4.32.0-pre`, as expected because stage0 generated the compiler's input library, but the completed stage1 executable compiled `toolchain/conformance/Valid.lean` successfully and emitted `/tmp/lean4android-m5-stage1-probe.olean` with exact olean version `4.32.1`, full commit `f054605aea4b840552cca2e725580bffd1e1b704`, size 7,992 bytes, and SHA-256 `c1612a21dabb9998d0e0c0e566581ce0b4c0775dab92aab5c02b06fd26274577`. The release producer boundary is therefore validated. Exact next step: create a fresh pinned Mathlib/dependency tree with no stage0 outputs, use this stage1 Lean/Lake pair to build a representative Mathlib slice, and inspect its emitted header before designing the isolated Android-v2 Core/Std cross build.
- Created genuinely fresh exact-pinned sources at `/tmp/lean4android-mathlib-release/mathlib` (Mathlib plus all eight locked dependency commits) with no official cache or prior build outputs. Validation in progress: `LEAN_NUM_THREADS=4 stage1/bin/lake --no-cache build Mathlib.Data.Nat.Prime.Basic` is alive in unified session 25844 and has locally generated more than 160 modules. It has already passed both exact stage0 assertion sites, `Batteries.Tactic.Lint.Simp` and `Qq.Macro`, with no IR interpreter or header failure. No cache, dependency update, or network resolution is used in this build. Exact recovery step: poll session 25844; if it did not survive, rerun the same local-only command in that tree, then inspect the target olean header after a clean exit.
- The clean local-only representative build exited 0: `Mathlib.Data.Nat.Prime.Basic` completed successfully with 605 jobs and 591 newly generated olean files. Both former stage0 crash modules passed. The target contains `.olean`, `.ilean`, `.olean.private`, `.olean.server`, `.ir`, and trace facets; its 54,168-byte olean has exact header `4.32.1` plus full commit `f054605aea4b840552cca2e725580bffd1e1b704` and SHA-256 `c900db3d2609608d2cfa8519d6eccaa9206b15b7edb26006fc466f5fd830d330`. This proves the release stage1 producer can generate the Mathlib artifact ABI required by the official release while using no cache, dependency update, or network resolution. No producer process remains. Exact next step: add and audit an isolated Android release-ABI build target using host stage1 as `PREV_STAGE`, separate from `work/build/android-arm64` and `lean-4.32.1-android1`, then build and inspect its Core/Std artifacts before any promotion.
- Added opt-in, validated build overrides for the CMake executable, host target, host producer stage, Android build variant, and distribution ID. Defaults remain byte-for-byte scoped to the existing stage0/`android-arm64`/`android1` paths; the documented M5 recipe selects stage1, fresh `work/build/android-arm64-release`, and future `android2` output without changing app constants or the accepted distribution. Shell syntax, default/override resolution, `git diff --check`, and the canonical Lean patch equality guard passed.
- Validation in progress: start a fresh four-job Android cross-build with durable pinned CMake 3.31.10, `HOST_PRODUCER_STAGE=stage1`, and `ANDROID_BUILD_VARIANT=android-arm64-release`. Preflight confirmed 3.4 TB free on `/mnt/d`, the exact canonical Lean patch match, existing audited Android dependency prefix, and an absent/fresh candidate target. The accepted `work/build/android-arm64` and `output/lean-4.32.1-android1` remain untouched. Exact command is the M5 release-candidate recipe in `toolchain/README.md`; after exit, audit generated Core/Std headers, AArch64 ELF/archive members, dependencies, and conformance before assembling or promoting anything.
- The candidate configured successfully in the fresh directory with NDK Clang 19.0.1, API 29, arm64-v8a, static Android dependency prefix, exact Lean commit `f054605aea4b840552cca2e725580bffd1e1b704`, and host stage1 as the previous-stage producer. The four-job native build is alive in unified session 27351 and is compiling the Android runtime/kernel/library targets; no accepted path or device state has changed. Exact recovery step: poll session 27351 without interrupting it; if it did not survive, rerun the same documented incremental candidate command and inspect the exact damaged/missing artifact before removing anything.
- The candidate completed its native runtime/kernel/library phase and crossed from dependency scanning into actual Core/Std/Lean module and generated-C production. Session 27351 remains alive with four jobs, all observed producer commands use `work/build/host/stage1/bin/lean`, and all candidate outputs remain confined to `work/build/android-arm64-release`. No accepted distribution, app constant, or device state has changed. Exact next step remains polling session 27351 to a clean exit, then auditing release olean headers and every candidate AArch64 ELF/archive boundary before assembly.
- While that independent build continued, added a delivery-neutral dependency-pack payload source and bounded staging installer. It requires the exact toolchain identity, accepts only a new staging root, creates parents without following links, enforces each declared size and SHA-256 while streaming, and runs the complete verifier (including executable-magic rejection) before returning. Activation, rollback, markers, and cleanup remain intentionally outside this low-level slice. Focused `DependencyPackManifestTest` passed in 5m28s (40 tasks; 5 executed, 35 up-to-date), covering success plus wrong identity, existing destination, truncation, corruption, executable content, undeclared data, symlinks, traversal, and case collisions.
- A second host crash killed the incremental candidate resume before it produced additional durable modules: no build/Lean/Gradle process survived, the candidate still contains 913 oleans, and final Lean/Lake binaries plus the candidate `Init.olean` are absent. No accepted artifact or device state changed. There is no identified corrupt output or reason to clean; exact recovery is the documented four-job `build-android.sh` candidate command, which should reuse the 913 completed modules and continue in `work/build/android-arm64-release`.
- The post-crash resume reused all native targets, then stopped on `mv .../Std/Time/Zoned/TimeZone.c.tmp: No such file or directory`. Narrow inspection found the complete same-invocation `TimeZone.olean`, `.ilean`, `.olean.private`, `.olean.server`, `.ir`, and final generated `TimeZone.c`, all timestamped together; no temporary file or active producer remains. This is consistent with a duplicate/racing target move rather than a missing final companion. Preserve those outputs and rerun incrementally first; delete only this module's proven output set if the identical failure repeats.
- The clean incremental retry passed the `TimeZone` boundary without deletion and continued through later Std/Lean generation. It has now emitted candidate `lib/lean/Init.olean`; direct header inspection proves exact olean version `4.32.1` and full commit `f054605aea4b840552cca2e725580bffd1e1b704`, resolving the old Android `4.32.0-pre` header incompatibility at the Core/Std artifact level. The full four-job build remains alive in session 32432 and final binaries/audits are still pending; do not promote from this partial evidence.
- The next incremental boundary exposed one proven crash-corrupt companion: compiling `Lean/CoreM` rejected `Lean/Util/RecDepth.olean.server`, whose 688 bytes were entirely zero while the main olean had a valid release header. Removed only that module's eight generated facets (`.olean`, `.ilean`, `.olean.private`, `.olean.server`, `.ir`, generated `.c`, `.o`, and `.o.export`), preserved its dependency file and all unrelated outputs, and will resume the exact build so `RecDepth` is regenerated before `CoreM`.
- That targeted repair succeeded: `RecDepth` regenerated and `Lean/CoreM` passed. The build later found a second proven zero-filled crash companion, the 96-byte parent-module `Std/Tactic/BVDecide/Normalize.olean.private`; its main olean and all child modules were distinct and valid. Removed only the same eight generated facets for the parent `Normalize` module, preserving its `.depend` and every `Normalize/*` child. Exact next step is another incremental resume and confirmation that `Std/Tactic/BVDecide` passes.
- The `Normalize` repair succeeded and aggregate `Std/Tactic/BVDecide` passed. Core/Std/Lean generation completed through `Lean.lean`, then Leanc/LeanIR and Lake generation began. The session entered a long `/mnt/d`-saturated archive/link interval and the host crashed again before a captured exit. Recovery inspection finds no surviving build/Lean/Lake/Gradle process, 2,324 durable candidate oleans, intact exact-release `Init.olean`, and no final candidate Lean/Lake/leanc binaries. The accepted Android1 distribution, app constants, and device state remain untouched. Exact next step is the same four-job incremental candidate command; preserve all 2,324 outputs and inspect only an explicitly reported damaged facet if recovery finds another invalid crash companion.
- Recovery resumed from those 2,324 oleans, completed the Lake tail and `make_stdlib`, then the final linker safely rejected two invalid `libInit.a` members. Exact symbols traced them to `Init/Data/String/Lemmas/Pattern/TakeDrop.o` and `Init/Internal/Order.o`; both `.o` and `.o.export` pairs were entirely zero-filled at the same original crash timestamp. A complete magic scan of every candidate temp `.o`/`.o.export` found only those four invalid files. Removed only those four proven corrupt objects, preserving their valid generated C/olean facets and all other objects. Exact next step is the same incremental command, which must rebuild the two objects, refresh `libInit.a`/`libleanshared.so`, and link Lean/leanc/leanir without the prior warnings or undefined initializers.
- The final narrow rebuild recompiled only those two C modules, refreshed `libInit.a` and both shared libraries, and exited 0 with no invalid-member warning or undefined initializer. Lean, Lake, leanc, leanir, leanchecker, `libleanshared.so`, and `libLake_shared.so` all linked successfully in isolated `work/build/android-arm64-release`. This is build completion only; release-header, complete AArch64 ELF/archive, dependency, hash, and conformance audits are now in progress before any distribution assembly or promotion.
- Build-tree audits passed. All 2,433 main oleans across Init/Std/Lean/Lake/root have exact version `4.32.1` and commit `f054605aea4b840552cca2e725580bffd1e1b704`; all 4,985 checked `.o`/`.o.export` files are valid AArch64; all 2,619 members across 15 static archives are AArch64 with zero readelf errors. The five final executables and five shared libraries are AArch64 and depend only on the packaged Lean/Lake libraries plus Android `libc.so`, `libdl.so`, and `libm.so`. Host stage1 checking with the candidate library path accepted `Valid.lean` (theorem plus `#eval`) and rejected `Invalid.lean` at line 2 with the expected `rfl` diagnostic.
- Final build-tree SHA-256/size anchors include Android Lean `4990b8466b45feab53d501ab0060f590941631a6d7df6a1cce41e46f8fc6f9ea` / 7,304 bytes, Lake `808931ba6b8d3589fead6a45d3f6084a720fb94d06262e951c6e2e69e01d9f31` / 11,600 bytes, `libleanshared.so` `9f4b103c9b2532fd9c36d69943a65fc7c510f8ead6c7248446c17d01560e1c48` / 272,678,960 bytes, and `libLake_shared.so` `019d0bae69f779cf06ca621f6ef09ec641eb6047211e3f4deb276a3bbd40876c` / 13,634,832 bytes. Exact next step is isolated distribution assembly with `ANDROID_BUILD_VARIANT=android-arm64-release` and `TOOLCHAIN_ID=lean-4.32.1-android2`, retaining Android1 and leaving app constants unchanged.
- Isolated Android2 assembly is now alive in session 37801 using the validated `android-arm64-release` build and exact new target `toolchain/output/lean-4.32.1-android2`; preflight found that target absent and 3.4 TB free. The script may remain quiet while copying the multi-gigabyte sysroot on `/mnt/d`, then must pass its packaged ELF/dependency and manifest/hash audit. Android1 remains preserved and no app/device state has changed. Exact recovery step is to poll session 37801; after a crash, inspect the candidate target before rerunning the same assembly command, which is authorized to recreate only Android2.
- Crash recovery found that session 37801 and all relevant build/Lean/Lake/Gradle processes are gone. The isolated Android2 directory contains 10,355 files and its expected native entries, but has no `manifest.json`; it is therefore an incomplete, untrusted assembly rather than a packaged toolchain. The already audited `android-arm64-release` build tree remains the trustworthy candidate input, Android1 remains untouched, and `/mnt/d` has approximately 3.4 TB free. Validation in progress: rerun `LEAN4ANDROID_ANDROID_BUILD_VARIANT=android-arm64-release LEAN4ANDROID_TOOLCHAIN_ID=lean-4.32.1-android2 toolchain/scripts/assemble-distribution.sh`; the script is explicitly scoped to recreating only the incomplete Android2 output. After exit, require the complete manifest, ELF/dependency audit, distribution counts/size/hash anchors, and zero residual processes before considering packaging complete. APK staging/build and promotion remain pending.
- The resumed legacy assembly is alive as session 28862 and uses the pre-edit script already loaded by that shell. At the user's request, future assemblies now print stage boundaries and, during each copy lasting at least 30 seconds, periodic destination file counts and human-readable copied size; ELF audit and manifest hashing also announce their start. `bash -n` and `git diff --check` pass for the script/history change. This intentionally does not interrupt or alter session 28862; the new progress reporting will apply on the next invocation.
- At the user's explicit request, interrupted only session 28862 with SIGINT so the progress-enabled script can be used immediately; it exited 130, and a subsequent process check found no assembly or matching copy process. Restart the same isolated Android2 command now. It will recreate only the already incomplete Android2 output, while Android1 and the audited release build input remain untouched.
- The progress-enabled retry ran as session 48865 and reported the Lean-library copy reaching 6,278 files / 1.3 GiB. At the user's explicit request, interrupted only that session with SIGINT so stdout can also be persisted; it exited 130 and no assembly/matching copy process remained. Exact restart adds shell `pipefail` and tees combined stdout/stderr to the sibling `toolchain/output/lean-4.32.1-android2-assembly.log`, which is outside the distribution directory recreated by the script. This preserves the real assembly exit status while making `tail -f` available from another shell.
- The logged assembly completed all copies, ELF/dependency audit, and manifest hashing with exit 0. Post-completion inspection found 14,899 distribution files / 14,891 sysroot files, no residual assembly/copy/Lean/Lake/Gradle/CMake process, and native hashes exactly matching the audited build-tree anchors. It also caught a pre-existing manifest-writer defect: the structurally valid 14,898-entry manifest hard-coded `lean-4.32.1-android1` even when assembling Android2. The writer now accepts the validated `TOOLCHAIN_ID_VALUE`, streams file hashing instead of reading each whole file into memory, and emits file/byte percentage progress. Validation in progress: regenerate only Android2's manifest from the already assembled bytes with combined output logged at `toolchain/output/lean-4.32.1-android2-manifest.log`; then repeat identity/count/size/hash/process checks. No distribution recopy or Android1 mutation is required.
- Android2 isolated distribution packaging is complete. The corrected manifest regeneration hashed all 14,898 payload entries / 3,667,196,078 bytes, reported 22/48/66/100% progress, and exited 0 in 111 seconds after its `/mnt/d` metadata pre-scan. The final manifest is 3,166,524 bytes, identifies `lean-4.32.1-android2`, binds Lean 4.32.1 commit `f054605aea4b840552cca2e725580bffd1e1b704`, arm64-v8a, API 29, and has SHA-256 `9b93929b9ac06891ac943eb1373b87d98fdfb687e8c228979e4d8734fc8cb544`. Its aggregate entry bytes exactly equal the 14,898 non-manifest filesystem payload bytes; the distribution has 14,899 files total and 14,891 sysroot files. Exact native hashes remain Lean `4990b8466b45feab53d501ab0060f590941631a6d7df6a1cce41e46f8fc6f9ea`, Lake `808931ba6b8d3589fead6a45d3f6084a720fb94d06262e951c6e2e69e01d9f31`, `libleanshared.so` `9f4b103c9b2532fd9c36d69943a65fc7c510f8ead6c7248446c17d01560e1c48`, and `libLake_shared.so` `019d0bae69f779cf06ca621f6ef09ec641eb6047211e3f4deb276a3bbd40876c`. No assembly, hashing, copy, Lean, Lake, Gradle, or CMake process remains. Android1 and app/device state are unchanged.
- Repository-wide long-operation observability is implemented across toolchain builds, dependency builds/installs, source fetches, distribution copies/audits, signed/runtime delivery packaging, Mathlib producer fetch/inventory, filtered/full manifest verification, and device LSP conformance. `LEAN4ANDROID_LOG_FILE` tees combined output without masking exit status; `LEAN4ANDROID_PROGRESS_INTERVAL_SECONDS` controls default 30-second heartbeats. Measurable copy/hash/inventory operations report percentage with file/item and byte denominators; native build/download tools retain their own progress when no honest repository-level percentage exists. Shell syntax, Python compilation, synthetic tee/heartbeat/failure-status tests, manifest identity generation, small Mathlib inventory/filtered-manifest percentage tests, and `git diff --check` pass.
- Exact next M5 gate: add an isolated Android2 APK/runtime selection that leaves Android1 as the default, assemble the toolchain-bearing candidate with no build cache and durable logging, audit its staged manifest/native entries, then fresh-install or otherwise isolate app data on the API-33 tablet and rerun the existing Core/Std direct Lean, Lake, LSP/editor, migration, offline, and no-orphan conformance before promoting any active toolchain constant or beginning the full release-ABI Mathlib build. The current Android2 distribution is packaged and host-audited, but APK/device compatibility and Mathlib pack production remain pending.
- Final focused validation passed `:core-toolchain:testDebugUnitTest` in 1m01s (40 tasks; 1 executed, 39 up-to-date), covering the dependency-pack parser, hostile manifests, payload verification, corruption/executable rejection, and install boundary. The first sandboxed invocation failed before Gradle configuration only because it could not determine a usable wildcard IP; the unrestricted identical retry passed. No Gradle or native process remains.

## 2026-08-15 — M5 isolated Android2 APK conformance in progress

- Started from committed checkpoint `62f643a`, the host-audited `lean-4.32.1-android2` distribution, a clean worktree, and no surviving Gradle/Lean/Lake/native process. Android1 remains the checked-in/default app identity and its accepted device data must not be overwritten.
- Implementation in progress: add validated opt-in Gradle properties for toolchain distribution ID, filtered sysroot bytes, filtered-manifest digest, and a debug-only isolated application-ID suffix. Key generated staging by toolchain ID, expose the selected ID consistently to app/core-toolchain BuildConfig, remove the editor's duplicate hard-coded ID, and parameterize device conformance by package/toolchain. Exact next step: focused Kotlin/unit compilation with defaults, then a two-pass Android2 staging measurement followed by no-build-cache candidate APK assembly logged outside recreated outputs.
- Default-path focused validation passed `:app:compileDebugKotlin :core-toolchain:testDebugUnitTest` in 30m54s (98 tasks; 6 executed, 92 up-to-date). The long first run populated the newly toolchain-keyed Android1 staging directory; filtered-manifest verification reported 28/70/98/100% across 14,864 files / 2,192,429,171 bytes and reproduced exact digest `f7e389dcee7bd8f146fcd9e7f05ca6ddc9243bd3e99e3261a5dee79e7d1aa797`. This proves the opt-in selection leaves default Android1 BuildConfig/staging identity unchanged. No Gradle process remains. Exact next step: run Android2 `:app:writeFilteredToolchainManifest` with shell `pipefail` and durable `tee`, measure its filtered directory bytes and manifest digest, then pass those exact values into candidate compilation/assembly.
- Android2 staging and verification passed in 27m05s with durable log `toolchain/output/lean-4.32.1-android2-gradle-stage.log`. The filtered tree has 14,864 files / 2,192,029,800 declared payload bytes; `du -sb` measures 2,194,503,784 bytes for installer preflight accounting. Its TSV identifies `lean-4.32.1-android2` and has SHA-256 `dbb428a4f7823e459741d16e663ab89eb94492d231c0d8cf2a43138d81dadaf3`; verification reported 26/67/98/100% and completed in 110 seconds. No Gradle process remains. Exact next step: assemble the isolated debug candidate with these measured byte/digest properties, `-PisolatedToolchainCandidate=true`, no build cache, and durable combined logging; then audit application ID, BuildConfig identity, APK payload, and hashes before touching the device.
- Isolated Android2 APK assembly passed in 28m28s (144 tasks; 28 executed, 116 up-to-date) with durable log `toolchain/output/lean-4.32.1-android2-apk-build.log`. Exact APK: 805,509,604 bytes, SHA-256 `afa01e2de470ba4d2bf7eaf86c6a977c072b2747fcf67363b09d540fce624dfb`. AAPT proves package `org.lean4android.app.android2candidate`, version `0.1.0-dev-android2candidate`, min API 29 / target 36. Generated app/core BuildConfigs agree on Android2, preflight 2,194,503,784 bytes, and manifest digest `dbb428a4f7823e459741d16e663ab89eb94492d231c0d8cf2a43138d81dadaf3`. APK inspection proves the embedded TSV identity/digest, eight arm64 native entries, `Init.olean`, 2,431 each of private/server/IR facets, and exact native hashes matching the host distribution. No Gradle or Lean/Lake process remains. Exact next step: resolve current USB/device state, install only this isolated package for owner user 0, launch it from fresh app-private state, and validate Android2 installation/direct/Lake/LSP/editor behavior without mutating the accepted Android1 package or its data.
- Preservation/monitoring checkpoint: the authorized SM-T870 remains online, and the isolated package remains installed as UID 10509 and foregrounded without reinstall, force-stop, uninstall, or data mutation. Three samples from 19:36:40 through 19:37:05 PDT showed `Lean server: Ready` with a stable package-owned process tree: app PID 6788, Lake PID 6841, coordinator Lean PID 6851, and worker Lean PIDs 6857/6860. RSS fluctuated normally while PIDs and readiness remained stable; no host Gradle, packaging, copy, Lean, or Lake job is alive. Preserve this device state and continue UID-scoped monitoring; do not confuse the accepted Android1 package's native children with candidate orphans.
- Documented Android1 APK recovery separately in `ANDROID1_REBUILD.md`; no rebuild was started. The accepted Android1 runtime is the completed M4 line through M4.6, while M5 begins the Android2/Mathlib candidate work. The document distinguishes the shared overwrite-prone Gradle APK path from identity-specific preserved copies, gives the default/no-override logged rebuild command, required host/device checks, and the full-toolchain fallback. The existing Android1 distribution, Android2 candidate, installed packages, and device processes were not changed.
- Read-only tablet storage diagnosis explains Android Settings reporting approximately 10.54 GB for the accepted Android1 app: its installed base APK is 805,926,958 bytes, while credential-encrypted private data is 9,236,377 KiB. Of that data, 7,056,584 KiB is the deliberately preserved M5 completeness-first `mathlib-4.32.1-complete-probe`, 2,175,534 KiB is the installed Android1 toolchain/sysroot, and ordinary `files`, cache, preferences, and code cache account for only about 4.3 MiB. This Mathlib tree was streamed into Android1 during the Phase B feasibility probe before the official-cache versus Android1 olean-header incompatibility was discovered; it is not part of the APK and is not active Android1 application content. No file or device state was changed during diagnosis.
- Validation in progress: at the user's request, migrate the preserved completeness-first Mathlib tree from Android1 UID 10505 to isolated Android2 UID 10509. Android sandboxing prevents a direct cross-package `cp`, so the safe plan is to stop both packages, stream a relative no-follow tar through ADB into a newly created Android2 `no_backup/mathlib-packs` directory, verify exact regular-file count, allocated/declared bytes, absence of links and host executables, and run a real Android2 Mathlib import before deleting the Android1 source. `/data` has 26,619,236 KiB free, enough for the temporary duplicate. No source deletion is authorized until all destination checks pass; exact recovery after interruption is to inspect the candidate destination and remove only that incomplete new Android2 probe before retrying.
- The UID-correct ADB/tar bridge completed with exit 0 after streaming 6,996,735,488 tar bytes in 898 seconds; durable byte progress is in `toolchain/output/mathlib-android1-to-android2-transfer.log`. Source and destination each report exactly 122,204 regular files and zero symlinks; representative `lake-manifest.json`, `Mathlib.olean`, `Batteries.olean`, and `Qq.olean` SHA-256 values match, while the 16 KiB allocated-size difference is directory metadata. The optional logical-byte scan was invalid because Android `find -exec` exceeded its argument limit and its partial totals must not be used. Copied the four-file relative-path `M5MathlibProbe` project into Android2. The corrected `lake env lean` run with an absolute source path passed the immediate Android1 header-failure boundary and ran quietly for roughly two minutes, but WSL then lost the tablet entirely (`error: closed`, no device in ADB or `lsusb`), so the result is inconclusive. Android1's source remains intact, both apps were force-stopped before migration, and Android2 now holds the unpromoted copy. Exact recovery: reattach the Samsung USB device, resolve/repair only its current node if required, verify both tree counts/sentinel hashes again, check package-UID residual processes, rerun the absolute-path Android2 Mathlib probe to exit 0, and only then remove exactly Android1's `no_backup/mathlib-packs/mathlib-4.32.1-complete-probe` at the user's request.
- After reattachment as `/dev/bus/usb/001/003` with correct `root:plugdev` 0660 permissions, ADB reconnected and repeat validation again found 122,204 files, zero links, matching Mathlib/Batteries/Qq hashes, and no residual UID-10505/10509 process. The full representative Android2 source imports analysis, number theory, and `Mathlib.Tactic`; it again crossed Android1's immediate incompatible-header boundary without stderr but did not complete within the explicit 900-second limit, exiting 124. The tablet then disappeared from both ADB and `lsusb` again, so timeout cleanup is unobservable and the heavy probe is a performance/device-stability failure rather than compatibility success. Android1's source remains deliberately undeleted. Exact next step after another reattachment is package-UID cleanup inspection followed by a narrow `Mathlib.Data.Nat.Prime.Basic`/`norm_num` probe with bounded memory/timing observation; do not delete Android1 until at least that real import exits 0 and the destination remains verified.
- After reattachment as USB device 004, neither package had a residual process. Installed a byte-verified 164-byte narrow fixture importing `Mathlib.Data.Nat.Prime.Basic` and `Mathlib.Tactic.NormNum`; Android2 rejected the official-cache `Basic.olean` in seven seconds with `incompatible header`, exit 1. Direct bytes explain the mismatch missed by version/commit-only inspection: both artifacts name version 4.32.1 and commit `f054605…`, but official Mathlib begins `olean 02 01` while Android2 `Init.olean` begins `olean 02 00`. The copied official `Basic.olean` has SHA-256 `3d8c0849…`, whereas the earlier clean stage1-produced targeted replacement was `c900db3d…`; the latter must be reproduced and tested, then the complete Mathlib/dependency artifact closure must be rebuilt with the exact Android2 producer/target configuration. No package process remains and `/data` has 19,080,500 KiB free. Android1's probe remains undeleted because the promised Android2 import gate failed; deleting it now requires an explicit decision to retain the verified Android2 copy only as source/provenance data, not as a working pack.
- At the user's explicit request, force-stopped both packages and permanently removed only their resolved, non-symlink `no_backup/mathlib-packs/mathlib-4.32.1-complete-probe` directories (7,056,584 KiB under Android1 and 7,056,600 KiB under Android2). Both targets are absent, no UID-10505/10509 process remains, installed Core/Std toolchains and projects were outside the targets, and `/data` free space increased to 34,172,872 KiB. Added a dedicated `build-android2-artifacts.sh` wrapper and README recipe with combined durable logging, 30-second module-count progress, approximate full-build percentage against 8,654 jobs, pinned-source guards, `--no-cache`, and Android2 Core/Std selection. A separate clean producer tree fetched Mathlib commit `520045ab…` plus all eight locked dependencies successfully with 9/9 progress in 155 seconds; log is `toolchain/output/mathlib-android2-fetch.log`. Exact next gate is the logged `Mathlib.Data.Nat.Prime.Basic` rebuild, header/hash inspection, and physical Android2 import before the full build.
- Targeted Android2-compatible Mathlib production is now alive in unified session 79896 with four jobs and durable combined log `toolchain/output/mathlib-android2-basic-build.log`. The first wrapper attempt exposed and fixed two pre-build configuration defects: a duplicate readonly repository variable, then `LEAN_SYSROOT` selecting the non-host-executable Android binary. The validated wrapper now creates an isolated adapter sysroot with host stage1 executables and Android2's audited `lib/lean`, while retaining pinned/no-cache/no-update guards. The corrected run has produced five oleans through Batteries and the former bootstrap trouble area after 294 seconds with no incompatible header or IR assertion. Exact recovery if the session dies is the documented targeted command with `LEAN4ANDROID_MATHLIB_TARGET=Mathlib.Data.Nat.Prime.Basic`; preserve the clean incremental tree at `toolchain/work/mathlib-android2-producer` and inspect only an explicitly failed artifact before removing anything.
- Added automatic conservative crash recovery for subsequent targeted/full invocations. A producer-tree `flock` rejects concurrent writers; `build-state.txt` records running/complete/failed/interrupted state; a no-follow preflight scans generated package build roots with progress, removes only orphaned `.tmp`/`.part` files, validates finalized olean magic/version/commit and 16-byte hash sidecars, detects empty/all-zero generated facets, and invalidates only the damaged module's known library/IR facet family. Lake then uses `--rehash` plus `--no-cache`, preserving healthy completed modules while updating stale dependencies. Static Bash/Python/whitespace validation passes. The currently active session predates the new lock/state code and must not be launched concurrently, but it remains healthy at 33 oleans after 1,185 seconds; after it exits or a crash kills it, every rerun uses the recovery path automatically.
- Updated `docs/delivery/M5_MATHLIB_INTEGRATION_PLAN.md` from its stale planned/Android1 state to the observed M5 checkpoint: Android1 accepted through M4.6; Android2 distribution/APK focused conformance; official-cache incompatibility and tablet cleanup; measured Phase A inventory; active Android2 producer; partial dependency-pack verifier/streaming-installer work; remaining activation, UI, channel, and acceptance boundaries; and exact crash-recovery/monitoring commands. The active pre-recovery-wrapper build remains untouched in unified session 79896 with four jobs and durable log `toolchain/output/mathlib-android2-basic-build.log`; its latest observed progress is 80 `.olean` files after 2,838 seconds. Do not launch another producer concurrently. After successful targeted completion, inspect facets/header/hash, deploy only the rebuilt slice, prove a real Android2 import, and check residual package processes before starting the estimated 8,654-job full build.
- Recorded the post-bake-off UI dependency policy in `IMPLEMENTATION_PLAN.md`: native Jetpack Compose remains the production UI stack; prefer its existing primitives and focused project-owned code; add narrowly scoped libraries only for a demonstrated problem whose correct local implementation would be disproportionately large; and require especially strong, documented justification before introducing WebView or another web runtime. No runtime dependency or application code changed.
- Added the user-prioritized UI/UX roadmap as M5.0: first a persistent Docked/Popup Output presentation choice with automatic Build/Run reveal, then enabled/disabled automatic LSP completion in a caret-anchored selectable popup rather than Goals. Deferred auto-indent and syntax-aware code folding to M7 design/measurement work. This is documentation only; the active Android2 Mathlib producer and application/device state were not changed.
## 2026-08-15 — Current UI code overview documentation

- Added `docs/editor/CURRENT_UI_OVERVIEW.md`, documenting the current native Compose application shell, adaptive workspace, drawer, editor, and data flow, with a direct map to the files containing the top bar, editor, Output, Messages, and Goals code and their supporting state/model files and tests.
- Verified the named composables/helpers against `MainActivity.kt`, confirmed every linked source/test file exists, and passed `git diff --check`.
- Documentation-only change: no Kotlin, build configuration, runtime artifact, app/device state, or process changed, so no Gradle or device validation was required. The previously recorded M5 producer process is not currently visible in the process table; this documentation work did not attempt to resume or alter it.
## 2026-08-15 — stdin support design documentation

- Added `docs/project/STDIN_SUPPORT.md` as the M5.1 implementation design for immediate-EOF, interactive-console, and contained project-file stdin. It defines typed process/service ownership, one-writer serialization, bounded backpressure, ordered EOF, dirty-file revision choices, separately retained/exportable stdout and stderr, recreation/process-death behavior, UI truthfulness, security limits, implementation sequence, and host/device validation.
- Cross-linked the design from the M5.1 roadmap and rebuilding boundary. The design explicitly keeps stdin as an unstructured pipe rather than a PTY/terminal and prohibits inferring an input request from prompt-like output or blocked-child heuristics.
- `git diff --check` passed. Documentation only: no Kotlin, build configuration, runtime artifact, app/device state, or process changed, so no Gradle or device validation was required. No relevant process was started. Exact next implementation step remains M5.1 after the preceding M5/M5.0 gates: add the typed stdin/snapshot model and race/backpressure tests in `core-process` before exposing UI controls.

## 2026-08-16 — Android2 Mathlib Basic live progress interpretation

- Confirmed the resumed `Mathlib.Data.Nat.Prime.Basic` producer is still advancing without intervention: durable state remains `running` for the 2026-08-15 23:43:57 PDT invocation, and the log advanced through Lake job `[168/180]` while the independent monitor reported 164 `.olean` files present.
- Lake's `[completed/known]` prefix counts build jobs in its dynamically discovered dependency graph; the wrapper's `oleans present` value independently counts finalized `.olean` files under all `.lake/*/build/lib/lean` roots. They are correlated but intentionally not identical: scheduling/aggregate jobs need not emit an olean, the known-job denominator can grow, and the 30-second filesystem sample can lag a just-completed Lake line. No artifact was changed or process interrupted. Exact next step remains letting the single lock-owning build continue and using its terminal exit plus target-facet/header audit as completion evidence, not either live counter alone.

## 2026-08-16 — Lake module-discovery repair plan

- Began a documentation-only corrective plan after manual testing showed that the generated `roots = ["Main", "<Project>.Basic"]` configuration excludes newly created sibling modules such as `<Project>/Extra.lean` and arbitrary top-level modules beside `Main.lean` from Lake's buildable library set.
- Agreed sequencing: do not interrupt the active targeted `Mathlib.Data.Nat.Prime.Basic` producer. Implement and validate this repair immediately after that targeted build and its basic Android import test, before proceeding with broader/full Mathlib production.
- Planned boundary: cover the generated inner project directory with a Lake submodule glob; reconcile explicit Lake entries for top-level Lean files and top-level module directories on every relevant create/import/save/rename/move/delete lifecycle and again before Build/Run/LSP startup; default the Files → New path to `<LeanProjectName>/New.lean` while still allowing the user to remove or replace that prefix.
- Added the ordered M5 corrective checklist and exit criteria to `IMPLEMENTATION_PLAN.md`. It covers canonical topology-derived TOML, atomic/idempotent lifecycle reconciliation, `<LeanProjectName>/New.lean` prefill with an editable prefix, module-name validation, supported-project migration, drift recovery before Build/Run/LSP, host coverage, and physical offline acceptance.
- No Kotlin, Lake configuration, generated project, build/runtime artifact, process, or device state was changed. Exact implementation step remains waiting for the active targeted Mathlib Basic build and Android import validation, then completing this corrective before broader/full Mathlib production.

## 2026-08-16 — Parallel Mathlib lane and roadmap recovery in progress

- Began separating slow Mathlib artifact production from the foreground product roadmap at the user's request. The goal is to keep one resumable, resource-bounded Mathlib producer lane while allowing independent M5 corrective, M5.0, M5.1, M5.2, M5.3, M6 preparation, and API-matrix work to advance when they do not consume or mutate the producer tree.
- Corrected an initially incomplete process-table diagnosis using the authoritative producer signals: the wrapper's `flock` is held and the durable log is actively advancing. At 2026-08-16 12:08 PDT it reported 473 finalized `.olean` files and Lake jobs through `[486/489]`. The recorded PID is not visible from this shell's process view, demonstrating why neither PID lookup nor process-table visibility alone establishes liveness. The current targeted producer remains live and must not be interrupted or launched concurrently; no device child was started by this planning work.
- Added `mathlib/scripts/status-android2-build.sh`, which reports the state record, authoritative lock, advisory PID visibility, finalized olean count, log freshness, last progress line, and a safe next-action assessment without mutating the producer. Updated the implementation roadmap, M5 delivery plan, rebuilding guide, and toolchain README with two coordinated lanes, exact targeted/full commands, distinct durable logs, safe two/four-job guidance for later invocations, checkpoint/device ownership, and conservative crash recovery. The M5 dynamic Lake corrective now proceeds in the foreground without waiting for the targeted build but still gates the broader/full producer.
- Validation passed: both Mathlib scripts pass `bash -n`, the new status helper observed the held lock and a log advancing through 473 finalized oleans, and `git diff --check` passed. No Gradle, APK, or device validation was required because this change affects documentation and read-only producer observability only. The live producer was not interrupted or modified. Exact foreground next step is the dynamic Lake module-coverage corrective; exact producer next step is monitoring until terminal completion, followed by facet/header and Android2 import validation.

## 2026-08-16 — UI styling architecture design in progress

- Began the user-requested foreground architecture task before further non-Mathlib implementation: add an explicit UI style/theme separation increment to the roadmap and decide how dimensions, spacing, foreground/background/content colors, typography, shapes, and component-specific visual states should move out of UI behavior code.
- Current code evidence: the app has only an inline `MaterialTheme` light/dark selection; `MainActivity.kt` owns most visible composables and embeds many `dp` dimensions, paddings, Material color roles, shapes, and text-style selections, while `HoverMarkdown.kt` contains a smaller second cluster. `EditorPaneLayout.kt` already separates behavioral/adaptive layout rules, which must remain distinct from visual tokens.
- Added `docs/editor/UI_STYLE_ARCHITECTURE.md` and the M5 foreground prerequisite in `IMPLEMENTATION_PLAN.md`. The design compares inline styling, Material-only theming, flat constants, typed Kotlin tokens/component styles, modifier presets, CSS-like classes, external JSON/TOML/YAML/XML/custom stylesheets, Android resources, a separate module/third-party library, generated design-tool tokens, and component extraction alone.
- Selected a layered Compose-native Kotlin design system: `Lean4AndroidTheme` plus Material foundations, semantic tokens through composition locals, immutable styles for meaningful product components, and incremental composable extraction. Adaptive/window/IME policy, state, semantics, content, and caller modifiers remain explicit logic; the design rejects a runtime parser/cascade/class-string system at this stage and defines bounded revisit triggers.
- Documented component/node coverage, proposed packages/API shape, theme/font-preference behavior, staged migration, host/physical validation, and M4.6-preserving exit criteria. Cross-linked the decision from the current UI overview. Documentation validation passed with `git diff --check` and all referenced repository documents exist. No Kotlin, dependency, Gradle artifact, app/device state, or producer state was changed; no build was required. The active Mathlib producer was not interrupted. Exact foreground next step is implementation phase 1: characterize the current visual baseline and extract `Lean4AndroidTheme` without changing its default light/dark schemes.

## 2026-08-16 — UI styling architecture first slice complete

- Began the first implementation slice from `docs/editor/UI_STYLE_ARCHITECTURE.md`. Scope is intentionally behavior-preserving: extract the root Material light/dark setup, add typed semantic dimensions and component styles, and migrate the smallest bounded editor components before attempting screen/state restructuring.
- Initial migration targets are Goals, Output, Messages, pane splitters, file tabs, and the symbol row because their current visual contracts are localized and already physically accepted. Adaptive pane placement/fractions, IME rules, callbacks, semantics, content, and caller-owned modifiers remain in existing logic.
- The worktree was clean at start. The targeted Mathlib producer was initially live and isolated; its lock was held and the status helper reported 487 finalized `.olean` files. No producer command was launched by this UI work.
- Validation in progress: `GRADLE_USER_HOME="$PWD/.gradle-user-home" ./gradlew --no-build-cache --no-daemon --console=plain :app:testDebugUnitTest --tests org.lean4android.app.ui.theme.LeanThemeTest :app:compileDebugKotlin` is alive in unified session 31087 after the restricted-sandbox attempt failed before task execution with the known wildcard-IP/socket error. The approved rerun has completed dependency/module compilation steps and is still processing app toolchain staging on slow `/mnt/d`; no code failure has been reported. Exact recovery if the process does not survive is rerunning the same focused command, which may reuse only outputs Gradle recorded complete. Do not remove staged toolchain or Gradle roots.
- User stopped both long-running processes to prioritize foreground UI/Gradle work. Verified recovery state at 13:39 PDT: no Gradle or producer process is visible; the Mathlib producer lock is free; its stale state still says `running`; and its durable log stopped at 13:15:29 after 512 finalized `.olean` files. Preserve those incremental artifacts and do not resume Mathlib for now. The Gradle invocation stopped during the unobservable `Sync` staging/file-comparison interval and produced no compiler/test result, so it must be rerun; no generated staging directory is assumed damaged or removed.
- Added `scripts/run-ui-gradle.sh` at the user's request so UI/Gradle validation has durable combined logging and 30-second elapsed-time heartbeats even while Gradle's bare `stageToolchainNative`/`stageToolchainSysroot` `Sync` tasks are silent. The runner preserves explicit task arguments and the pipeline exit status, defaults to the required full `testDebugUnitTest :app:assembleDebug` recipe, and reports the last nonblank Gradle output without claiming a fabricated percentage. Updated the Kotlin/Compose and APK recipes accordingly and ignored Gradle's root `.kotlin/` generated state.
- Focused validation restarted through the new runner as `scripts/run-ui-gradle.sh :app:testDebugUnitTest --tests org.lean4android.app.ui.theme.LeanThemeTest :app:compileDebugKotlin`, with durable log `toolchain/output/ui-gradle-build.log`. The slow first run completed Gradle successfully in 56m53s (106 tasks; 18 executed, 88 up-to-date), including the new theme test and app Kotlin compilation. A host-side process check during its silent stage found the Gradle daemon active at roughly 26% CPU, and its I/O counters showed approximately 4.6 GB read and 2.2 GB written. That live run exposed recursive heartbeat text because the previous heartbeat qualified as the last output; editing the script while its shell still held the source descriptor caused only that wrapper process to report a post-build parse failure. The final runner excludes its own heartbeat marker, passes `bash -n`, and a clean rerun proved bounded heartbeats plus exit-status propagation with exit 0 in 2m23s (all 106 tasks up-to-date).
- Full `scripts/run-ui-gradle.sh` validation passed `testDebugUnitTest :app:assembleDebug` in 33m02s (175 tasks; 15 executed, 160 up-to-date), with the runner exiting 0 after 1,984 seconds. The resulting default Android1 APK is 805,445,474 bytes with SHA-256 `4a8094b453587b4640c297543b33524796a9fb1d9dab131bfab54983ae3f4411`. Update-install for owner user 0 on the authorized SM-T870/API 33 succeeded; a cold launch preserved the two-tab project and visibly rendered the editor, line-number gutter, syntax highlighting, file tabs, symbol row, splitters, Output, and Goals through the extracted dark theme, with `Lean server: Ready`. The app was then force-stopped and a process check found no app, Lean, or Lake child. Mathlib remains deliberately stopped with its incremental 512-olean producer tree preserved; no Gradle or Mathlib producer remains alive. The next UI styling slice is incremental migration of the remaining shell/drawer/dialog and hover-markdown visual constants, without mixing adaptive behavior into the theme layer.

## 2026-08-16 — UI styling architecture second slice in progress

- Began the next foreground slice while Mathlib production remains explicitly paused. The preserved producer tree stays untouched at 512 finalized `.olean` files, and no producer resume is authorized by this work.
- Scope is behavior-preserving migration of the remaining application shell, drawer/project tree, workspace/settings/empty-editor surfaces, dialogs, editor chrome, and hover Markdown visual constants into typed Compose theme styles. Adaptive layout, window/IME policy, state, callbacks, semantics, and process ownership remain in their existing code.
- Exact next step: extend the semantic style model, replace the bounded inline visual literals, add focused token invariants, then run narrow theme tests/Kotlin compilation followed by the required full UI Gradle recipe and proportional device validation. No Gradle, Lean, Lake, or device process is assumed alive at this checkpoint.
- The implementation now adds typed shell, project-tree, and hover styles plus semantic dimensions for the remaining migrated surfaces. `MainActivity` consumes those styles for drawer/workspace/settings/project-card/tree/empty-editor/editor-chrome layout, while `HoverMarkdown` consumes theme-owned link, code, typography, spacing, and content colors. No behavior, adaptive policy, semantics, or process callback moved into the styling layer.
- Focused validation passed in 3m39s (106 tasks; 5 executed, 101 up-to-date) for `LeanThemeTest` and app Kotlin compilation. The first restricted invocation failed before task execution with the known wildcard-IP socket limitation; the identical approved runner invocation succeeded. Exact next step is the full durable `scripts/run-ui-gradle.sh` regression, followed by diff/artifact inspection and proportional physical UI validation.
- Full validation passed in 3m35s (175 tasks; 4 executed, 171 up-to-date). The default Android1 APK is 805,512,672 bytes with SHA-256 `5eab0ee62771f577b7fba1954f050386e52522405ea96a4f6d51078f845961b8`; `git diff --check` passed. Physical validation is pending because `lsusb` currently exposes only the two Linux root hubs and no Samsung transport. No install or device mutation occurred.

## 2026-08-16 — M5 dynamic Lake module corrective in progress

- Began the next foreground gate after the host-complete UI styling slice; its physical styling check remains queued until the reference tablet is reattached. Mathlib remains stopped and its 512 finalized producer artifacts remain untouched.
- Initial implementation boundary is centralized `core-project` topology reconciliation: deterministic roots/globs, supported legacy generated-block migration, module-path validation, lifecycle triggers, drift recovery before Lake commands, and the derived `<LeanProjectName>/New.lean` UI default. Imported arbitrary Lake semantics must continue to fail explicitly rather than being silently rewritten.
- Exact next step: implement the repository reconciler and focused lifecycle/migration tests, then wire the derived New path and pre-LSP drift recovery. No Gradle, Lean, Lake, producer, or device process is active.
- First focused `:core-project:testDebugUnitTest` pass succeeded in 2m27s; expanded rename/pre-mutation-failure/no-follow coverage then passed in 1m26s (51 tasks; 5 executed, 46 up-to-date). Local inspection of the pinned Lake 4.32.1 schema established that explicit `globs` replaces the default roots-derived selection, so the canonical configuration now includes every top-level root in both `roots` and `globs`, followed by sorted top-level directory `.*` globs. This correction is implemented before aggregate validation.
- Repository lifecycle triggers now validate Lean module components and reconcile supported generated Lake configuration after create/copy/save/rename/delete and during archive/folder import; project rename updates package/library/default-target identity with rollback on reconciliation failure. Lake Build/Run and LSP startup reconcile again for drift recovery, and Files → New prefills the derived inner-module path. Exact next step is aggregate tests/APK assembly, canonical Lake execution evidence, and physical validation when the tablet returns.
- Aggregate `testDebugUnitTest :app:assembleDebug` passed in 4m19s (175 tasks; 15 executed, 160 up-to-date). Final default Android1 APK is 806,301,308 bytes with SHA-256 `7e2788d29853b4bea5bdb1653838847c38a312e92eba118977ca8e2d240c8367`. Documentation now records the host-implemented boundary and explicitly leaves physical offline migration/Lake/LSP/recreation acceptance pending because the Samsung transport is absent.
- Final producer audit confirms Mathlib is still paused: lock free, recorded PID 474 absent, stale `running` state, log unchanged since 13:15 PDT, and exactly 512 finalized oleans preserved. No Gradle, producer, Lean, or Lake process remains. Exact resume boundary is physical validation of the combined styling/Lake candidate when the reference tablet is reattached; until then the corrective is host-complete but not milestone-complete.

## 2026-08-16 — Foreground device acceptance resumed

- The user reports the reference tablet is attached and requested continuation of all non-Mathlib work. Started from clean commit `564ede6`; the Mathlib producer lock remains free, recorded PID 474 is absent, its log is unchanged, and all 512 finalized oleans remain preserved. Mathlib will not be resumed by this lane.
- Immediate scope is to update-install the already host-validated combined styling/Lake APK for owner user 0, exercise supported legacy migration plus inner/top-level/overlapping module coverage through the real UI and offline Lake/LSP paths, verify recreation/project rename and exact no-orphan cleanup, then proceed to M5.0 Output presentation.
- Exact next step: resolve the current Samsung `04e8:6860` USB node and permissions, establish an authorized project-ADB transport, inspect installed package/device baseline, then install only the candidate APK. No device mutation has occurred at this checkpoint.
- Resolved Samsung node `/dev/bus/usb/001/002` as `root:plugdev` mode `0660`; project ADB authorized `R52R40K1PPN` as the expected SM-T870/API 33 without a permission change. Update-installed the exact candidate for owner user 0. Preserved device baseline is physical 1600×2560, accelerometer rotation 0, user rotation 1, and Wi-Fi enabled.
- Cold launch visibly preserved the themed landscape editor, two original tabs, panels, symbol row, and `Lean server: Ready`. LSP startup migrated legacy `TestProj2` to the first canonical configuration. Files → New visibly prefilled `TestProj2/New.lean`; the real dialogs then created that inner module, top-level `Extra.lean`, and overlapping `Extra/Nested.lean`, producing the expected five source files and topology-derived TOML.
- The first Wi-Fi-disabled Run provided important corrective evidence rather than acceptance: Lake built `TestProj2.New`, `Extra`, and `Extra.Nested`, replayed `Main`, but exited 1 with `TestProj2: some modules have bad imports`. Wi-Fi was restored immediately. Root cause is now localized to Lake's `roots` contract: project-root module directories must be allowed roots as well as `.*` build globs; the host implementation included them only in globs. Exact next step is to make canonical roots the sorted union of top-level file modules and directory modules, update tests, rebuild/reinstall, and rerun this retained fixture offline.
- Corrected canonical roots to the sorted union of project-root `.lean` stems and module-directory names, while globs remain top-level file modules plus directory `.*` selections. The first focused run correctly exposed one test that assumed `Main` always sorted before the derived project name; the implementation was correct and the test now derives the deterministic order. The corrected focused suite passed in 1m15s (51 tasks; 2 executed, 49 up-to-date). Exact next step is the required aggregate APK regression, update-install, and repetition of the retained offline fixture.
- Aggregate validation passed in 3m47s (175 tasks; 4 executed, 171 up-to-date), and the replacement APK remained 806,301,308 bytes with SHA-256 `af283b834a1c871089b35a01c6aa62a3641d6d5df9b2b8580da2fe2d05aab27d`. The second Wi-Fi-disabled retained-fixture run still exited 1 with the same bad-import computation after migration to roots `Extra`, `Main`, and `TestProj2`; Wi-Fi was again restored immediately.
- That result refines the Lake contract: roots must name actual source modules, not namespace directories. The next candidate therefore derives sorted roots from every contained `.lean` path (`Extra`, `Extra.Nested`, `Main`, `TestProj2.Basic`, `TestProj2.New` for the retained fixture) and retains top-level file plus directory-wildcard globs as the build selection. Exact next step is focused/aggregate validation and a third offline device run; do not accept the corrective until it exits 0.
- The all-source-root candidate passed focused tests in 2m34s and aggregate validation in 5m22s (175 tasks; 6 executed, 169 up-to-date); APK SHA-256 is `0b710181efb1d5b941433e3301daf01f81c064491f51a912c0edd5dc29d54fd9`. Its third offline device run still produced bad imports. This isolates Lake's remaining invariant: roots cannot overlap as ancestor and descendant (`Extra` plus `Extra.Nested`). Wi-Fi was restored.
- Canonical roots now form the sorted minimal actual-source frontier: a module is a root only when no ancestor module source exists. Thus the retained overlap uses `Extra` but not `Extra.Nested`, while a directory without a parent source retains its actual leaf roots such as `TestProj2.Basic` and `TestProj2.New`; directory globs still select all descendants. Exact next step is a fourth focused/aggregate/device cycle using this device-derived contract.
- The fourth offline run disproved that remaining hypothesis: all five individual modules replayed successfully, but Lake's aggregate import-computation job still exited 1. Wi-Fi was restored immediately. Moving only the retained fixture's derived `.lake` directory aside and rebuilding clean reproduced the same result, so stale artifacts are not the cause; the prior derived tree remains recoverable as `.lake.m5-root-debug-backup` until acceptance cleanup.
- Pinned Lake 4.32.1 source inspection and an exact host reproduction localized the contract. `roots` defines the library namespace and also supplies exact default globs; explicitly combining source roots with overlapping file/directory globs makes `recCollectLocalModules` report bad imports even though every source compiles. The exact five-file fixture fails with explicit globs and succeeds with roots `Extra`, `Extra.Nested`, `Main`, `TestProj2.Basic`, and `TestProj2.New` and no `globs` field. The implementation now emits every contained source module as a sorted root and deliberately omits `globs`. Exact next step is focused and aggregate validation, update-install, a final offline tablet run, then removal of only the created fixture files and derived debug backup after success.
- Final roots-only validation passed: focused `:core-project:testDebugUnitTest` completed in 1m52s (51 tasks; 5 executed), and the required aggregate `testDebugUnitTest :app:assembleDebug` completed in 3m50s (175 tasks; 4 executed). The exact default Android1 APK is 806,301,308 bytes with SHA-256 `8ff8b2d03f468fc739dcce5a60b814268cbb84853d0d45adc8abe016ab27fd1d`; owner-user-0 update installation succeeded.
- On-device reconciliation produced roots `Extra`, `Extra.Nested`, `Main`, `TestProj2.Basic`, and `TestProj2.New` with no explicit globs. With Wi-Fi disabled, the real Run action exited 0 in 3,048 ms, Lake completed all seven jobs successfully, `Main.lean` executed and printed the expected `TestProj2.answer : Nat`, and LSP remained Ready. Wi-Fi was restored immediately.
- Deleted the three acceptance sources through the real tab lifecycle UI in dependency-safe order (`Extra/Nested.lean`, `Extra.lean`, `TestProj2/New.lean`). Reconciliation restored the original roots `Main` and `TestProj2.Basic`; the temporary derived `.lake.m5-root-debug-backup` was then removed exactly. A force-stop/cold launch preserved the original two tabs and Ready LSP with the canonical roots-only configuration.
- Final tablet state is the preserved landscape baseline: physical 1600×2560, accelerometer rotation 0, user rotation 1, Wi-Fi enabled. Both the accepted Android1 app and the isolated Android2 candidate opened briefly during package-path diagnosis are force-stopped; exact checks show no `lean`, `lake`, `liblean_exe.so`, or `liblake_exe.so` process. M5 dynamic Lake module coverage is complete. Mathlib remains paused with its 512 finalized artifacts untouched. Exact foreground next step is M5.0 Output presentation; exact producer state remains deliberately paused.

## 2026-08-16 — M5.0 Output presentation in progress

- Began Priority 1 from committed dynamic-module baseline `6633507`; the worktree was clean and the tablet was restored/force-stopped with no native child. Mathlib remains deliberately paused and untouched.
- Scope is a persistent Settings → Interface → Output presentation choice, existing docked behavior with automatic Build/Run reveal, popup-only output layered over the workspace with deterministic dismiss/reopen/recreation behavior, and no change to Messages or Goals. Exact next step is to extract the preference/reveal state model, wire settings and presentation surfaces, add focused host tests, then run aggregate and physical compact/tablet acceptance.
- Implemented persisted Docked/Popup parsing with a pure reveal-state model, Settings → Interface radio choices, automatic selected-mode reveal for both Play and drawer Build, and a More → Show Output action that reopens the current bounded result without rerunning. Popup mode removes Output from the editor split while retaining the saved docked fraction, uses an in-app dismissible dialog with Back/Escape behavior and cancellation, and leaves Messages/Goals unchanged. Focused preference test plus app Kotlin compilation passed in 4m49s (106 tasks; 5 executed, 101 up-to-date). Exact next step is the required aggregate regression and physical mode/persistence/recreation/cancellation checks.
- The first aggregate pass succeeded in 5m11s (175 tasks; 10 executed), and the first physical pass proved real Settings persistence, automatic Popup Run, Exit 0, direct dismiss/reopen, and popup visibility across orientation recreation. That recreation exposed one honest gap: the popup survived but its completed bounded result reset to the idle text because `runState` was not saveable.
- Added a saveable rendered Output snapshot updated at each bounded run/integrity state transition. A first focused compile caught and corrected one Kotlin visibility error before any replacement install; the corrected focused test/compile passed in 3m20s (106 tasks; 5 executed), and the final aggregate regression passed in 3m33s (175 tasks; 4 executed, 171 up-to-date).
- Final Android1 APK is 806,339,848 bytes with SHA-256 `32f5b0411cd8ea96174d2c84aefdbc4f12dd3ec7aa1964ced87f180f244c3808`; owner-user-0 update installation succeeded. In persisted Popup mode, Run exited 0 in 2,934 ms, the Output dialog and exact completed text survived landscape→portrait Activity recreation, and the UI was restored to landscape. The real Interface setting was then restored to Docked; after manually collapsing Output, Run automatically expanded it and exited 0 in 2,829 ms while Goals stayed Ready.
- Final device state is physical 1600×2560, accelerometer rotation 0, user rotation 1, Wi-Fi enabled, Docked Output preference, and the app force-stopped. Exact `pidof` checks are empty for the app, `lean`, `lake`, `liblean_exe.so`, and `liblake_exe.so`. M5.0 Priority 1 is complete; Priority 2 automatic inline LSP completion remains the exact next foreground increment. Mathlib remains paused and untouched.

## 2026-08-16 — M5.0 automatic inline completion in progress

- Began Priority 2 from clean commit `d379aa8`. Existing evidence shows completion requests already use generation/document-version rejection but responses are reduced to labels and rendered in Goals only; requests are manual and have no edit semantics. The tablet remains at the restored stopped baseline and Mathlib remains paused.
- Scope is a persisted Settings → Editor completions toggle, bounded automatic debounce after normal text edits, cancellation/stale-position rejection, parsed LSP label/insertText/textEdit data, a caret-adjacent accessible popup, safe selection insertion through the normal editor history/recovery/LSP path, and removal of completion text from Goals. Exact next step is to add/test the pure completion parser/edit model before wiring Activity and Compose state.
- Implemented the pure candidate parser/edit boundary and automatic UI flow. It accepts list or CompletionList responses, label/detail/plain insertText and TextEdit/InsertReplaceEdit insertion ranges, falls back to the current identifier prefix, refuses automatic snippet expansion by inserting the reviewed label instead, and routes the chosen edit through the normal single editor-history mutation. Automatic requests require Ready LSP, collapsed selection, no IME composition, and an identifier prefix; they debounce 240 ms, synchronize documents first, and reject stale generation, version, path, and position responses.
- At the user's direction, retained and visible automatic suggestions are capped at exactly five. The caret-adjacent popup exposes selectable semantics, touch insertion, Up/Down/Enter/Escape handling without taking editor focus, and the previous Goals-pane completion text/manual action are removed. Settings → Editor persists an Automatic completions switch and disabling it clears pending UI/request state. Focused parser/edit/bound tests plus app Kotlin compilation passed in 3m15s (106 tasks; 5 executed, 101 up-to-date). Exact next step is aggregate regression, then real-LSP typing/insertion, disable, rapid-stale, recreation, compact/IME, and no-orphan device acceptance.
- Aggregate `testDebugUnitTest :app:assembleDebug` passed in 3m44s (175 tasks; 5 executed), producing an 806,387,252-byte APK with SHA-256 `dba62ef815414e29f9c1178c0d057d2cd6e521def9302bf9af531c38438040ed`; owner-user-0 update installation succeeded. Real Ready-LSP typing of `#check Nat.su` on the API-33 tablet displayed exactly five suggestions above the visible IME, and touch selection replaced the server range with `Nat.toSubscriptString` through the editor path while Goals remained completion-free.
- Physical inspection caught and corrected a double-counted popup anchor and immediate same-position re-request after insertion. The popup now uses caret coordinates relative to its Compose anchor and suppresses automatic reopening at the just-inserted position until the user edits or moves again. The corrected focused completion tests plus Kotlin compilation passed in 3m08s (106 tasks; 5 executed, 101 up-to-date), and the final aggregate passed in 3m38s (175 tasks; 4 executed, 171 up-to-date).
- The final owner-user-0 update install succeeded. Its APK is 806,387,168 bytes with SHA-256 `5da0cae97c2f7b85ce9f6bc6ed766c40a6750f064df9294edd3d3caaf7da1842`. Both temporary completion-test sources were removed through the real file lifecycle, restoring the original two-tab project topology. M5.0 Priority 2 is host-complete and real-LSP/IME/insertion proven; a repeat screenshot of the corrected relative anchor is the only residual physical polish check and does not reopen the five-item/edit contract. Mathlib remains paused and untouched; exact next foreground milestone is M5.1 general project files and program streams.

## 2026-08-16 — M5.1 general files and program streams in progress

- Began M5.1 from committed M5.0 baseline `27e898b` with a clean worktree. The reference tablet is restored to landscape (accelerometer rotation 0, user rotation 1), Wi-Fi enabled, owner-user-0 app force-stopped, and exact app/Lean/Lake child checks empty. Mathlib remains deliberately paused with its incremental artifacts untouched.
- Following `docs/project/STDIN_SUPPORT.md`, the first implementation slice is the typed `core-process` stdin plan/snapshot/result model and single-owner bounded writer with fake-process race/backpressure tests. Immediate EOF remains the default; UI/service/general-file expansion follows only after the process contract is proven. Exact next step is to inventory the existing launcher/supervisor API and add the typed model without exposing streams to Compose.
- Added typed `StdinPlan`, `StdinState`, and input-operation results without exposing `OutputStream` above `core-process`. Every existing job now defaults to explicit immediate EOF. Interactive jobs use one daemon writer, atomically accept or reject UTF-8 submissions against a 64 KiB pending-byte limit, preserve send/send-line order, order EOF after accepted bytes, reject post-EOF input, and close input during cancellation/process completion. Focused supervisor tests passed in 2m40s (27 tasks; 5 executed), including observed default EOF, exact Unicode+LF delivery, bounded rejection, explicit EOF, post-close rejection, output retention, and owned cancellation. Exact next step is to harden blocking-write/cancellation and broken-pipe races, then add streamed validated byte sources and richer immutable stream snapshots before service/UI wiring.

## 2026-08-16 — Editor width/completion corrective in progress

- Manual tablet testing found that the completion popup wrapper caused the `BasicTextField` hit target to wrap its longest line instead of filling the editor viewport, so clicks in trailing line space were outside the editable area. It also found automatic requests were driven by selection changes as well as edits and were intermittent for expected prefixes such as `TestProj2.a` and `rf`.
- Corrective scope is to retain a full-width editor hit target while preserving horizontal overflow, trigger completion only from actual text-entry/IME-commit events (never cursor-only changes), and physically prove the two reported prefixes before resuming stdin UI work. Exact next step is a narrow Compose layout/trigger-state correction with pure trigger tests, followed by aggregate APK and tablet replay. Mathlib remains paused.
- Replaced the content-wrapping popup container with a full-width editor viewport whose horizontally scrollable child has the viewport as its minimum width and retains natural multi-line height. Physical testing proved the actual `EditText` bounds span `[113,333][2508,435]`; tapping trailing space on line 2 and typing appended at that line's end, while multiple lines and the large editor surface remained visible.
- Automatic completion now records only real text mutations or IME composition commits. Cursor/selection-only changes dismiss candidates without issuing a new request. Candidate filtering and prefix-first ranking now happen across the full server response before the five-item cap, rather than capping unrelated raw results first. The exact imported `TestProj2.a` replay showed `answer`/`answer_is_positive`; the exact `rf` tactic replay showed `rfl`, `rfl'`, `apply_rfl`, and `ac_rfl`, while a cursor-only screenshot showed no popup.
- Focused trigger/parser/ranking tests and Kotlin compilation passed in 3m52s (106 tasks; 5 executed, 101 up-to-date). The preceding full corrective regression passed in 4m28s (175 tasks; 14 executed), and final APK assembly after prefix ranking passed in 4m39s (144 tasks; 3 executed). The installed final APK is 806,394,544 bytes with SHA-256 `5abe6b154428c40d4068fc87b9a3790f04840d02fcb21c5216886c8e6548d065`. The temporary source was deleted through the real lifecycle, and the app is force-stopped. Exact next step resumes M5.1 stdin service/UI wiring.
- Resumed stdin at the retained-service boundary. `ProjectJobService.start` now accepts a typed stdin plan; reconnectable snapshots include live stdin state; and typed `send`, send-line selection, and `closeInput` operations reject unknown/stale job IDs without exposing streams. `ProcessJobSupervisor` publishes accepted/pending/dequeued/closed/failure stdin transitions so service listeners do not need to poll. Focused process tests plus app/service compilation passed in 3m43s (96 tasks; 9 executed, 87 up-to-date). Exact next step is to move the Activity's build-then-run chain onto this service and add the explicit None/Interactive run choice and Output composer; no user-facing stdin control is claimed yet.
- Continued the retained-service wiring by ensuring every asynchronous stdin transition—not only direct binder operations—publishes a fresh snapshot containing both job and stdin state. The service now constructs all initial, listener, and reconnect snapshots from the supervisor's authoritative state, while fast-start races safely fall back to the returned/snapshotted terminal state. Focused supervisor tests plus full app Kotlin compilation passed in 3m43s (96 tasks; 9 executed, 87 up-to-date). The remaining boundary is Activity build/run migration and Compose controls.
- Began the Activity migration: it now binds/unbinds `ProjectJobService` alongside the LSP service, registers exactly one listener per binding, and retains snapshots keyed by typed job ID in Compose-observable state. This establishes the reconnect/render feed without yet switching launch ownership or presenting controls. Exact next step is to replace `activeJob` build/run callbacks with job-ID callbacks against this feed, then expose mode/input operations.
- The Activity binding/snapshot feed compiles successfully in 2m48s (87 tasks; 1 executed, 86 up-to-date). No APK/device mutation was needed for this non-visible ownership slice. Exact next step remains switching the build-then-run chain from Activity-owned supervisors to service job IDs; the existing UI still runs with immediate EOF until that cutover is complete.

## 2026-08-16 — M5.1 retained Build/Run ownership cutover in progress

- Began from the clean Activity-binding checkpoint with Mathlib still paused. Scope is to replace Activity-owned Build/Run supervisor callbacks with typed retained-service job IDs and authoritative snapshots, while preserving build-then-run ordering, bounded output, cancellation, Output reveal behavior, configuration reconnection, and immediate-EOF behavior until explicit input controls are wired.
- No Gradle, Lean, Lake, device, or Mathlib producer process is assumed alive. Exact next step is to inventory the current Activity launch state and service API, implement the smallest ownership cutover with focused tests, and record validation or the exact recovery command before proceeding to the run-mode composer.
- The Activity-owned `ProcessJobSupervisor` has been removed from Build/Run. Build and program phases now start through `ProjectJobService`, are correlated by typed job IDs, preserve the existing combined result and elapsed-time presentation, and cancel only the current service-owned phase. Activity destruction no longer directly closes the child; listener snapshots drive phase transitions.
- Narrow `:app:compileDebugKotlin` validation passed in 2m44s (87 tasks; 1 executed, 86 up-to-date) through the durable runner. The first restricted attempt failed before task execution with the known wildcard-IP socket limitation; the approved identical invocation succeeded. Exact next step is to add transition-focused coverage and pass the selected stdin plan only to the program phase, then expose the run configuration/composer and perform aggregate/device lifecycle validation.
- Lifecycle review after compilation found that this is deliberately not yet the completed ownership boundary: the service owns each child, but `ActiveRunChain` still lives in the Activity, so a configuration change between build completion and program launch cannot reconstruct the phase transition or callback in the new Activity. No device build was installed and no completion claim is made. The exact next implementation step is to move the sequence phase/build-result/selected-stdin-plan metadata and transition logic into `ProjectJobService`, expose a reconnectable sequence snapshot, and make Compose derive running/result state from that snapshot before adding interactive controls.
- Closed that lifecycle gap by moving build/program phase, active job ID, build result, entry point, selected stdin plan, elapsed time, terminal result composition, cancellation, and input routing into `ProjectJobService`. `ProjectRunSnapshot` embeds the authoritative active process/stdin snapshot; new Activity bindings receive retained sequence snapshots and Compose derives running or terminal Output state from them, while only the program phase receives the selected stdin plan.
- Focused `ProjectRunSequenceTest` plus app Kotlin compilation passed in 4m13s (106 tasks; 6 executed, 100 up-to-date), including preservation of program exit/timed-out state and ordered build/program stdout/stderr composition. The preceding compile-only pass also succeeded in 2m50s. No APK/device mutation occurred. Exact next step is the explicit None/Interactive run selection and reconnectable Output input composer using sequence-ID `sendToRun`, `closeRunInput`, and `cancelRun` operations.

## 2026-08-16 — M5.1 interactive Run UI in progress

- Began from committed service-sequence baseline `0b37575`. Scope is an explicit None (EOF)/Interactive Run choice and an Output-owned text composer with Send line, Send, EOF, and Cancel actions routed by retained sequence ID. Project-file input remains deferred until the contained immutable byte-source contract is implemented; the UI must not advertise it prematurely.
- Exact next step is to thread the selected `StdinPlan` only into the program phase, render controls solely from the authoritative `ProjectRunSnapshot.stdin` state, clear text only after accepted sends, and validate focused state/Compose compilation before aggregate and physical lifecycle work. No process or device mutation is active at this checkpoint.
- Implemented the explicit Run dialog with None (EOF) and Interactive choices; both Play and drawer Build use it. Only the program phase receives `StdinPlan.Interactive`. While its authoritative stdin snapshot is open, Docked or Popup Output shows a multiline composer with Send line, exact Send, EOF, and Cancel; accepted sends clear the draft, rejected sends remain visible with a truthful reason, pending byte counts are shown, and nonempty drafts require confirmation before EOF.
- The first compile exposed missing retained-run parameters in the nested shared editor layout and failed before producing an APK; those parameters were threaded explicitly and the corrected compile passed in 3m02s. Full `testDebugUnitTest :app:assembleDebug` then passed in 5m06s (175 tasks; 18 executed, 157 up-to-date). The APK is 806,468,456 bytes with SHA-256 `4c16020a7108490d8e5db70768472a0064d21e48340589e75a66a8b726921f2a`. No install/device mutation has occurred yet. Exact next step is physical interactive send/EOF/cancel/recreation validation, followed by contained project-file stdin.
- Physical API-33 testing first proved the dialog selection itself was correct but exposed that both `lake lean Main.lean` and direct plain file elaboration execute the fixture's `#eval main` with EOF instead of presenting the supervised pipe to a user program. The supported program phase now runs the pinned executable as `lean --run Main.lean` after Lake build, with `.lake/build/lib/lean` prepended to deterministic `LEAN_PATH`; Lake build and LSP ownership remain unchanged.
- Focused toolchain/project tests plus app compilation passed in 3m55s (105 tasks; 11 executed, 94 up-to-date), and the final `--run` APK assembly passed in 4m04s (144 tasks; 6 executed, 138 up-to-date). The installed APK is 806,468,456 bytes with SHA-256 `d19fac4c2c765d2631cfdc9e7d68859d5e4fb937c35cf342600abc5ad4e21a22`. On the tablet, a visibly selected Interactive run reached `Input open`, displayed the Output composer, accepted `Ada` through Send line, and exited 0 with `Hello, Ada!` after 37,903 ms. This proves the real pipe, writer, UI acceptance, and natural exit path. Rotation/EOF/cancel remain pending; an attempted rotation test did not start a new run and therefore supplied no lifecycle evidence.
- UI automation accidentally appended two characters to `TestProj2/Main.lean`; the app was force-stopped and the source was restored byte-for-byte from the previously observed baseline. Host and device SHA-256 both equal `3cca81cb9e95452b4a8e9e3e7ec54ad82821be0558d3da965c2b5a283e1709d0`; the corrupted recovery snapshot and pushed fixture were then removed. The app is force-stopped, exact app/Lean/Lake process checks are empty, rotation is restored to accelerometer 0/user 1, and Wi-Fi is enabled. Exact next step is EOF/cancel/recreation acceptance, then contained project-file stdin.

## 2026-08-16 — M5.1 interactive lifecycle acceptance in progress

- Began from committed direct-program baseline `abdb02b` with the exact restored `TestProj2` fixture, stopped app/no native children, fixed rotation accelerometer 0/user 1, and Wi-Fi enabled. Scope is explicit EOF, cancellation of a program blocked on stdin, and controlled Activity recreation while the retained input channel is open.
- Exact next step is to repeat the visibly selected Interactive launch without editing source, exercise EOF and cancellation separately, then use an Activity-only recreation command that does not background the task or alter rotation. Any service-lifetime failure will be treated as implementation evidence rather than hidden by relaunching.
- Physical explicit EOF passed in fixed landscape: the program reached `Input open`, EOF was sent through the composer, and it exited 0 after 19,654 ms with the expected empty-name output. The first blocked-input cancellation exposed uncaught Android `InterruptedIOException` failures in both stdout/stderr drain threads, which killed the app process. `ProcessJobSupervisor` now contains close-induced drain interruption during cancellation and converts non-cancellation drain I/O failures into a terminal failed state; focused supervisor tests passed in 1m57s (27 tasks; 5 executed, 22 up-to-date), including a blocking stream that throws on cancellation.
- The corrected cancellation APK assembly passed in 3m42s (144 tasks; 7 executed, 137 up-to-date). Physical replay reached `Input open`, Cancel produced visible `Run cancelled.`, the app process remained alive, and no `lean --run` child remained. A subsequent rotation then exposed a separate service-destruction race: the bound-only service was destroyed during the configuration gap, cleared `jobs`, and its close callback crashed in `runSnapshot` because the active job had already been removed.
- Active sequences now place `ProjectJobService` in started state and drop that state after the last sequence becomes terminal, preserving ownership across configuration binding gaps. `onDestroy` closes jobs while snapshot state still exists and clears maps afterward. App Kotlin compilation passed in 2m22s (87 tasks; 1 executed, 86 up-to-date). This latest started-service fix is host-compiled but not yet installed/physically rechecked. The tablet is restored to accelerometer 0/user rotation 1 with the app force-stopped and exact app/Lean/Lake checks empty. Exact next step is assemble/install and repeat open-input rotation plus cancellation, then proceed to project-file stdin.

## 2026-08-16 — M5.1 started-service physical recheck in progress

- Began from committed lifecycle-fix baseline `8a458e7`. Exact scope is assemble/install, start a visibly Interactive `lean --run`, rotate with input open, confirm the new Activity receives the retained sequence/input snapshot, then cancel and prove app survival plus no program child. No broader device or project mutation is authorized by this check.
- APK assembly passed in 3m27s (144 tasks; 3 executed, 141 up-to-date). The candidate is 806,468,892 bytes with SHA-256 `6f11a835253dc795e86437896b773f47ada75e9d8e5c23a9b032b3328773f40c`. Installation and the physical rotation/cancellation replay remain pending; no process is assumed alive. Exact resume step is owner-user-0 update-install this hash, wake/launch in the restored accelerometer-0/user-1 baseline, then perform the open-input rotation test before any project-file work.
